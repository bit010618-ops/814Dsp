# 当前进度：全文数字信号处理讲义

> 2026-08-24 全书候选重导出与真题分区复核：修正 2025 年第七题第 4 小题系统图中被静态导出遗漏的乘法器标注，现以完整 MathJax 公式 \((-1)^n\) 显示；重建、页码回填和导出后的候选为 533 页，150 个题面详解页码全部回填，未发现 `P.____` 占位或非章节末尾的空白页。代表页复核确认第二章真题页的页眉、题面和“详解见 P.469”一致；附录 F 保持为书末详解区。整书装配、训练位置、图源队列、页码回填及该图形组件回归为 `20 passed, 1 deselected`；被排除项仅为已知 Edge 浏览器进程环境测试。候选 PDF：`full/outputs/数字信号处理讲义_图源队列完成候选.pdf`，仍为阶段性候选，待后续全书逐章审阅，不得称为最终定稿。

> 2026-08-24 含图真题队列完成：第二章 2022 年第八题的延时差分流图、零极点图与幅频图已按真实系统关系导出并目视复核；2025 年第七题第 4 小题修正了误把行内 MathJax SVG 当图形放大的规则，重新绘制四幅采样／频移／滤波／恢复频谱并消除图内标题、标签和刻度冲突。两个条目均以静态 A4 PDF 实际复核通过，至此含图真题修复队列的 11 个源候选均标记为已验证。浏览器 DOM 两项测试仍受本机 Edge 退出码 `2147483651` 影响；本轮以本地 MathJax 静态渲染和 WeasyPrint 实际导出的 PDF 页面为验收依据。

> 2026-08-24 真题分区页眉页脚硬约束补充：用户再次强调各章真题必须跟在对应章节正文后，书末只保留附录 F 详解；已把章节真题区与附录 F 的标题、页眉及仅保留连续页码的页脚要求写入永久上下文。后续每次全书重排均须逐页核对分区标题、页眉和页脚，不得残留总真题区或“真题整理讲解序”等旧标题。

> 2026-08-24 训练题空白尾页修复：全书候选 PDF 审计定位到第七、八章若干“只有页眉与页码”的空页，根因为固定 `105mm` 答题区在单题页内容后被挤到下一页。用户不要求固定批注区，故全书训练答题区改为自然高度；保留“单题一页”规则和题页自然留白。结构／图源／页码回填回归 16 项通过；重导出后全书由 536 页降为 533 页，扫描未发现正文或训练中仅含页眉和页码的空页，150 个“详解见 P.____”仍全部回填。候选仍为阶段性输出，未宣称最终验收。

> 2026-08-24 分章真题重排候选导出：按新全局结构完成候选稿重排并两次页码回填；全书为 536 页。八个“第 X 章真题整理”均位于对应章节正文／公式总表之后，书末详解区标题与页眉均为“附录 F：真题整理详解”，150 个题面“详解见 P.____”全部回填。视觉复核发现第八章题页曾被强制分页挤成独立空白标题页，已改为章标题与第一题同页，复核通过；同时将旧题干的括号式 LaTeX 与 `omega_m` 兼容转为统一 MathJax 公式。结构、图源队列、页码回填回归共 15 项通过。候选 PDF：`full/outputs/数字信号处理讲义_章末真题重排候选.pdf`；这是当前阶段候选，不得误称为全书最终验收版。

> 2026-08-24 全局真题编排规则修正：用户再次确认题面必须随所属章节正文结束后立即出现，而不是集中堆在全书末尾；书末只保留完整答案。已将此写入 `.codex/CONTEXT.md` 的永久约束，并把总装配器改为“第 X 章正文及公式总表 → 第 X 章真题整理 → 下一章正文”，每题仍为独立题页；答案总区标题改为“附录 F：真题整理详解”，不再使用“真题整理讲解序”或“华理 814 历年 DSP 真题整理详解”。装配回归 11 项通过，证明八个章末真题标题均位于下一章之前，书末只保留答案区。规则记录已本地提交 `f776067`，但本次 `git push origin main` 遭本机凭据管理器 `SEC_E_NO_CREDENTIALS` 拒绝，尚未上传；不得宣称已推送。

> 2026-08-22 公式名称与作用说明修正：用户纠正公式前不能写“第几章核心关系”或“某小节关系式”；必须让读者直接知道该公式是什么、用于什么计算或判别。根因是通用公式命名器对 749 条未识别公式回退为“知识点名称的关系式（用于说明该性质中各变量的对应关系）”，依旧是目录式空话。已先写失败回归：`DFT 的基本性质`下的等式必须命名为“DFT 基本性质关系（用于根据时域运算快速推导对应的频域结果）”。随后将回退规则改为按实际知识对象和动作命名，并加入线性、卷积、采样、滤波、FFT 等常见语义分类。验证：`full/tests/test_build_all_main_body.py` 与 `full/tests/test_build_full_handout.py` 为 `17 passed`；重建全书 HTML 后 1,208 条公式说明中“核心关系”“核心公式”“用于说明该性质中各变量的对应关系”“第 X 章核心”均为 0。仍有 344 条未能细分到标准公式名的“知识点＋计算表达式”说明，已明确写明其用于按本节已知条件求结果，后续将随逐式审核继续细化。

> 2026-08-22 正文分页空白修复与视觉验收：对八章正文首轮 WeasyPrint PDF 的非末页密度审计发现，章节公式总表被 `break-before:page` 强制推到下一页，造成六处不必要的大面积下半页空白。先新增回归测试并确认其失败，再把公式总表改为自然续排；正文结束后会立即接入公式总表，且公式总表仍保持在每章最后的内容序列中。相关 9 项生成、分章和静态公式回归均通过。重新导出的八章 PDF 已用 PyMuPDF 逐页密度审计，所有非末页均未触发空白阈值；原问题页的 150 dpi 视觉复核确认公式、标题和正文自然衔接，无 LaTeX 源码、裁切或重叠。为恢复这一图像级验收能力，用户已授权下载，项目隔离目录安装 PyMuPDF 1.28.2；该运行时和临时 PNG 不纳入 Git 提交。

> 2026-08-22 正式 PDF 导出环境恢复：用户授权下载后，已从 WeasyPrint 官方 Windows 发行包安装 WeasyPrint 69.0 至项目隔离目录 `full/vendor/weasyprint/`（安装包 SHA-256：`330101FF3EA50EBDE4ABF805283B6D703D5F3D71C77C983DB94357EC4524A3EF`）。以本地 MathJax 静态公式导出第七章 7.4 的真实 A4 页面并经 150 dpi 渲染复核：中文、表格、竖式分数、上下标和多行公式均完整，无 LaTeX 源码、裁切或重叠；为宽展示公式加入仅在超出容器时的等比适配规则。WeasyPrint 对现有网页式 SVG 的外部样式和 `foreignObject` 支持不足，会导致复杂 DSP 图出现黑块或标签失效，因此不得直接用于该类图形；后续须将复杂结构图替换为精确裁取的原课件技术图或可由该导出器正确渲染的独立矢量图，再进行全书导出。安装二进制、缓存和诊断产物均不纳入提交。

> 2026-08-22 SVG 静态导出兼容修复：根因验证后，静态 MathJax 导出器现将 SVG 内的 `foreignObject` 公式标签转换为 MathJax 生成的嵌入式矢量图，并把匹配的 SVG 样式规则内联到对应图形。第七章 7.1 实际 A4 PDF 第 3 页经 130 dpi 渲染复核：FIR 时/频域关系图、幅频响应图的箭头、曲线、坐标、中文标签和数学标签均完整，黑块与挤在左上角的原始标签均已消失。该处理保持公式来自完整 LaTeX→MathJax，不改为普通 HTML 或手工拼接；后续全书分章导出将使用此链逐页复核。

> 2026-08-22 独立 PDF 导出链恢复：用户授权后，从 wkhtmltopdf 官方发布页下载并安装 0.12.6（patched Qt）至 `full/vendor/wkhtmltopdf/`；安装程序未提供 Authenticode 签名，已记录 SHA-256，且仅使用项目隔离目录。根因验证表明该引擎不能直接执行 MathJax 3，会将 LaTeX 原文印出；新增 `prerender_mathjax_svg.js`，先由本地 MathJax 将完整公式整体转为 SVG，再交给该引擎分页。单一章节实测可生成 A4 PDF（第一章 26 页）。首轮全书单进程排版在约 29 MB 静态 HTML、四千余公式下持续计算八分钟未落盘，故停止未成品并转为逐章导出。首章视觉抽检又发现正文通用 `svg{max-width:100%;height:auto}` 会使旧 Qt 引擎隐藏 MathJax SVG；已以专用 `.mathjax-svg` 类隔离该规则，并通过单公式和章节回归。剩余工作：继续调校静态公式字号/页首排版，并分章导出、合并、逐页复核；尚不得把此候选称为最终 PDF。

> 2026-08-22 离线 PDF 引擎安装审计：在用户授予仅网络安装权限后，尝试将 WeasyPrint 安装到独立目录 `full/vendor/weasyprint_py`。两次启动均未创建目标目录，Python 安装进程持续占用 CPU 超过两分钟且未输出可用状态；诊断时重复启动的第二进程已先停止，随后停止原始异常进程。未删除任何项目文件，未上传任何资料，也未完成 WeasyPrint 安装。按“连续失败不盲试”的规则，当前 Python/pip 安装链不可继续作为 PDF 导出方案；须改选并重新验证其他离线导出架构。

> 2026-08-22 真题导航 ID 基线：附录 E 的每个导航行现输出唯一 `data-exam-id`，与审计清单 156 个纳入单元一一对应；装配回归验证该 ID 数为 156 且无重复。训练题页和书末详解尚未输出相同 ID，故当前不能用页数或生成顺序替代题号级覆盖；后续映射必须逐题依据原始题干/审核清单建立，不能机械按页面序号绑定。

> 2026-08-22 真题覆盖计数更正：导航中的 156 是可拆分的纳入单元，实际归属于 91 道原始候选题；成品中的 153 是训练分页数。由于同一候选题可拆成多个独立单元，且一题可跨页，156 与 153 不能直接相减，也不能据此断言缺题。此前按年份页面数推得“至少缺 8 个单元”的结论已撤回。当前正确状态是：训练/详解的题号级覆盖尚未具备可机器验证的 ID 标记，不能宣称附录 F 对 156 单元已完成逐项覆盖；后续须给训练与详解增加可追溯题目 ID，再以清单审计。

> 2026-08-22 附录 A/B/C/D/I 装配：新增可复用附录生成器。附录 A 以八章正文中的公式总表为唯一公式来源，抽出八组正式 MathJax 公式；附录 B 提供系统性质、z 变换/ROC、DFT/循环卷积和 IIR/FIR 的标准作答模板；附录 C 为八章各给一题与正文例题、真题去重的综合训练和完整解答；附录 D 给出高频公式与计算检查；附录 I 给出八章闭环自测和最后两分钟检查。至此 A/B/C/D/E/F/I 均已进入全书 HTML 装配，G/H 未生成。装配/正文回归 7 项通过；输出静态核对为 8 个附录 A 公式组、8 道附录 C 题、CDN 引用 0、禁用来源性措辞 0。PDF 可视验收仍等待离线 PDF 引擎授权。

> 2026-08-22 附录 E/F 装配：全书装配器已从 `exam_training_manifest.json` 自动生成“附录 E：华理 814 真题考点导航”，按八章列出全部 156 个唯一审计单元的年份、训练位置、题目与待回填详解页码；原集中详解区已明确为“附录 F：华理 814 历年 DSP 真题整理详解”。导航在分章训练后、附录 F 前，避免与正文或题页混排。全书装配与正文公式总表回归 6 项通过；离线 HTML 中 CDN 引用为 0。最终 PDF 页码和附录 A/B/C/D/I 仍未完成。

> 2026-08-22 本地 MathJax 实测：已将 MathJax 3.2.2 的官方 ES5 发行包置于 `full/vendor/mathjax/`，公式脚本改为本地 `file:` URI；Node 直接渲染 `\delta(n)=\begin{cases}1,&n=0\\0,&n\ne0\end{cases}` 返回正式 `mjx-container` 数学结构，未保留 `\begin{cases}` 源码。离线公式渲染链已闭环；浏览器 PDF 导出仍是独立的 GPU 进程故障，尚未据此宣称解决。

> 2026-08-21 第七章重点训练提交：`0251bee`（Add chapter seven FIR training）已推送至 `origin/main`；仅含三道重点 FIR 训练及详解、全书装配接入、回归测试和任务记录。

> 2026-08-21 第七章重点训练完成：完成 2022 年海明窗带阻 FIR、2024 年线性相位 FIR 零点约束、2025 年矩形窗线性相位高通 FIR 三道训练及书末详解；按原卷逐页转写，候选全书训练题面由 99 增至 102 道。组件、全书装配及正文相关回归 40 项通过；第七章余下 8 道补充题继续制作。

> 2026-08-21 第八章训练提交：`a34c585`（Add chapter eight multirate training）及 `0e66c7e`（格式检查修复）已推送至 `origin/main`；仅含第八章训练、对应详解、全书装配接入、回归测试与任务记录。

> 2026-08-21 第八章训练完成：按原卷逐页核对后，完成 2013、2015、2016、2020 年四道多采样率／复用题的单题页和书末详解；两道带水印多采样率题图以无水印系统链与教材式频谱重绘。全书候选训练题面由 95 增至 99 道；组件、全书装配和正文相关回归 39 项通过。仍不宣称 PDF 视觉验收，因为浏览器 headless GPU 缓存故障未恢复。

> 2026-08-21 训练装配提交：`0f223ae`（Assemble verified chapter training components）已推送到 `origin/main`；仅含全书装配器、回归测试、当前任务记录和项目阶段状态。接下来补第七、八章训练及详解。

> 2026-08-21 训练装配推进：`build_full_handout.py` 已接入全部既有第一、二章补充训练批次及第 1--6 章既有训练/书末详解，当前候选全书 HTML 含 95 道训练题面。与 156 道审计清单相比尚缺 61 道（第 1、2、3、4、6、7、8 章分别缺 3、1、31、9、2、11、4 道），下一步优先补第七、八章。新增全书装配回归通过；但本机 Edge 和 Chromium headless 都因 `DawnGraphiteCache` GPU 缓存后端错误崩溃，故本阶段未生成或宣称新的 PDF 视觉验收结果。

> 2026-08-21 推送成功：循环索引图片格式确认提交 `c20cb2a` 已正常推送至 `origin/main`；仅包含该项规则记录、任务记录和项目进度记录。

> 2026-08-21 循环索引图片格式最终确认：以用户提供的 `x((n+m))_N` 样式为准，所有读者可见的循环索引统一采用完整圆括号外的周期下标；有效正文源中未发现 `mod N`、`\bmod N`、`\operatorname{mod} N`、`\mathrm{mod} N` 或 `\pmod N`。本次仅补充持久化需求记录；相位主值等非循环模运算不改动。

> 2026-08-21 提交与推送：第六章 IIR 正文图形本地提交为 38ac16a（Add chapter six IIR design diagrams）。常规 git push origin main 继续失败于同一项本机 Git 凭据错误 SEC_E_NO_CREDENTIALS；未读取或绕过凭据。

> 2026-08-21 第六章 IIR 正文图形补全：根据源页与当前组件覆盖对照，补入三张必要正文图而非真题内容：IIR 设计路线、脉冲响应不变法时域采样路线、双线性变换 s 平面至 z 平面的稳定区域映射。三图均为无水印 SVG，模块、箭头、单位圆、坐标轴和稳定区域由真实关系绘制；数学标签由完整 LaTeX/MathJax 渲染。首轮 PDF 发现 6.3 流程图因整图保护被推页，造成上页底部空白；已裁紧 SVG 视区而未缩小字形，令图自然接在该页末尾。组件与全书装配 3 passed；139 页候选 PDF 导出 math=3926、unrendered=[]、pageErrors=[]，160 dpi 实查第 72--75 页无水印、黑图、裁切、重叠、公式源码或非章节末尾大面积空白。

> 2026-08-21 提交与推送：第七章频谱关系图本地提交为 e80c77b（Add chapter seven FIR spectrum figure）。常规 git push origin main 仍被本机 Git 凭据管理器阻止：schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS；未读取或尝试绕过凭据。

> 2026-08-21 第七章正文图形补全：根据用户指出的“缺表格、缺蝶形／流程／滤波器图及黑图”风险，先对当前第七章正文成品页逐页复核；四类线性相位表、零极点图、对称中心 stem 图和频率采样图均已存在且清晰。为补全原课件中“输入频谱—FIR 幅频响应—输出频谱”的核心关系，新增无水印、真实坐标驱动的三联教材图《FIR 幅频响应决定频率成分的去留》：输入含低／高频成分、低通响应带 \(-\omega_c,\omega_c\) 截止标记、输出仅保留通带部分。零极点 SVG 中的两个伪公式标签也已替换为说明性“镜像零点”，精确零点公式只保留在紧邻的 MathJax 正文中；新图的数学标签亦由 MathJax 的完整 LaTeX 渲染。组件与全书装配 3 passed；137 页候选 PDF 导出 math=3902、unrendered=[]、pageErrors=[]，160 dpi 实查第 78、79 页，未见水印、黑图、裁切、重叠或公式源码。

> 2026-08-21 循环索引输出级修订：将第三章 DFS 与 DFT 正文中残留的“按模 \(N\)”及“按模 8”文字说明统一改为“按 \(N\) 周期”与“按 8 周期下标”；所有循环索引仍采用完整圆括号外的周期下标，如 \(x\left((n-m)\right)_N\)、\(x^*\left((N-k)\right)_N\)。先由回归发现遗漏文字，再以单项修复关闭；DFT 组件与全书装配 4 passed。137 页候选 PDF 导出 math=3890、unrendered=[]、pageErrors=[]，150 dpi 实查循环移位、循环共轭对称与循环卷积页，公式和文字均无 mod N 记法、公式源码、裁切或重叠。

> 2026-08-21 第六章真题训练第十一项：2025 年第七题第 5 小问“脉冲响应不变法与双线性变换的相加、级联性质”已整体纳入。详解证明两者保持相加；仅双线性变换保持相乘/级联，脉冲响应不变法一般不保持。组件与总装配 13 passed；137 页候选 PDF 导出 math=3890、unrendered=[]、pageErrors=[]，150 dpi 实查题面和两页详解无水印、公式源码、重叠、裁切或错误分页。

> 2026-08-21 第六章真题训练第十项：2007 年第十题第 3 小问“两种 IIR 设计法的频率映射区别”已纳入。详解以完整公式区分线性 \(\omega=\Omega T\) 与非线性 \(\omega=2\tan^{-1}(\Omega T/2)\) 映射，并说明混叠、频率扭曲和预畸变。组件与总装配 12 passed；135 页候选 PDF 导出 math=3856、unrendered=[]、pageErrors=[]，160 dpi 实查题面与详解无水印、公式源码、重叠、裁切或错误分页。

> 2026-08-21 第六章真题训练第九项：2022 年第六题“二阶巴特沃斯低通的双线性变换设计”已整体纳入。详解先预畸变得到 \(\Omega_c=1\)，再缩放模拟原型并完整代入双线性变换，得到以 \(z^{-1}\) 表示的二阶 IIR 系统函数。组件与总装配 11 passed；133 页候选 PDF 导出 math=3838、unrendered=[]、pageErrors=[]，160 dpi 实查题面与书末详解无水印、公式源码、重叠、裁切或错误分页。

> 2026-08-21 第六章真题训练第八项：2017 年第九题“低通数字滤波器的双线性变换预畸变与阶数”已按原题纳入。详解给出 \(\Omega_p=2\tan(0.1\pi)\)、\(\Omega_s=2\tan(0.15\pi)\) 和按 \(2\,\mathrm{dB}\)/\(15\,\mathrm{dB}\) 指标写出的巴特沃斯最小阶数表达式；不代入数值计算。组件与总装配 10 passed；132 页候选 PDF 导出 math=3814、unrendered=[]、pageErrors=[]，160 dpi 实查训练题与两页详解无公式源码、重叠、裁切或错误分页。

> 2026-08-21 第六章真题训练第七项：2015 年第二题“四类理想频率滤波器的幅频响应”已按原题纳入。书末详解使用真实坐标轴与矢量路径重绘理想低通、高通、带通、带阻；带通和带阻各以独立刻度标注 \(\omega_1\)、\(\omega_2\)，不再合并为居中标签。组件、第三章循环记号与总装配 11 passed；130 页候选 PDF 导出 math=3778、unrendered=[]、pageErrors=[]，160 dpi 实查题面与详解无水印、公式源码、重叠或裁切。

> 2026-08-21 提交与推送：第六章第七项本地提交为 `64bc702`（`Add chapter six ideal response training`）。常规 `git push origin main` 仍被本机 Git 凭据管理器阻止：`schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS`；未读取或尝试绕过任何凭据。

> 2026-08-21 第六章真题训练第六项：2006 年第十一题“最小相位模拟/数字滤波器映射”已整体纳入。详解明确脉冲响应不变法不保证零点映射，双线性变换保留有限零极点的左半平面到单位圆内映射，并保留严格真分式可能形成 \(z=-1\) 边界零点的条件。组件与总装配 8 passed；129 页候选 PDF 导出 math=3738、unrendered=[]、pageErrors=[]，160 dpi 实查无公式源码、重叠或裁切。

> 2026-08-21 第六章真题训练第五项：2005 年第十二题“由数字滤波器反推模拟原型”已整体纳入，同题的脉冲响应不变法和双线性变换法两问共同保留。详解给出 \(H_{\mathrm{ii}}(s)=\frac{1}{s+\frac{\ln2}{2}}-\frac{\frac12}{s+\ln2}\) 及 \(H_{\mathrm{bl}}(s)=\frac{8(s+1)^2}{(3s+1)(5s+3)}\)。组件与总装配 7 passed；127 页候选 PDF 导出 math=3704、unrendered=[]、pageErrors=[]，160 dpi 实查题面与答案两页无公式源码、重叠、裁切或黑图。

> 2026-08-21 第六章真题训练第四项：2005 年第八题“连续系统的冲激响应、频率特性与抽样离散化”已整体纳入，五问不拆分。详解给出部分分式、\(h(t)\)、\(H(j\omega)\)、真实表达式驱动的对数横轴幅频图、连续域抽样关系和 \(T_s=0.2\) 时的 \(H(z)\)。首轮 PDF 发现题页页首裁切，已以全书 `.exam-page` 不可拆分规则修复；组件与总装配 6 passed，125 页候选 PDF 导出 math=3674、unrendered=[]、pageErrors=[]，160 dpi 实查题面与答案页无公式源码、重叠、裁切或黑图。

> 2026-08-21 第六章真题训练第三项：2023 年第九题“模拟滤波器的脉冲响应不变法与双线性变换”三问整体纳入，不拆分。详解给出两一阶节并联型和共享两级延时的直接 II 型；两图均为标准端口、分支、加法器与箭头的矢量结构图。组件与总装配 5 passed；123 页候选 PDF 导出 math=3596、unrendered=[]、pageErrors=[]，150 dpi 实查末尾三页无公式源码、图形裁切、箭头悬空或文字重叠。

> 2026-08-21 第六章真题训练第二项：2021 年第八题“采用双线性变换法设计一个贝特沃斯低通滤波器”已按原卷完整纳入，原卷“贝特沃斯”用词不改。书末详解按预畸变、巴特沃斯阶数、截止频率约束和双线性代换四步给出；训练和详解均为完整 MathJax 公式。组件与总装配 4 passed；120 页候选 PDF 导出 math=3518、unrendered=[]、pageErrors=[]，160 dpi 实查训练页和书末详解无公式源码、重叠或裁切。

> 2026-08-21 推送状态：第六章第二项本地提交为 860d818，但向 origin/main 推送时本机 Git 凭据管理器返回 SEC_E_NO_CREDENTIALS；未改远端、未尝试凭据绕过。待用户正常登录 GitHub 后，执行常规 git push origin main。

> 2026-08-21 第六章真题训练第一项：2007 年第八题“双线性变换保持稳定性证明”已按原题独立纳入。题干的 \(s=\frac{2}{T}\frac{z-1}{z+1}\) 完整 MathJax 渲染；详解通过 \(\operatorname{Re}\{s\}=\frac{2}{T}\frac{\left|z\right|^2-1}{\left|z+1\right|^2}\) 说明左半平面与单位圆内的映射，原试卷水印未带入。组件与总装配 3 passed；118 页候选 PDF 导出 math=3464、unrendered=[]、pageErrors=[]，160 dpi 实查第 106 与 118 页无公式源码、重叠或裁切。

> 2026-08-21 第五章真题训练完成：2020 年“2.IIR 滤波器的级联型和并联型结构特点；”已按原题独立放入训练区，详解集中书末。详解以因式分解乘积形式说明级联串接，以部分分式求和形式说明并联分路汇总，并交代有限字长与动态范围的选择因素。组件与总装配 3 passed；116 页候选 PDF 导出 `math=3438`、`unrendered=[]`、`pageErrors=[]`，160 dpi 实查第 105 页训练题及第 116 页详解无水印、公式源码、重叠或裁切。

> 2026-08-21 第四章训练已纳入全书候选稿：2017 年 8 点按时间抽样基-2 FFT 流图、2022 年一次半长 FFT 和 2004 年重叠相加法三题已位于八章正文之后，详解集中在书末。首次整书预览发现流图的组件缩放样式未带入总装配，右侧被裁切；已添加回归并将等比缩放、边界和图题样式提升为全书规则。组件与总装配共 5 passed；新候选 PDF 为 114 页，导出 `math=3434`、`unrendered=[]`、`pageErrors=[]`，160 dpi 实查书末第 112--113 页无图形裁切、公式源码或重叠。

> 2026-08-21 第四章真题恢复第一项：2017 年第六题第 5 小问“5.画出 8 点按时间抽样的基-2FFT 算法的流程运动图。”已按原卷逐字纳入独立训练页，详解集中放在书末组件。采用完整的无水印 8 点 DIT 流图，输入码位倒序、三级蝶形、旋转因子和自然顺序输出均完整。先失败后通过的组件回归 1 passed；实际 A4 导出 math=18、unrendered=[]、pageErrors=[]，160 dpi 页面复核无裁切、重叠或公式源码。

> 2026-08-21 第四章真题恢复第二项：2022 年第五题第 3 小问“现有一长度为 N 的序列 \(x[n]\)，试用一次 \(N/2\) 点的 FFT 计算其 N 点 DFT”已按原卷独立纳入；它不依赖前两项连续时间题。书末详解使用实序列复数打包、共轭对称拆分和 DIT 蝶形合成；循环下标采用 \(C^*\left((L-k)\right)_L\)。组件回归 2 passed；A4 导出 math=54、unrendered=[]、pageErrors=[]，训练与详解页均无裁切、重叠或公式源码。

> 2026-08-21 第四章真题恢复第三项：2004 年第十一题（300 点输入、60 点滤波器、128 点 FFT/IFFT 的重叠相加法）已按原题独立纳入。详解逐步得到每段 69 个新样本、5 个输入分段，并明确滤波器 FFT 只预计算一次，故总计 6 次 FFT、5 次 IFFT。组件回归 3 passed；A4 导出 math=72、unrendered=[]、pageErrors=[]，视觉检查无裁切、重叠或公式源码。

> 2026-08-21 循环索引外置下标复核：读者可见的循环索引全部保持为完整括号外的周期下标，例如 \(x\left((n+m)\right)_N\) 和 \(x^*\left((N-n)\right)_N\)，不再使用 `mod N`。已扫描有效源文件；仅相位主值的 `\\pmod{2\\pi}` 因非循环索引而保留。第三章 DFT 回归 `2 passed`。

> 2026-08-21 全书训练/详解装配基础完成：新增全书装配入口，八章正文后接入当前已验证的第 1--3 章训练，全部已接入详解集中在书末。旧的固定“详解见 P.59/P.18”仅在总装配层归一为 `详解见 P.____`，等待所有训练、附录完成后统一回填真实页码。候选 PDF 共 108 页，11 项定向测试通过；导出报告 `math=3358`、`unrendered=[]`、`pageErrors=[]`，A4 抽查训练起始、训练末页和详解起始无公式源码、裁切或重叠。

> 2026-08-21 真题/附录恢复盘点：`exam_training_manifest.json` 已按八章归类 156 个题目条目（第 1--8 章依次 41、38、36、12、1、13、11、4 个），并已保存题目归属、跨章处理方式和图形要求。现有可复用训练/详解主要覆盖第 1--3 章；第 4--8 章尚未进入全书训练装配。下一步在八章正文后新增独立训练—书末详解装配阶段，最终分页后再回填“详解见 P.__”的准确页码。

> 2026-08-21 循环索引记号确认：读者可见的循环下标统一写为完整括号外的周期下标，例如 \(x\left((n+m)\right)_N\) 与 \(x^*\left((N-n)\right)_N\)，不使用 `mod N`。新增回归同时禁止 `\bmod`、`\operatorname{mod}`、`\mathrm{mod}` 与 `\pmod` 进入第三章 DFT 组件；相位主值等非循环索引的模运算不在本次替换范围内。

> 2026-08-09 第二章补充真题第二十五批完成：2007 年计算题第 2 小题“常数序列的 DTFT”按原题干纳入。详解从 \(X(e^{j\omega})=k\sum_n e^{-j\omega n}\) 推至 \(2\pi k\sum_m\delta(\omega-2\pi m)\)，并明确其广义函数含义与主值区间表示。组件与总装配共 4 项测试通过；91 页候选 PDF 的第 90--91 页以 180 dpi 实查，题目页保留允许的作答空间，详解页无原始 TeX、裁切或重叠。

> 2026-08-09 第二章补充真题第二十四批完成：2003 年第八题“极点、ROC 与稳定性”按原题干纳入。详解先用 \(\left|z\right|=1\subset\operatorname{ROC}\) 给出仅稳定时 \(\left|a\right|\ne1,\left|b\right|\ne1\)，再结合因果 ROC 在最外极点外侧得到 \(\left|a\right|<1,\left|b\right|<1\)。组件与总装配共 4 项测试通过；89 页候选 PDF 的第 88--89 页以 180 dpi 实查，题目页保留允许的作答空间，详解页无原始 TeX、裁切或重叠。
> 2026-08-09 第二章补充真题第二十批完成：2014 年“由 DTFT 幅频、相频图判定序列性质”按原题干纳入。幅频图与相频图分别采用真实坐标 SVG，保留完整坐标轴、箭头、关键刻度及标签；详解逐项判定“非周期、实、非偶、有限能量”，全部公式由 MathJax 整体渲染。首轮候选 PDF 发现合成器会剥离组件局部样式，造成图组在页间拆分；已以失败回归锁定并将成组不分页样式移入全书级 CSS。组件与总装配共 5 项测试通过；87 页候选 PDF 的第 86--87 页以 180 dpi 实查，两图同页，详解同页，无公式源码、裁切或重叠。

> 2026-08-09 第二章补充真题第十九批完成：2013 年计算题第 5 小题按原题干纳入。双边序列的两个分量分别给出 \(\left|z\right|>\frac13\) 与 \(\left|z\right|<\frac12\)，最终 ROC 为 \(\frac13<\left|z\right|<\frac12\)。组件与总装配 4 项测试通过；85 页候选 PDF 的第 84--85 页视觉验收通过。

> 2026-08-09 第二章补充真题第十八批完成：2015 年填空题第 2 小题按原题干纳入。该题由 \(x(n)\) 求 \(\operatorname{Re}\{X(e^{j\omega})\}\) 对应序列，与 2020 年“实部序列的 DTFT”题方向不同，不作重复处理。详解得 \(x_{\mathrm{cs}}[n]=\frac12(x[n]+x^*[-n])\)。组件与总装配共 4 项测试通过；候选 PDF 第 82--83 页以 160 dpi 实查，无公式源码、重叠或裁切。

> 2026-08-09 第二章补充真题第十七批完成：2005 年“一、计算”第 4 小题按原题干纳入，保留 \(F(z)=z^2/(z^2-2z-3)\) 与 \(1<\left|z\right|<3\)。详解按 \(z/(z-a)\) 形式分解，明确极点 \(3\) 选内侧 ROC、极点 \(-1\) 选外侧 ROC，得到双边序列 \(f[n]=\frac12(-1)^nu[n]-\frac12 3^nu[-n-1]\)。组件与总装配共 4 项测试通过；81 页候选 PDF 的第 80--81 页以 180 dpi 实查，无公式源码、重叠或裁切；第 80 页为单道大题训练页，按规则保留作答空间。

> 2026-08-09 第二章补充真题第十六批完成：2020 年一、填空题第 2、3 小题按原题干同页纳入，避免将两道互不依赖的小题拆为两张练习页。详解用完整 MathJax 公式推导 \(\operatorname{Re}\{x[n]\}\) 的 DTFT 为 \(X(e^{j\omega})\) 的共轭对称分量，并给出离散 LTI 系统稳定所需的 ROC 包含 \(\left|z\right|=1\)。首轮 180 dpi PDF 视觉复核发现一句行内公式仍漏渲染为 LaTeX 源码；已先添加失败 DOM 回归，修正后组件与总装配共 4 项测试通过。79 页候选 PDF 的第 78--79 页以 180 dpi 实查，未见公式源码、重叠或裁切；第 78 页是合并后的填空训练页，保留作答空间。

> 2026-08-09 第二章补充真题第十五批完成：2021 年第六题采用“同题重制”而非重复追加。原题的 AM 调制器、输入三角形周期谱、相干解调器和理想低通均重画为无水印教材级矢量图；全部数学标签由完整 LaTeX 交给 MathJax。详解新增 \(Y(e^{j\omega})\) 的双搬移谱图，给出 \(G=2/\cos\theta_c\)，并补全考虑 \(2\pi\) 频率折返的恢复条件。首次视觉复核发现零频中心被错误拆为两个三角谱，已先写失败回归并修复为单一中心峰。组件、图形、MathJax DOM 与总装配共 8 项测试通过；72 页候选 PDF 以 160--180 dpi 实查第 16、18、19 页，未见水印、原始公式源码、重叠或裁切。

> 2026-08-09 第二章补充真题第十四批已完成：2015 年第七题整体纳入。原卷水印零极点图已重绘为标准复平面图，保留原点零点、z=2 极点、单位圆与坐标轴；详解给出 H(z)、ROC、因果不稳定性、h[n] 和差分方程。首次 DOM 验收发现反斜杠在生成时被吞掉，已将训练与详解统一为单一反斜杠入口；组件与总装配 3 passed，180 dpi 实查第 70--71 页无水印、公式源码、重叠或裁切。

> 2026-08-09 第二章补充真题第十三批已完成：2025 年第八题整体纳入，保留因果稳定 LSI 差分方程、幅相表达式、频率平移与滤波特性三问。详解给出 (h_1[n])、(H_1(e^{j\omega})=j e^{-j\omega/2}\cos\omega\sin(3\omega/2))、自动伸缩 cases 相位、(h_2[n]=(-1)^nh_1[n]) 及高低频选择关系。首轮 PDF 视觉复核发现训练页反斜杠公式漏渲染和相位公式框横向滚动条；已先添加 DOM 回归并修复。组件与总装配回归 4 passed；69 页候选稿以 180 dpi 实查第 67--69 页，无公式源码、横向滚动条、重叠或裁切；第 69 页为当前第二章末页，按规则允许留白。

> 2026-08-09 第二章补充真题第十二批已完成：2025 年第四题按原题题干整体纳入，详解给出七点矩形序列、DTFT 几何和、线性相位因子、零点与幅度谱。幅度谱为真实数据驱动 SVG；首次视觉复核发现横轴刻度与手写纵轴原点产生双重 0，已先以失败回归锁定，再移除重复输出。组件与总装配回归 5 passed；66 页候选稿以 150--180 dpi 实查第 64--66 页，题页保留一题一页作答空间，详解与图页无公式源码、重叠或裁切；第 66 页为当前第二章末页，按规则允许保留空白。

> 2026-08-09 第二章补充真题第十一批已完成：2024 年第九题四小问已整体纳入。详解推导 \(\left|a\right|<1\)、延时消零后的 \(h[n]=a^n\left(u[n]-u[n-N]\right)\)、有限几何和形式与 FIR 判定，并说明(2)对任意有限 \(a\) 均稳定。组件与总装配回归 5 passed；63 页候选稿以 180 dpi 实查第 61--63 页，无公式源码、重叠或裁切。训练页保持一题一页；第 63 页为当前第二章末页，按规则允许保留空白。

> 2026-08-09 第二章补充真题第十批已完成：2022 年第八题两小问已按原卷整体纳入，含无水印标准系统结构图、\(H(z)=1-z^{-N}\)、\(N=8\) 零极点图和 \(2\left|\sin(4\omega)\right|\) 幅频曲线。首次视觉验收发现零极点图 `Re/Im` 轴标签反斜杠遗漏，已由失败测试锁定并修复；组件与总装配回归 5 passed，60 页候选稿以 180 dpi 实查第 58--60 页，无水印、公式源码、重叠或裁切；训练页保持一题一页作答空间。

> 2026-08-09 第二章补充真题第九批已完成：2020 年第四题已按原卷题干整体纳入。详解以正负复指数分量核对 \(H\!\left(e^{\pm j\frac{\pi}{2}}\right)=4\)，得到 \(y[n]=4\cos\left(\frac{\pi n}{2}\right)\)，明确无附加相位偏移。组件与总装配回归 5 passed；57 页候选稿以 180 dpi 实查第 56--57 页，训练页保持一题一页作答空间，详解页无公式源码、重叠、裁切或普通正文大留白。

> 2026-08-09 第二章补充真题第八批已完成：2023 年第八题四小问已整体纳入，包含二阶差分 FIR 系统的 \(H(z)\)、\(h[n]\)、因果稳定性、幅相频响应、线性相位与指定输入输出详解。幅频图和相频图为真实数据驱动 SVG；组件与总装配回归 5 passed，52 页候选稿扩至 55 页后以 160 dpi 实查第 53--55 页，无图文重叠、裁切、公式源码或普通正文大留白。

> 2026-08-09 第二章补充真题第七批已完成：2024 年 DSP 第 2 小题已逐字纳入，详解包含 \(H(z)\) 多项式化、\(z=\pm1\) 零点、\(z=\pm j0.8\) 极点及精确幅频表达式；零极点图与幅频图均采用真实坐标 SVG。组件与总装配回归 5 passed；重建 52 页候选 PDF 后以 180 dpi 实查第 50--52 页，训练页保留作答空间，详解页无公式源码、重叠、裁切或普通正文大留白。

> 2026-08-08 第二章补充真题第六批已完成：2014 年第 5 题已纳入原题、单延时反馈结构图、零极点/ROC 图与完整详细解。求和器采用标准圆形加法器和 MathJax 的 \(+\)；零极点图尺寸改为内联，避免总装配丢失局部 CSS 后把整图推入新页。2014 详解的反变换步骤已自然续排进第 48 页，候选 PDF 第 47--49 页经 180 dpi 实查无图形裁切、标签重叠或普通正文大留白；相关回归 5 passed。

> 2026-08-08 第二章三幅用户指出的图形已先行修复并在实际 PDF 中复核：s 平面到 z 平面的映射图改为内联样式的真实坐标 SVG，消除了总装配丢失局部 CSS 后出现的黑色实心圆和散落箭头；2015 年零极点图已分离单位圆、虚轴、原点和刻度标签；2021 年调制/频谱/相干解调图已扩大画布并重排，完整显示。三项新回归先失败后通过，连同训练组件与总装配共 6 passed；180 dpi 实查第 2、15、16 页无重叠、裁切或公式源代码。
> 2026-08-01 第 41 页反馈结构图再次按视觉问题收束：两条反馈箭头改放到各自增益块的输出侧，求和器下方仅保留两条独立、无箭头尖端拥挤的接入线；题目的数学关系、两条反馈分支和原题干均未改动。定向回归 5 passed，重建 49 页候选 PDF 后以 220 dpi 实查第 41 页，无重叠、裁切或端口混淆。

> 2026-08-01 第 41 页 2013 年反馈系统框图已修复：两条下方反馈支路分别经 `\frac{5}{2}` 和 `-1` 独立增益块，再进入求和器的左下端口与下端口；不再以紧邻的折线箭头和在线系数混排。回归测试先失败后通过（3 passed），并已重建候选 PDF 以 200 dpi 人工复核，无重叠、裁切或端口混淆。

> 2026-08-01 第二章五个已迁移正文模块已合成为一个连续 A4 候选稿。首轮实测发现浏览器固定定位页眉压正文、CSS `counter(page)` 全部输出为 0；已改为先由 Chromium 完成正文分页、再用 ReportLab/PyPDF 在预留页边距逐页叠加统一页眉和真实页码。第二轮 150 dpi 实际预览抽查第 1、7、14 页：页眉、页码和正文均无重叠，页码为 1、7、14；`pypdf` 也确认 PDF 共 14 页且三页都含对应页码。候选稿尚未纳入第二章强化训练、补充真题和全书末尾详解，末页仍留有大面积空白，因此不得写入 `output/` 或视作章节交付。

> 2026-08-01 2019 年“图形卷积”详解公式漏洞已收束：问题根因是修复前个别行内 LaTeX 分隔符在生成字符串中被错误转义，导致 MathJax 未接管相应片段；最新第一章第 57 页已以实际 A4 渲染复核，题干中的 f_1、f_2、卷积、支撑区和长度检验均为正式数学排版。新增浏览器最终 DOM 回归门禁，要求 MathJax 输出节点存在且目标段落不得残留 `\\(`、`\\)`、`\\[`、`\\]` 源码；测试 2 passed。以后任何此类残留均判定整页不合格，必须修复并重导出。

> 2026-08-01 第二章迁移启动：旧第二章 8 个组件仍含公式位图，现行合成基线为 26 页且“少于 20 页”的续排测试失败，不能交付。2.1 z 变换基础已先迁移为 MathJax 连续正文与真实 SVG 映射图；150 dpi 三页预览确认公式、自动 cases 大括号、坐标轴和单位圆完整，未见源码、裁切或重叠。后续将把其余第二章组件按同一基准替换后再进行整章合成。

> 2026-08-01 第二章 z 反变换与性质组件已迁移为完整 MathJax 连续正文：围线积分、部分分式、幂级数、线性/移位/卷积/微分性质均不再以公式位图或手工数学元素实现。测试先失败后通过（1 passed）；150 dpi 三页预览中前两页公式无源码、裁切或重叠。组件末页将在最终整章 HTML 中直接续接 DTFT 内容，禁止将当前独立组件的页末留白带入成品。

> 2026-08-01 第二章 DTFT 与共轭对称内容已迁移：正反变换、存在条件、单位圆、2π 周期性、共轭对称分解、实序列频谱对称性及“由实部恢复实因果序列”例题均采用一个连续 MathJax 文档。150 dpi 实际预览曾暴露临时行内标记与 TeX 花括号冲突，已改为无冲突标记并以测试覆盖；最终三页预览无原始 LaTeX、裁切或重叠。后续仅在整章合成器中决定分页，禁止把独立预览页的末端空白带入成品。

> 2026-08-01 第二章系统函数、频率响应与几何法内容已迁移：系统函数/ROC/因果稳定判据、差分方程例题、频率响应与三点平均、一阶极点和梳状零点均进入一个连续 MathJax 文档。几何图采用真实 SVG 坐标轴、箭头和单位圆；首次预览发现零点/极点同坐标后已重画为分离位置并再次核验。150 dpi 预览无公式源码、裁切、标签重叠或图形错位。独立组件的最后空白页仅为后续整章续接空间，不能进入最终成品。

> 2026-08-01 第二章特殊滤波器基础已迁移：一阶低通/高通、数字陷波、全通与最小相位的设计规则、系统函数、频率换算、零极点关系和检查顺序均改为连续 MathJax 正文。用户要求的 MATLAB 绘图/代码内容未纳入；150 dpi 三页预览确认公式正式渲染且无裁切、重叠。最终整章将继续接入训练和详解，不保留该独立预览的末页留白。

> 2026-08-01 第一章真题与详解管线收束：分章强化训练、8 道补充真题、3 道重点详解和补充详解均已迁移到 MathJax + 真实 SVG 图形管线，替换旧的 ReportLab 公式位图与手工坐标图。实际 150 dpi PDF 预览发现并修复 2020 年题的数学源码漏渲染；修复后题干、自动伸缩 cases、传递函数和重建公式均清晰且无重叠。整章为 60 页，普通正文连续续排；仅真题作答页与章节末页保留空白。相关回归 9 passed，下一步将第一章成品写入 output/ 后进行任务范围提交、推送，再按同一整章批次推进第二章。

> 2026-07-29 采样工程与时域恢复重制：抗混叠处理链、频谱副本、带通信号频带位置、重构通带和 sinc 插值波形均已替换为数据驱动 SVG；全部公式以完整 LaTeX 交由 MathJax 渲染，重构滤波器使用标准 cases 自动伸缩大括号。合成讲义第 33--38 页已做实际 PDF 预览：无图文重叠、裁切或公式位图；恢复导语已续入上一页，避免空白整页。相关组件及合成器回归 9 passed，git diff --check 通过。下一步重制模拟—数字处理链和章节收束组件，持续复审第一章剩余旧图形。

> 2026-07-29 第一章“离散时间信号的表示方法”迁移里程碑：新增独立的 MathJax + 数据驱动 SVG 自然续排组件。所有展示公式均以完整 LaTeX 交给 MathJax；单位抽样序列使用标准 cases 环境，不再使用 ReportLab 图片公式、手工大括号或固定像素 stem 图。A4 PDF 以 130 dpi 逐页复核：第 1 页已将单位抽样序列标题自然续入，第 2 页继续接入移位加权和，消除原组件的整页空白；公式、坐标轴、箭头与 stem 图未见裁切或重叠。相关回归共 7 项通过（禁用 pytest 缓存后复跑仍待执行）；窄屏 CSS 已加入流式收缩规则，最终移动端浏览器验收仍需在后续整章 HTML 合成时复核。

> 2026-07-29 表示方法唯一内容补全：新组件补入原课件的三组数列 x_1、x_2、x_3，保留其与零点位置说明的关联；测试先失败后通过。重新打印的 A4 第 1 页已人工复核，三组数列公式、函数表示公式、全文 stem 图和下一小节标题均清楚且未裁切。

> 2026-07-29 图形渲染规范升级：用户新增“真实坐标绘制 + 标准教材坐标轴 + 桌面/移动端/打印实际预览”的全局硬约束。规范已写入 docs/requirements；下一步先逐图审计第一章，所有 HTML/CSS/字符模拟、固定像素拼接、静态截图、重叠、裁切或不符合教材轴规范的图均须重做。第一章在图形审计与公式整体 MathJax 迁移完成前，不再视为可交付章节。
> 2026-07-29 第一章审计结论：20 个组件构建器中，已定位到公式位图嵌入、手工大括号、固定像素 stem/频谱/结构图及课件截图路径；按项目执行核心命令全部不通过。审计清单已写入 docs/audits/2026-07-29-第一章图形与公式渲染审计.md。下一步：先将表示方法页迁移为统一 MathJax 公式与真实坐标图的首个基准页。
> 2026-07-29 图形基础设施首个验收：新增真实数据驱动的 SVG stem 图组件。测试先行覆盖教材轴/箭头、首杆避让、标题与纵轴标签净空和窄屏缩放；初始桌面预览发现标题标签重叠、初始窄屏预览发现 720px 裁切，均已分别以失败测试复现并修复。最终桌面与 390px 窄屏实际预览通过。下一步：为该组件接入统一 MathJax 数学标签，并以它替换第一章表示方法页的旧 stem 图。
> 2026-07-29 MathJax 公式片段验收：统一公式入口已扩展为可输出紧凑 PDF 片段；实际渲染 \delta(n) 的 cases 公式并以 180 dpi 预览确认大括号自动覆盖两行、取值/条件在括号内对齐且无裁切。旧组件中的手工大括号与公式位图不再作为最终实现提交。
> 2026-07-29 表示方法基准页：已创建首个 HTML 基准页，将完整 LaTeX cases 交给 MathJax、将离散序列交给数据驱动 SVG stem 图。测试先行发现并修复 LaTeX 在 Python f-string 中被错误转义及移动端缺失 viewport 导致正文裁切的问题；桌面与 390px 窄屏实际预览均已复核公式、坐标轴、首杆避让与标签无重叠。该页是后续第一章替换的基准，不是最终独立分页，后续将继续自然续排。
> 2026-07-29 第一章公式规范升级：用户要求全文数学结构必须由 KaTeX/MathJax 对完整 LaTeX 源码整体渲染，手工大括号、拆分定位和公式图片均不可接受。已建立并通过 Edge + MathJax 的 cases 烟雾测试：完整 LaTeX 由统一入口生成 HTML 后整体渲染，标准大括号自动覆盖两行内容。当前第一章旧 PDF 管线仍需迁移后才可重新验收；全局规范与统一入口待推送。
> 2026-07-29 执行纠正：已复核仓库中既有的“连续续排、非章末不留大空白”要求；第二章此前以小节固定三页的组件页进行预览，仍产生大留白，不符合该要求。后续改为先组装 2.1--2.5 的连续正文流后再分页，组件 PDF 不再作为页面成品或验收对象。下一步：实现第二章连续合成器并对合成 PDF 逐页检查密度、无重叠与无裁切。
> 2026-07-29 第二章 2.4：频率响应核心知识组件已完成，已排除 MATLAB 与音频实验。逐页核验无重叠或裁切；全量 full/tests 140 passed。下一步继续 2.5 几何法画频率响应。
> 2026-07-29 第二章 2.3：已完成系统函数、ROC 因果稳定判据和原差分方程例题组件。逐页核验无重叠或裁切；全量 full/tests 139 passed，git diff --check 通过。下一步继续 2.4 系统频率响应。
> 2026-07-29 第一章整章验收：正文 1--45 页按文本密度扫描与候选页渲染复核完成；低密度候选均为完整多图、频谱、公式或章节开篇页，未发现无意义大空白。训练 46--56 页保持一题一页，详解 57--60 页保持独立。准备将最终第一章 PDF 写入 `output/` 并执行最终回归、提交推送，然后启动第二章批量重制。
> 2026-07-29 输出规则：用户确认仓库根目录 `output/` 只放每个已完成且验收通过的章节成品 PDF，不纳入组件、截图或临时预览。第一章完成整章验收后作为首个成品写入；后续每完成一章同步加入。
> 2026-07-29 连续续排：在第 44 页“同一组样值未必对应唯一连续信号”之后续入下一块“两个给出相同样值的连续信号”标题；第一条公式经回归验证不能安全容纳，仍在下一页完整保留，避免越界或裁切。渲染复核无重叠；全量 `full/tests` 为 `133 passed`，`git diff --check` 通过。下一步：继续审校第一章余下正文低密度页；第二章仍暂停。
> 2026-07-29 连续续排：第 40 页将完整“移位内插波形”图前移，第 41 页继续严格插值与样值不变性后续入“模拟信号的数字处理链路”标题和导语，第 42 页以“（续）”接入完整处理链和后续内容。180 dpi 逐页复核确认图形上下边界、刻度、公式、页眉页脚均无重叠或裁切；全量 `full/tests` 为 `132 passed`，`git diff --check` 通过。下一步：继续审校第一章余下正文低密度页；第二章仍暂停。
> 2026-07-29 连续续排：第 35 页在奈奎斯特采样定理结论后续入“抗混叠滤波”的完整标题与导语；第 36 页以“抗混叠滤波（续）”承接处理链、带宽约束公式和频域作用说明。流程图、公式、页眉页脚均保留完整。180 dpi 视觉复核无重叠、裁切或不必要的大面积空白；全量 `full/tests` 为 `130 passed`，`git diff --check` 通过。下一步：继续审校第一章余下正文低密度页；第二章仍暂停。
> 2026-07-26 连续续排扩展：第一章“抗混叠滤波与带通信号采样”组件已按相同规则完成逐页续排。第一页续入“频域作用”的标题、导语和图题；第二页续入“带通信号的采样参数”及公式；第三页续入“无混叠采样”及公式。组件保留 4 页以承载完整频谱图与非整数情形，但前三页均不再因强制分页留下大片空白；180 dpi 预览确认图文、公式和图形无重叠。待总讲义重建与全量回归后提交。
> 2026-07-26 连续续排修正：已将第一章“时域采样定理”中原先独立成页的“采样后的频域周期延拓”标题、导语、展示公式、频域示意图及说明续入“理想时域采样”页，组件从 4 页压缩为 3 页。200 dpi 页面预览确认图形完整、公式和图文无重叠；第一章总讲义已同步重建。`full/tests` 84 passed。下一步：将相同的安全断点续排规则继续应用于其余正文和例题组件，最终全书合成后再回填真题详解页码。

> 2026-07-26 里程碑：真题续排规则已首次落地。补充真题中原本在 2020 年小题后强制换页的 2002 年频谱题题干已上提到同页，组件由 4 页压缩为 3 页；200 dpi 视觉复核确认题干、右侧索引与题目正文均无重叠。真题索引临时文本改为“详解见 P.待回填”，去除会视觉粘连的连续下划线；全书最终合并后必须替换为真实页码。用户进一步明确：即使下一项完整内容放不下，也应先续入不重复的标题、导语、公式或若干步骤，随后再在下一页接排；此规则已写入需求记录，后续将由总讲义续排器在安全内容断点处分块执行。全量 `full/tests` 84 项通过，`git diff --check` 通过。

> 2026-07-26 里程碑：全局版式规则更新并实际重建第一章。用户已明确取消全部“复习提示”模块；公共绘制入口现不再输出提示框、标题或提示内容，所有第一章组件及总讲义已重建。模拟—数字处理链末端的 `y_a(t)` 标签与箭头重叠已按可测试的净空布局参数修复；单测明确要求箭头终点与输出标签左边界至少相隔 8 pt。更新后的第一章总讲义为 55 页；300 dpi 抽查第 1、20、40、50 页确认提示框已消失、图形与标签未见重叠。全量 `full/tests` 84 项通过，`git diff --check` 通过。用户进一步重申：除章节末页和真题“一题一页”作答空白外，文字、例题、图解与详解均必须自然续排，不能因固定组件页界形成大片空白；当前总讲义仍以组件页为不可拆分块，后续必须将续排器改为可在安全内容断点处分块续排，不能将当前 55 页视为最终排版。

> 2026-07-26 里程碑：第一章补充真题新增 2003 年第六题“冲激采样后的低通重建”。详解给出采样脉冲串、截止频率为 ωs/2 且增益为 T 的理想低通重建核，以及在 t=mT 时非零整数核值为零、零点核值为一的严格证明。视觉复核确认公式与正文清晰无重叠；第一章总讲义已同步重建，83 项回归测试通过。

> 2026-07-26 里程碑：第一章补充真题新增 2002 年第八题“模拟信号采样后的离散频谱与混叠”。未复用原卷图形，已重绘连续谱、Ωs=8π 时的混叠离散谱和提高采样频率后的无失真离散谱；所有坐标、谱线和标签均经视觉复核无重叠。详解明确 5π 分量折叠至 −3π，避免混叠需 Ωs>10π。第一章总讲义已同步重建，83 项回归测试通过。

> 2026-07-26 里程碑：第一章补充真题新增 2003 年差分方程求单位样值响应题。训练组件新增题页；详解组件新增第三页，给出零状态 z 变换、部分分式展开、因果收敛域及 h[n] 的完整结果。160 dpi 渲染复核确认展示公式、页眉页脚与正文无重叠；第一章总讲义已同步重建，83 项回归测试通过。

> 2026-07-26 里程碑：第一章补充真题新增 2014 年离散系统时变性判定题。详解将相邻样点的交替项合并为 y[n]=2x[n]，明确给出“不是时变系统”的结论；已同步重建训练、详解和第一章总讲义，83 项回归测试通过。

> 2026-07-26 里程碑：第一章补充真题继续纳入 2015 年“采样后信号类型”与 2020 年“组合带限信号抽样频率”两题；训练页为白底无底框，详解页将后两题自然续到第二页，未缩小公式字号。视觉复核确认题干、行内公式与展示公式均清晰，2020 年结果公式不压页底；重建后的第一章总讲义已同步更新，83 项回归测试通过。

> 2026-07-26 里程碑：第一章新增 2002、2003 年两道“采样”补充真题及对应书末详解组件，题干按原题信息重排为白底教材页，详解补足从最高角频率、最小采样角频率到频率/周期换算的完整链条。两页组件均可编辑，展示公式使用统一的正式数学渲染；300 dpi 视觉复核确认公式、题目标题、页眉与页脚均无重叠。补充真题已接入 full/outputs/chapter_01_handout.pdf 章末，第一章自然续排成品现为 63 页；最终“详解：P××”页码仍按全书排版完成后统一回填。下一步继续录入第一章其余已审定真题，之后再制作全书书末详解。

> 2026-07-22 里程碑：第一章自然续排成品已接入 3 页重点真题训练，输出更新为 `full/outputs/chapter_01_handout.pdf` 共 62 页。训练承接章节小结后自然流入第 60 页，2006 与 2019 年题目在第 61、62 页获得完整图形和书写空间；未按原 PPT 页界机械换页。200 dpi 末页抽检确认图内延时、函数上下标、坐标和题干完整，无水印、无缺字、无重叠；最终书末详解的精确页码仍待全书分页完成后回填。

> 2026-07-22 里程碑：第一章“分章强化训练”及对应“真题整理详解”已完成为两个独立、可编辑的 A4 组件，各 3 页。训练按 2002、2006、2019 年三道重点题组织，纯白页面、无黄色底色、边框、横线或“练习”标签；题目右侧仅保留“详解：真题整理详解”的最终索引占位。2006 年离散系统和 2019 年卷积图均已重绘，未带入水印。延时块与图内函数名均改用正式数学渲染，避免 Unicode 上下标缺字；300 dpi 复核确认无缺字、无公式代码、无图文重叠。全书最终分页后，仍需将这三道题与补充题接入章节末尾，并在书末详解中回填精确页码。
> 2026-07-22 里程碑：第一章正文自然续排主干已生成 `full/outputs/chapter_01_handout.pdf`。生成器按内容模型顺序读取 17 个可编辑组件，裁去各组件自身的页眉页脚空白后在统一版心内连续排布，并重建统一页眉、页码；从 62 个独立组件页压缩为 60 页 A4。顶部标题安全区已加入回归测试，避免裁边截断标题。抽检开篇、第 20 页、第 40 页和章末第 60 页：标题、公式、图形与页眉页码清晰；章末保留大面积书写空白。全文读取层禁用来源词扫描通过，全部 80 项测试通过。该输出尚未接入第一章真题训练与书末详解，故不能视为完整第一章终稿。
> 2026-07-22 里程碑：第一章导览（第 1 页）已补成独立可编辑 A4 开篇页，按原顺序保留四节目录：离散时间信号——序列、离散时间系统、常系数线性差分方程、连续时间信号的抽样。页内使用正式蓝金教材风格、统一页眉页码，不含任何来源性字样；300 dpi 渲染复核无裁切或重叠。内容模型同时补登记已验收的第 2–8 页“离散时间信号的由来”组件，第一章全部非范围外单元均已有对应可编辑组件输入，待进入整章自然续排。
> 2026-07-22 里程碑：第一章“采样认知案例与本章收束”（第 177–185 页）组件已完成。第 180、183、184 页的车轮视觉案例合并为完整的混叠解释；第 181–182 页以两组连续信号取得同一离散样值的等式说明“样值不唯一”现象。已保留 Nyquist 与 Shannon 的学术背景、采样频率与奈奎斯特频率关系、车轮倒转／静止的混叠解释及本章收束。四页经 300 dpi 复核：公式均为正式数学渲染，轮辐与图内标签清晰且无重叠，无水印、模板或读者可见来源性字样。该组件待与第一章正文自然续排。
> 2026-07-22 里程碑：第一章“模拟信号的数字处理方法”（第 168–176 页）组件已完成。标题页并入处理链总览；第 171 页的处理器链信息纳入可编辑总图；第 173–175 页的理想/实际数模恢复与采样间隔影响连续组织。已保留前置预滤波、模数转换、量化编码、滤波降噪等数字处理任务、零阶保持、平滑滤波与收束语。四页经 300 dpi 复核：所有公式正式渲染且方括号、上下标完整，处理链箭头和图内标签无重叠，读者可见内容无来源性字样。该组件待与第一章正文自然续排。
> 2026-07-22 规则补充：用户再次确认成品的所有部分都不得有“源文件”或类似身份字样。读者可见的标题、图题、图注、旁注、训练、附录、页眉页脚、目录、索引和答案页均不得出现“源课件”“源文件”“原始材料”“根据原课件”，也不得使用“参考/依据/改编自某课件（文件、材料）”等表达；来源追溯仅保留在内部审计，绝不进入 PDF。

> 2026-07-22 里程碑：第一章“时域采样信号的恢复”（第 160–167 页）组件已完成。标题页和重复标题页并入正文；第 164–165 页作为内插恢复递进图合并为完整解释状态。已保留无混叠时的理想低通重构、重构滤波器频响与冲激响应、时域卷积、内插求和式、内插函数及其在采样点的严格取值条件。四页自绘频谱/内插图经 300 dpi 复核：所有图内变量走正式数学渲染，标签不与坐标轴或曲线重叠，无缺字、公式代码或来源性文字。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“采样定理的工程应用与带通信号采样”（第 150–159 页）组件已完成。标题页和重复导览页并入正文；第 154–155 页音频实验案例按范围约束排除。已保留抗混叠滤波的模数转换链路、采样前带宽约束、频谱副本分离、带通频谱参数、整数倍带通采样与非整数情形的带宽扩展。四页自绘流程图/频谱图均经 300 dpi 复核：公式正式渲染、下标完整、标签不与坐标轴或箭头重叠，且无来源性文字。该组件待与第一章正文自然续排。

> 2026-07-22 版面命名规则：面向读者的成品只称《数字信号处理讲义》。所有标题、图题、图注、旁注、练习、附录、页眉页脚、目录和索引均不得出现“源课件”“源文件”“原始材料”“根据原课件”或其他来源性措辞；内部审计追溯不参与 PDF 排版。已将第一章周期性组件中的“源课件参数的周期结论”改为“参数的周期结论”。

> 2026-07-22 范围变更：用户明确本讲义只涉及 DSP 基础知识，所有 MATLAB 函数、绘图、程序代码、音频读写和实验内容一律移除。第一章原课件第 31–44 页保留为已审计的范围外来源单元，不转写、不生成成品页面；已生成的 MATLAB 组件及原第 35 页 impseq 直接转写同步撤除。

> 2026-07-22 里程碑：第一章“线性卷积性质与相关”（源页 98–114）组件已完成。第 98、114 页并入正文；第 102–104 页合并为有限支持区间的完整动画推导；第 107、108 页为音频读写、程序代码和实验输出，按范围约束排除。已保留交换、结合、分配律，有限支持区间，双脉冲延时叠加系统，以及实序列互相关和自相关的定义与延时峰值解释；300 dpi 视觉复核确认公式正式渲染、没有代码、缺字或重叠。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“因果性与稳定性”（源页 115–123）组件已完成。第 115、123 页并入正文；已保留一般系统的因果性与 BIBO 稳定性定义、LSI 的单位脉冲响应判据、全部四组源课件判断例及绝对可和的卷积上界推导。300 dpi 视觉复核确认公式正式渲染、行内复杂求和式已提升为展示公式、无缺字或重叠。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“常系数线性差分方程”（源页 124–133）组件已完成。第 124、133 页并入正文；第 132 页为 MATLAB filter、代码与绘图输出，按范围约束排除。已保留方程通式、常系数/阶数/线性定义、四种求解法、同一方程对应的因果与非因果迭代解，以及一阶反馈结构图；300 dpi 视觉复核确认图内标签和反馈线不裁切、不重叠。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“时域采样定理”（源页 134–149）组件已完成。第 134、149 页并入正文；第 139–144 页合并为采样频域周期延拓的完整动画推导。已保留理想冲激采样、采样频率/角频率、频域复制、不混叠与混叠、Nyquist-Shannon 定理、折叠频率和频率单位对应；频谱图均以可编辑矢量形式重绘，300 dpi 视觉复核确认无水印、无重叠和无公式代码。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“时不变性”（源页 77–84）组件已完成。第 77、84 页并入正文；第 83 页为音频仿真实验，按范围约束排除。已保留时不变定义、两类累加器、抽取系统反例和滑动平均系统；300 dpi 视觉复核确认公式正式渲染、没有源码文字或重叠。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“线性卷积基础”（源页 85–97）组件已完成。第 85 页并入第 86 页，第 88 页并入概念页，第 93–94 页合并为完整例题详解，第 97 页为重复页；第 96 页为 MATLAB `conv` 与绘图代码，按范围约束排除。已保留 LSI、单位脉冲响应、卷积和推导、图解四步骤、移位单位脉冲性质与完整有限长序列例题；300 dpi 视觉复核确认公式、图内标签和页眉页脚均正常。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“基本运算”（源页 16–30）组件已完成并以 300 dpi 视觉复核。第 20 页仅引用既有移位组件以避免重复；其余内容已重写为和/积、回声、反褶、累加和/差分、尺度、能量与平均功率五页。已修复公式中混入中文导致的缺字，全部公式均正式渲染，分数均使用竖式，标签与图线不重合。该组件待与第一章其余内容自然续排，尚非最终章节分页。

> 2026-07-22 里程碑：第一章“典型序列”（源页 45–57）组件已完成。第 45 页并入第 46 页、第 57 页为完全重复页；第 52、54、56 页为 MATLAB 代码或绘图例程，按范围约束审计后不纳入成品。已保留单位抽样、阶跃、矩形、实指数、正弦与复指数的唯一概念信息，并补绘正弦与复指数的离散序列图；300 dpi 视觉复核确认公式完整渲染、无缺字、图内标签不重合。全量测试 64 项通过，待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“周期性”（源页 58–67）组件已完成。第 58 页并入第 59 页、第 67 页为完全重复页；第 66 页为 MATLAB 代码与作图输出，按范围约束审计后不纳入成品。已保留周期定义、正弦与复指数判据、求基本周期的方法、两道例题和调幅序列的频率/周期结论；300 dpi 视觉复核确认公式全部正式渲染、无源码文字、无重叠。该组件待与第一章正文自然续排。

> 2026-07-22 里程碑：第一章“线性性质”（源页 68–76）组件已完成。第 68、69 页并入正文，第 76 页为重复标题；第 75 页为音频仿真实验，按范围约束排除。已保留叠加原理、零输入零输出的必要性、平方系统、时间反褶系统与三点中值滤波器的判别例；300 dpi 视觉复核确认公式正式渲染、没有源码文字或重叠。该组件待与第一章正文自然续排。

## 已完成

- 5 页样稿已验证自然续排、纯白题干页、页码答案索引及公式基线：展示公式 11 pt，正文行内公式 17.5 pt、相对正文下移 3 pt；PDFium 检查无裁切或重叠，15 项测试通过。
- 全文任务已启动；真题范围规则已锁定：样稿中的 2004 年题只是版式演示，全文必须审计并按章节收齐所有相关华理 814 真题。

## 当前阶段

- 已将用户新确认的真题图处理优先级写入持久化规范：逐幅检查水印；有水印先去除；若覆盖题目有效信息则修复或重绘，绝不裁掉坐标、曲线、公式、单位或标签。
- 初步源审计完成：课件 1056 页、8 章、18 组动画递进候选；真题 205 页，实际可用年份为 2002–2007、2013–2017、2019–2025。真题正文 55 页均已人工页级核对，51 页已确认含 DSP 内容、4 页明确范围外；尚未声称任何章节真题已收齐。
- 已实现并测试题号归属模型：独立小题分入各自章节，关联小题完整放在最晚所需章节；真实逐题内容清单尚待录入。
- 已生成 8 份课件章节底稿，完整覆盖 1056 个来源页；18 组动画候选已逐对视觉审核，全部保留。每组均含新增推导、图示、例题或小节过渡，不能以文本相似为由合并。
- 真题逐题准备完成第一层：`full/artifacts/exam_question_sections.json` 从 51 页 DSP 正文提取 177 个原文段落（126 个题号起点、51 个续接段），并排除 4 页明确范围外页面；该清单尚未完成题组关联和最终章节归属。
- 已将 51 个跨页续接段按原文顺序并回前一题号，形成 `full/artifacts/exam_question_candidates.json` 中的 144 个完整原文题组候选；题组仍全部为 `pending_dependency_review`，尚未进行独立小题拆分或最终分章。
- 已生成 `full/artifacts/exam_question_candidates_annotated.json`：为 144 个题组附上可审查的自动章节候选。因原 PDF 的公式字体与图形文字提取限制，68 个题组没有自动结论，不能由关键词猜测，均保留给人工依赖审核。
- 2002 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2002.json`：填空题中的离散稳定性、奈奎斯特采样作为独立小题分别归入第 2、1 章；采样链三问整体归第 1 章；DFT 卷积和频率分辨率归第 3 章；连续时间题明确排除。
- 2003 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2003.json`：采样/差分方程归第 1 章，H(z) 稳定性归第 2 章，DFT 谱分析及其相关判断归第 3 章；跨页 DFT 谱分析题整体放在第 3 章，连续时间题明确排除。
- 2004 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2004.json`：离散系统方框图、梳状滤波器和 h(n) 频响归第 2 章，离散 LTI 阶跃响应归第 1 章，DFT 分辨率归第 3 章，FFT 重叠相加实现题整体归第 4 章；连续时间题明确排除。
- 2005 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2005.json`：z 反变换/DTFT 归第 2 章，离散差分系统归第 1 章，DFT 循环卷积归第 3 章；模拟系统离散化综合题整体归第 6 章；两道独立 IIR 设计方法小问均归第 6 章。
- 2006 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2006.json`：z 反变换/DTFT 反变换归第 2 章，离散系统结构归第 1 章，DFT 与重叠保留法归第 3 章；脉冲响应不变法和双线性变换法作为独立小问归第 6 章。
- 2007 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2007.json`：修复 OCR 将第 8、9、10 题误合并的问题；独立单元分入第 1/2/3/6/7 章，双线性变换证明、FFT 流图和频率采样 FIR 设计分别作为完整题组放在第 6/4/7 章。
- 2013 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2013.json`：采样/离散 LTI 归第 1 章，z 与频响归第 2 章，DFS 归第 3 章，多采样率题整体归第 8 章；连续时间题明确排除。
- 2014 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2014.json`：离散时变性、卷积与采样归第 1 章，DFS 归第 3 章，DTFT 和离散 LTI 系统函数完整题归第 2 章；连续时间题明确排除。
- 2015 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2015.json`：填空中的采样/基本运算归第 1 章，DTFT 归第 2 章，DFT 归第 3 章，FFT 归第 4 章，FIR 对称性归第 7 章；离散系统、零极点图归第 2 章，多采样率链整体归第 8 章。原卷第 29–31 页的中央水印和推广页眉页脚已视觉确认；含图的第 4、7、8 题已登记为重绘，不能直接复用原页图。
- 2016 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2016.json`：离散卷积归第 1 章，FT/LT/ZT 关系归第 2 章，时分复用归第 8 章；DSP 题的独立小题归第 1 或第 3 章，一次 DFT 计算两实序列归第 3 章，频率采样 FIR 设计归第 7 章。原卷含水印的离散序列图已登记为重绘。
- 2017 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2017.json`：离散系统性质归第 1 章，LT/ZT 关系与差分方程系统归第 2 章，第六题独立小题归第 3 或第 4 章，IIR 设计归第 6 章，FIR 窗函数设计归第 7 章；原卷水印图不直接复用。
- 2019 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2019.json`：离散系统性质、离散卷积、冲激采样及采样保持恢复归第 1 章，栅栏效应归第 3 章，FIR 零极点与线性相位归第 7 章；离散卷积图已登记为重绘。2016 年第六题经对照第一章采样内容纠正为第 1 章完整题组。
- 2020 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2020.json`：填空/简答独立分入第 1、2、3、4、5、8 章，DFT 计算题归第 3 章，离散 LTI 频率响应题归第 2 章；原卷题面水印不复用。
- 2021 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2021.json`：离散卷积与采样恢复归第 1 章，离散调制/解调归第 2 章，DFT 题归第 3 章，IIR/FIR 设计归第 6/7 章；含图题均已登记为重绘。
- 2022 年题组人工依赖审核已完成并写入 `full/source/exam_question_review_2022.json`：采样与采样恢复链归第 1 章，实序列 FFT 归第 4 章，离散 LTI 流图归第 2 章，IIR/FIR 设计归第 6/7 章；含图题均已登记为重绘。
- 2023 年人工审核已完成：OCR 合并的单一原文题组已按真实小题拆为采样/离散系统（第 1 章）、DFT/重叠保留（第 3 章）、DIT-FFT（第 4 章）、离散 LSTI（第 2 章）和 IIR 设计（第 6 章）共 8 个单元；原卷水印不会带入讲义。
- 2024 年人工审核已完成：OCR 合并的多页题组已按真实题目拆为 9 个单元，采样归第 1 章，DFT/FFT 归第 3/4 章，离散系统归第 2 章，FIR 归第 7 章；水印图均不直接复用。
- 2025 年人工审核已完成并写入 `full/source/exam_question_review_2025.json`：离散系统/采样归第 1 章，采样后的 DTFT、离散 LSI 和 A/D—D/A 链归第 2 章，DFT 归第 3 章，IIR/FIR 归第 6/7 章。所有可用年份现已完成题号级人工依赖审核。
- 最终题库已由 `full/tools/build_exam_bank.py` 生成至 `full/source/exam_question_bank.json`：156 个讲义题目单元已按 8 章索引，同时记录年份、来源页、依赖/落点章节及题图水印处置；53 个范围外候选被保留为审计排除项。测试确认 144 个原始候选均恰好得到人工处置。
- 章节“重点精练”选择已写入 `full/source/exam_drill_selection.json`：第 1–4、6–8 章各 3 道真实代表题，第 5 章仅有 1 道直接对应真题，后续以课内例题补足，不杜撰真题；同章其余题进入补充真题。

## 最新全局图形净空规则 — 2026-07-22

- 用户明确要求：任何图内文字、公式、坐标刻度与线条均不得重合。此规则已记录为全局硬约束；重合时先预留标签净空并调整标签位置，绝不移动或改变技术信息。
- 当前采样组件（源页 2–8）已将横轴样点编号向右偏移 4 pt，并把四行波形绘图区右移、给宽公式行标保留 24 pt 净空。相关模型与测试已更新；重新以 300 dpi 渲染复核后，所有标签与线条均不重合，待本次组件一起提交推送。

## 最新动画状态选择规则 — 2026-07-22

- 用户明确：不能机械保留每组动画的最后一张；若最后页的课堂方框、圆圈、箭头或批注妨碍公式辨认，应使用此前未遮挡但内容完整的状态，并以干净讲义式标注补入后页唯一信息。
- 已作为全局规则写入需求变更记录和交接文件。后续逐组动画审阅必须记录所选状态与理由；“最后一页”不能成为自动规则。

## 下一步

- 第一章“离散时间信号的表示方法”组件（源页 9–15）已完成可编辑转写、300 dpi 视觉复核和组件测试：第 15 页仅作为与第 10 页完全相同的重复页合并；数列的 `n=0` 下划线、单位抽样序列定义、移位加权和及所有 stem 图均保留。图内 `delta`、`ne` 等符号统一改走正式公式渲染路径，横轴名移出最右刻度栏，确保无缺符和重合。该组件已正确锚定到 `c1-01-representations`，而非章节导览；`full/tests` 复核 61 项通过。该 3 页 PDF 是第一章的待续排组件，不是最终分页。
- 第一章 5 个必须直接按源课件补写的内容块（源页 20、35、129、130、171）已建立为 `full/source/chapter_01_direct_rewrites.json`，并由 `full/tools/build_chapter_01_direct_rewrites.py` 生成 4 页可编辑 PDF。PDFium 渲染逐页复核确认：序列 stem 图、MATLAB `impseq` 代码、因果／非因果迭代推导、模拟—数字处理链的关键信息均完整；课堂圈画、模板和水印式元素未带入。此 PDF 是待拼接的内容组件，不是最终第一章分页。
- 用户新增认可二十四稿的封面、目录、附录 A 公式速查及真题训练／详解方向。下一步在完整正文生成器中建立封面、最终页码目录、附录 A 公式汇编及真题页码回填；公式范围待确认采用“华理 814 覆盖全量”还是“高频速查”。
- 附录取舍已锁定：A 完整公式速查、B 答题模板、C 去重后的分章综合训练、D 考前高频公式与检查表、E 真题导航、F 全部纳入真题的整理详解、I 全书自测保留；G（有限字长／工程实验）和 H（音频工程案例）不作为独立附录，其中原课件必要内容仍随对应正文保留。
- `full/source/chapter_01_content_model.json` 已建立第一章 18 个连续正文单元，逐页唯一覆盖源页 1–185。源页 20、35、129、130、171 已锚定到已验收的可编辑直接转写组件；两组动画递进页 142–143、164–165 显式标为保留。模型禁止最终分页照搬 PPT 页界，并要求所有图逐幅核验／重绘后再进最终版。

- 已审阅二十四稿视觉参考 PDF：采用其正式教材的深青蓝层级和低饱和黄铜金细线点缀，但不直接复用其内容或图形；图仍独立按无水印、信息完整、可打印标准重绘／修复。
- 已生成 `full/source/exam_training_manifest.json`：156 个已纳入真题恰好一次分配为章节“重点精练”或“补充真题”，每题都保留书末“真题整理详解”待最终分页回填状态及逐幅去水印／修复或重绘指令。
- 验证：新建的 `full/tests/test_build_exam_training_manifest.py` 已先失败（实现缺失），再通过；下一步将训练清单接入可自然续排的全文生成管线，并先制作题干转写、图形清理和书末详解页码回填机制。
- 已生成 `full/source/exam_figure_work_queue.json`：将 17 条带图题目记录归并为 11 项唯一源题图重绘／修复工作。每项均带水印、当前状态为 `pending_redraw_or_repair`，并列出受保护的技术标注和渲染验收条件；同一源图跨章使用时不会产生不一致的重复重绘。
- 验证：`full/tests/test_build_exam_figure_work_queue.py` 已先失败（实现缺失），实现后通过；全套回归现为 51 passed。下一步：实现可自然续排的全文生成管线，并从工作队列逐项重绘真题图。
- 已把用户认可的视觉方向实际写入样稿模板：深青蓝正文层级配低饱和黄铜金细线 `#B08D57`；渲染后的样稿第 1、2 页确认细线清晰而不抢正文／公式。样稿和全书自动测试共 69 passed。下一步：将相同版式令牌迁移到全文生成管线。
- 已隔离导入并从零重建上一工程的 13 批全文生成器与 119 页 A4 基线；旧工程回归 `246 passed`，当前根目录可运行的旧回归抽样 `8 passed`，当前样稿／真题测试 `69 passed`。该基线只作为实现资产：旧 7 章／436 页范围不等于当前 8 章／1056 页的最终交付，详见 `docs/legacy-full-baseline-audit.md`。下一步从它抽取共享排版和图形函数，创建当前 `full/` 的八章生成入口与覆盖审计。
- 已用当前 1056 页主课件生成两份页级覆盖审计。二十四稿平均匹配 `0.6380` 高于历史 119 页稿的 `0.5342`，但仍有 43 个源页低于 `0.30`；它只能作为内容补写优先基线，不能替代逐页保真。详见 `full/docs/content-baseline-comparison.md`。下一步：以审计为输入建立当前八章转写状态模型并生成第一章。
- 已生成 `full/source/transcription_status.json`：当前 1056 个源页均有唯一转写记录。43 个低覆盖／无文本源页被强制标记 `source_direct_rewrite_required`，其余页为必须对照源图和公式的 `baseline_reconcile_required`。新增测试先失败后通过。下一步：据此生成第一章可编辑正文模型与 PDF。
# 2026-07-26 连续续排：模拟—数字处理链组件已完成最终压缩。第 3 页先续入“采样间隔与实际恢复”的标题、导语和正式公式，再续入“处理链的检查顺序”及全文，原先只有一段正文的第 4 页已取消；组件从 4 页减至 3 页。测试按 TDD 先分别证明“第 3 页缺失续排标题”和“组件仍为 4 页”后失败，再在实现后通过。200 dpi 视觉复核第 3 页确认图、标题、公式、正文、页眉页脚无重叠；全量 `full/tests` 为 84 passed，`git diff --check` 通过，第一章总讲义已重建。下一步：继续对下一处正文或例题组件做相同的安全断点续排。
# 2026-07-26 连续续排：线性卷积基础组件完成页内续排。第 1 页续入“图解计算法：反褶、移位、相乘、相加”的标题和导语；第 2 页续入例题标题、题干及样值公式；第 3 页在完整保留两组 stem 图之后续入“例题详解：由脉冲响应直接求输出”的标题、导语和首个公式，剩余推导在下一页自然续接。所有移动均以标题、导语或完整公式框为安全边界，未拆分图形。三项测试均先失败再通过；200 dpi 渲染复核确认无重叠、裁切或重复标题。全量 `full/tests` 为 87 passed，`git diff --check` 通过，第一章总讲义已重建。下一步：继续处理其他正文或例题组件的同类空白。
# 2026-07-26 连续续排：离散时间系统时不变性组件完成两处安全续排。第 1 页续入“累加系统的时不变性”、例 1 标题及完整定义公式；第 2 页续入“抽取与滑动平均系统”、例 3 标题、导语与完整判别公式。后续推导均在下一页自然续接，未拆分公式框或引入仿真实验内容。两项页级断言均先失败再通过；200 dpi 渲染复核无重叠或裁切。全量 `full/tests` 为 87 passed，`git diff --check` 通过，第一章总讲义已重建。下一步：继续审查第一章其他组件的强制空白并按同一边界规则续排。
# 2026-07-26 全局规则补充：用户明确要求所有例题题目不得改动。已写入需求方案和变更记录：例题标题、已知条件、问法、数值、单位、变量、公式、图中题设标注及限定条件必须保留原课件信息；仅可修正已确认的客观错误。允许补全或覆盖的是题干后的详细解答，续排仅改变位置、不改变题干内容。后续例题转写与已生成例题审计均以此为硬约束。
# 2026-07-26 例题题干保真核对

- 已对线性卷积例题逐页比对原课件第 92 页：题干恢复为原有顺序和表述——先给 LSI 系统单位脉冲响应 `h(n)`，再给输入序列 `x(n)`，最后询问输出响应 `y(n)`；移除先前新增的改写性标题和合并式题干。
- 新增回归测试锁定原题干的三段文字以及 `h(n)`、`x(n)` 两个原式；测试先失败后通过。
- 验证：受影响组件测试 `4 passed`；全量 `full/tests` 为 `87 passed`；重新生成组件和第一章合成讲义，视觉核对合成讲义第 25 页无重叠、无截断。
# 2026-07-26 时不变性例题与公式裁切修复

- 对照原课件第 79–81 页后，已恢复时不变性组件中三道例题的原题表述：保留“验证下面的系统是否为移不变系统”题干以及三项原始系统条件；详细推导仍保留在题干后。
- 修复第一章合成器对公式图像的裁切缺陷：公式由 XObject 图像绘制，原先仅测量可提取文本，页底公式框可能被裁成残边；现将图像变换边界一并纳入正文裁切边界。
- 验证：时不变性组件测试 `2 passed`，合成器测试 `4 passed`；视觉检查第一章合成讲义第 22–23 页，原先被裁公式已完整显示、无重叠或截断。
# 2026-07-26 线性系统例题题干保真

- 对照原课件第 72–74 页，已恢复三个线性系统例题的原题表述与条件：平方系统 `y(n)=x^2(n)`（含原有“零输入产生零输出”说明）、时间反褶系统 `y(n)=x(-n)`、以及三点中值滤波器 `y(n)=Mid{x(k)}` 与 `n-1≤k≤n+1`。
- 原有“线性判别例”类改写标题已移除；详解推导保留在各自原题后。
- 视觉检查发现三点中值滤波器题干过长时一级标题会越界，现以较小的题干层级完整显示；线性系统组件测试 `2 passed`，全量测试在重建前为 `90 passed`。
# 2026-07-26 因果性与稳定性例题提示与编码修复

- 已安全恢复该组件遗留的 GBK 乱码文本，在绘制入口统一转换，不直接批量改写源字符串。
- 原课件两道 LSI 例题提示已恢复：已知单位脉冲响应 `h(n)`，分别判断系统的因果性与稳定性；其后原有判定内容保持。
- 修复稳定性条件页标题顶端裁切，改用安全的续排标题高度。组件测试 `2 passed`；视觉检查第 2、4 页中文、题干、公式均完整且无裁切。

# 2026-07-26 后向差分例题题干保真

- 对照原课件第 26 页，已将概括性标题“例：矩形序列的后向差分”恢复为原题：`例：x(n)=R_{10}(n)，试求后向差分信号 Δx(n)。`；题干后的差分说明和详解保持不变。
- 新增回归测试先确认原输出缺少“试求后向差分信号”而失败，修复后组件测试 `3 passed`；重建第一章合成讲义后，全量 `full/tests` 为 `92 passed`，`git diff --check` 通过。
- 以 200 dpi 视觉检查组件第 4 页：题干、行内公式和结果公式均无重叠或裁切。

# 2026-07-26 单位抽样序列例题题干保真

- 对照原课件第 14 页，已将被简化的题干恢复为：`例：用单位抽样序列 δ(n) 表示任意序列 x(n)={1,2,3}`；题干后的图示与展开过程保持不变。
- 新增回归测试先确认原输出遗漏“表示任意序列”而失败，修复后组件测试 `3 passed`；重建第一章合成讲义后，全量 `full/tests` 为 `93 passed`，`git diff --check` 通过。
- 以 200 dpi 视觉检查组件第 3 页：完整标题、四幅序列图与结果公式均无越界、重叠或裁切。

# 2026-07-26 有限支持卷积区间例题题干保真

- 对照原课件第 102–104 页，已恢复原题的“例1：有两个序列”、两段非零区间、`设：y(n)=x(n)*h(n)` 与“问：y(n)不为零的区间为：______。”；题后重叠条件推导和结果公式保持不变。
- 新增回归测试先确认原输出缺少“例1：有两个序列”而失败，修复后组件测试 `2 passed`；重建第一章合成讲义后，全量 `full/tests` 为 `94 passed`，`git diff --check` 通过。
- 以 200 dpi 视觉检查组件第 2 页：题干、留白、公式框和推导均无重叠或裁切。

# 2026-07-26 周期性 A/B 例题题干保真

- 对照原课件第 64 页，已恢复完整题干“例：（1）判断序列 x(n) 是否有周期，如果有请计算其周期。”及 A、B 两小问；B 小问的原始相位条件 `e^{j(n/6-π)}` 已恢复，题后周期判定仍保留。
- 原课件第 65 页的 MATLAB 绘图要求继续按用户确认的范围排除，仅保留非编程的数学知识内容。
- 新增回归测试先确认原输出缺少完整题干而失败，修复后组件测试 `2 passed`；重建第一章合成讲义后，全量 `full/tests` 为 `95 passed`，`git diff --check` 通过。
- 以 200 dpi 视觉检查组件第 2 页：A/B 题干、公式框和结论均无重叠或裁切。

# 2026-07-26 因果性与稳定性例题列表保真

- 对照原课件第 117、119、120、122 页，已恢复一般系统因果性判断的（1）至（5）编号、LSI 因果性和稳定性判断的（1）至（4）编号，以及原有的 `n≥2`、`n≤−1`、`n−n_0+1` 项求和、绝对可和等限定说明。
- 修复过程中发现数学渲染器不支持 `\ge` / `\le` 简写，已改用等价的标准 `\geq` / `\leq`，不改变题干含义或显示内容。
- 新增回归测试先确认原输出缺少编号和条件而失败，修复后组件测试 `3 passed`；重建第一章合成讲义后，全量 `full/tests` 为 `96 passed`，`git diff --check` 通过。
- 以 160 dpi 视觉检查组件全部四页：题干、编号、公式和说明均无重叠或裁切。

## 2026-07-26 因果性与稳定性组件连续续排

- 第 1 页续入“LSI 系统的因果性条件”的标题、说明与完整公式；第 2 页续入“一般系统的稳定性”的标题、BIBO 定义与完整公式。后续页以“（续）”标题自然接排，未拆分题干、图形或公式框。
- 对照原题条件，LSI 因果性例（4）完整保留为 `-∞≤n≤∞`；全书例题题干一律不得改动，后续只允许在题干之后补全详解。
- 新增两项页级续排回归均先失败后通过；组件测试 `5 passed`，全量 `full/tests` `98 passed`，`git diff --check` 通过。200 dpi 复核第 1–3 页，无重叠或裁切，页面利用率改善。

## 2026-07-26 卷积性质组件连续续排

- 第 1 页续入“有限支持序列的卷积区间”原题题干；第 2 页续入“应用例：延时叠加系统”的标题与已知条件；第 3 页续入“实序列的相关：相似度与延时”的标题、导语与小节标题。第 2–4 页均以“（续）”自然接排。
- 全部移动均以完整标题、原样题干、完整公式或完整段落为边界；例题题目本身未改动。
- 三项页级续排回归均先失败后通过；组件测试 `5 passed`，全量 `full/tests` `101 passed`，`git diff --check` 通过。200 dpi 复核受影响的四页，无重叠或裁切。

## 2026-07-26 时域采样恢复组件连续续排

- 原第 4 页仅含“样值不变性”小节、一个公式与一段说明；已将该完整小节续入第 3 页，组件从 4 页压缩为 3 页。
- 公式框、内插波形、正文及页眉页脚均经 200 dpi 视觉复核，无重叠或裁切。
- 页数回归先失败后通过；组件测试 `1 passed`，全量 `full/tests` `101 passed`，`git diff --check` 通过。

## 2026-07-28 常系数线性差分方程组件连续续排

- 第 1 页续入“迭代法：因果单位脉冲响应”的完整标题与导语；第 2 页续入“迭代法：非因果单位脉冲响应”的完整标题与导语；第 3 页续入“由差分方程得到系统结构”的标题、导语、公式与图题。系统结构图保持为完整块在第 4 页续排。
- 三项页级续排回归均先失败后通过；组件测试 `4 passed`，全量 `full/tests` `104 passed`，`git diff --check` 通过。200 dpi 复核四页，无重叠或裁切。

## 2026-07-28 第二章逐页转写启动

- 用户确认第二章按每张内容转写：保持例题原题、补足详解，纳入相关真题训练与章末详解。
- 执行节奏：第二章制作期间可进行本地测试和视觉验收；正文、训练与详解全部完成后，仅提交并推送一次。
- 当前下一步：建立第二章 186–387 页的逐页审计与验收清单，再按 2.1–2.6 顺序开始重制正文和图形。

## 2026-07-28 第一章紧急版面整改

- 用户审阅当前第一章 PDF 后指出：多处正文页仍有大面积空白；单位抽样序列的分段函数未采用标准紧凑排版；复杂图形不应在错误重绘与整张原 PPT 图之间一律选择前者。
- 已定位根因：总讲义合成器把每个组件页面视为不可拆分的整块，组件内部的 `showPage()` 又把低内容页固化；分段函数由多个手工坐标的独立公式片段组成。后续先对这两个入口建立失败测试，再逐一修正。
- 第二章暂停，待第一章完成全章重排、分段公式修复与图形策略审查后，再将同一规范应用到第二章。

## 2026-07-29 第一章局部修复验证

- 已把“连续时间信号与离散时间序列的对应”与单位抽样序列分开为完整内容块；200 dpi 视觉复核确认两幅对应图、所有坐标轴及图的下边界均未裁切，分段函数已改为同一紧凑公式块。
- 已将线性系统的两个后续例题标题和定义公式分别续入前页可用空间，三页均经视觉复核无重叠；对应组件回归与表示方法组件回归共 `7 passed`，`git diff --check` 通过。
- 全章验收尚未通过：组装后的第一章仍为 59 页，现有“少于 55 页”的全章回归失败，表明其余组件仍有空白页需要逐项重排；不得提交或推送为完成状态。
- 下一步：继续按组件逐页重排，并在每个复杂图块上执行上下边界视觉检查；完成后再执行全章重建、完整测试与提交。

## 2026-07-29 第一章全局续排与差分方程重排

- 章节收束组件已从 4 页压缩为 2 页：完整保留两条采样公式、车轮图、图注、混叠说明与第一章小结；视觉复核确认图、正文和页脚均未裁断。
- 常系数线性差分方程组件由 4 页重排为 3 页：因果响应结果续入首页，非因果响应与结构方程组成第二页，完整反馈结构图单列第三页；复杂图使用完整原图，避免重绘或裁切丢失有效信息。
- 合并器现在以更紧凑但保留 22–34 pt 内容安全边距的方式续排；第一章最终合并版由 57 页降至 51 页。`test_chapter_01_difference_equation_component.py` 与 `test_build_chapter_01_handout.py` 共 9 项通过，最终 PDF 已逐页抽检分段函数、连续/离散对应图与反馈结构图。
- 已推送阶段提交：`b813ade`、`f649fbf`。第二章继续暂停；下一步为核验第一章其余正文页的图形边界和低密度页，再决定是否可作为“第一章完成”提交。

## 2026-07-29 第一章真题分页与最终图形复核

- 根因确认：补充真题组件原先把多题写入同一组件页，合并器又只保护了优先训练页，因此最终 PDF 可能将不同真题压到同一页；这与“一题一页”规则冲突。
- 已先新增失败回归，再将补充真题拆为 8 个独立 A4 题页，并让合并器同时保护优先训练与补充真题的页界。最终 11 道训练题均为独立打印页；答案索引已按最终 62 页版回填为 P.59–P.62。
- 已对最终 PDF 的第 3、4、51、59、61 页进行 180 dpi 视觉核验：连续/离散对应图的上下边界完整；单位抽样分段函数为完整两行大括号；训练页仅含一题；答案页无裁切或重叠。
- 验证：新增训练分页回归先失败后通过；`full/tests/test_chapter_01_training_component.py` 与 `full/tests/test_build_chapter_01_handout.py` 共 8 项通过。下一步：运行第一章全量回归、检查变更范围并提交推送本次修复。

## 2026-07-29 第一章正文续排迭代

- 最终 PDF 密度扫描定位了正文低密度页；先修复第 3 页连续/离散对应图后的大空白，将“离散时间信号的表示方法”完整标题与导语前移，下一页从“用数列与函数表示”安全续排。
- 典型序列组件同步采用同一规则：单位阶跃序列后前移“矩形序列与实指数序列”标题，实指数序列后前移“正弦序列”标题；原有公式和图形仍从完整内容块续排，没有拆分或裁切。
- 新增回归先失败后通过；全量 `full/tests` 为 `113 passed`，`git diff --check` 通过。下一步：继续处理密度扫描标出的其他正文页，特别是移位、累加差分、周期性与线性性质组件。

## 2026-07-29 第一章图形边界与基本运算续排

- 已重新生成最终合并 PDF 并以 180 dpi 复核第 3、4、9、10 页：连续/离散对应图的上下边界、离散 stem 图、刻度和说明完整；单位抽样分段函数为紧凑两行大括号；页面标题已下移至页眉横线下方。
- 基本运算组件把累加和、差分定义和前后向差分按完整语义块续入第 9 页，第 10 页从例题和时间尺度继续，避免原有低密度独立页；图形与公式均未裁切或重叠。
- 已通过相关组件回归 `11 passed`（表示方法、起源、基本运算）。待执行完整 `full/tests`、`git diff --check`，并仅提交本次涉及的工具、测试、PDF 与规则记录；第一章尚未完成，第二章继续暂停。

## 2026-07-29 典型序列页续排

- 第 11 页将矩形序列的三条完整公式和解释前移；第 12 页承接实指数与正弦序列的完整公式、说明与 stem 图，消除了原有大面积空白。
- 已移除典型序列组件残留的全部“复习提示”输出；主标题统一下移并在最终合并 PDF 中检查页眉净空。第 11、12、13 页的公式、坐标与图形完整且无裁切。
- 组件回归 `3 passed`，全量 `full/tests` `115 passed`，`git diff --check` 通过；已提交并推送 `88c286b`。下一步继续扫描第一章下一批正文低密度页；第一章尚未完成，第二章继续暂停。

## 2026-07-29 周期性页面续排

- 第 14 页已承接“由频率求基本周期”、有理性判据公式及完整解释；第 15 页从例题续排。已删除周期性组件的提示框输出并将组件主标题下移。
- 组件回归 `3 passed`；全量 `full/tests` `116 passed`、`git diff --check` 通过，已提交并推送 `2dfe4c6`；继续第一章低密度页扫描。

## 2026-07-29 表示图、分段函数与线性性质复核

- 最终合并 PDF 的第 3、4 页已重新生成并以 180 dpi 逐页核对：连续时间波形、离散 stem 图、坐标轴、刻度、`T` 标记、图下说明和分段函数的两行条件均完整可见；表示页主标题已下移，避免压到页眉横线。
- 线性性质组件将平方系统例题的完整定义与“零输入产生零输出”引导段续入前页，后页从叠加检验自然接续；第 16–18 页视觉复核未见重叠、裁切或不必要的大面积空白。
- 验证：新增标题净空回归与线性续排回归通过；全量 `full/tests` `118 passed`，`git diff --check` 通过。已提交并推送 `97670ac`（`fix: preserve chapter one graph boundaries`）。下一步：继续扫描第一章其他正文低密度页；第二章继续暂停。

## 2026-07-29 卷积基础页续排

- 第 20 页已承接图解计算法的“反褶”“移位”两个完整步骤；第 21 页从“相乘”“相加”继续，例题题干保持原样，并在题干结束后整体续入输入与单位脉冲响应两幅 stem 图；第 22 页从逐步计算、输出 stem 图和脉冲分解详解自然续排。
- 第 20–22 页以 180 dpi 视觉复核：公式、两幅输入图、输出 stem 图、坐标轴、题干与页眉页脚均无重叠或裁切，正文页不再有不必要的大面积空白。
- 验证：组件回归 `6 passed`，全量 `full/tests` `120 passed`，`git diff --check` 通过。已提交并推送 `ca7c3d4`（`fix: continue convolution basics pages`）。下一步：继续第一章的低密度页面扫描；第二章继续暂停。

## 2026-07-29 卷积性质页续排

- 第 24 页已在“延时叠加系统”引导后续入“输出推导”及第一条完整公式；第 25 页从结果公式、解释和相关主题续接。移除只含标题的悬空“互相关与自相关”，最终合并页中不再出现重复的相关续标题。
- 第 24、25 页以 180 dpi 视觉复核：所有公式、标题、正文和页眉页脚均无重叠或裁切，正文密度自然。
- 验证：组件回归 `7 passed`，全量 `full/tests` `122 passed`，`git diff --check` 通过。已提交并推送 `c3533bf`（`fix: continue convolution property pages`）。下一步：继续第一章其余正文页审校；第二章继续暂停。

## 2026-07-29 因果性与稳定性页续排

- 第 26 页已在 LSI 因果性条件后续入“四个单位脉冲响应例”及完整四项判断；第 27 页从判题步骤、一般系统稳定性和 LSI 稳定性条件连续承接。例题的已知条件、变量、范围与结论均原样保留。
- 第 26、27 页以 180 dpi 视觉复核：标题、公式、正文、分项条件、页眉和页脚均无重叠或裁切，正文没有不必要的大面积空白。
- 验证：组件回归 `6 passed`，全量 `full/tests` `123 passed`，`git diff --check` 通过。已提交并推送 `3e57214`（`fix: continue causality and stability pages`）。下一步：继续第一章其余正文页审校；第二章继续暂停。

## 2026-07-29 表示页交付版本复核

- 针对用户报告的旧版截图，重新以 200 dpi 从当前 `full/outputs/chapter_01_handout.pdf` 直接渲染第 3--5 页核验：第 3 页连续波形和离散 stem 图、坐标轴、刻度、`T` 与图下说明完整；第 4 页单位抽样序列为同一紧凑公式框中的两行大括号、取值及条件，均未裁切。
- 已把“最终合并 PDF、带版本预览、两项完整性”加入持久化要求。下一步：提交该要求记录并为用户生成带版本标识的当前预览 PDF；第一章其余正文审校仍继续，第二章暂停。
- 已提交并推送 `72741da`（`docs: record representation preview verification`）；预览文件为 `full/outputs/chapter_01_handout_preview_20260729_2320.pdf`。

## 2026-07-29 差分方程续排与全局页眉净空

- 差分方程页已将“非因果单位脉冲响应”的完整标题和导语续入因果迭代页；后页从完整“（续）”标题及反向迭代公式接续。一阶反馈结构图继续保留为完整原图块。
- 最终合并器的正文起点已下调，避免重建页眉后一级标题压到横线。以 180 dpi 抽查第 3、4 页的连续/离散对应图、分段函数，以及受影响的差分方程页，图形完整、标题与页眉分离。
- 验证：差分方程与合并器回归 `12 passed`；全量 `full/tests` `125 passed`；`git diff --check` 通过。下一步：提交并推送本里程碑，再继续第一章其余正文页审校；第二章暂停。
- 已提交并推送 `aaa5d8c`（`fix: preserve header clearance and continue difference equations`）；当前预览为 `full/outputs/chapter_01_handout_preview_20260729_2340.pdf`。

## 2026-07-29 表示方法页连续续排

- 将“用数列与函数表示”的完整标题、导语和首个数列公式续入连续/离散对应图所在页；下一页从“（续）”解释、函数式、单位抽样序列和完整 stem 图承接，未拆分分段函数或图形。
- 以 180 dpi 复核最终合并 PDF 第 3、4 页：连续波形、离散 stem 图、数列公式、两行分段大括号及另一幅 stem 图均无裁切或重叠，正文密度改善。
- 验证：相关组件回归 `9 passed`；全量 `full/tests` `126 passed`；`git diff --check` 通过。下一步：提交推送后继续第一章低密度页审校；第二章暂停。
- 已提交并推送 `70653d9`（`fix: continue representation method pages`）。

## 2026-07-29 周期性例题页连续续排

- 将“周期求解方法与调幅序列”及“组合序列的处理”完整前移到 A/B 周期判定例题的结论后；调幅公式、参数结论从下一页“（续）”承接，题干及 A/B 小问未改动。
- 以 180 dpi 核验最终 PDF 第 15、16 页：A/B 公式、正文、标题、页眉页脚均无重叠或裁切，例题页不再出现原有的半页空白。
- 验证：组件回归 `4 passed`；全量 `full/tests` `127 passed`；`git diff --check` 通过。下一步：提交推送后继续第一章低密度页审校；第二章暂停。
- 已提交并推送 `785b2a5`（`fix: continue periodicity example pages`）。
## 2026-07-29 线性性质例题连续续排

- 将线性性质组件由 3 页调整为 2 页：首页承接平方系统的叠加检验；下一页完整连续呈现时间反褶系统和三点中值滤波器反例，不拆分题干、定义、公式或结论。
- 修复中值滤波器结论中长行内公式与正文叠压：判定式改为独立公式框，文字结论随后自然续排。
- 已在最终合并 PDF 第 17--19 页以 180 dpi 核验，无公式、正文、页眉页脚重叠或裁切；`full/tests` 全量回归 `128 passed`，`git diff --check` 通过。
- 当前下一步：继续审校第一章其余正文低密度页；第二章继续暂停，直至第一章完成验收。
## 2026-07-29 时不变性页面续排

- 移除了时不变性组件全部提示卡片；将从负无穷开始累加器的完整推导、公式与结论续入第一个例题页，下一页从“（2）”继续。
- 最终合并 PDF 第 19--21 页以 180 dpi 视觉复核，无正文、公式、页眉页脚重叠或裁切；`full/tests` 全量回归 `128 passed`，`git diff --check` 通过。
- 当前下一步：继续审校第一章其余正文低密度页；第二章仍暂停，直至第一章完成验收。

## 2026-07-29 第二章 2.2 共轭对称性质组件

- 已完成共轭对称与共轭反对称分量、时域频域对应、实序列特殊情况及原课件实因果序列例题的三页可修改组件；例题题干保留原样，解答补全了抵消负时刻项、奇对称性和结果检验。
- 180 dpi 逐页复核：公式、正文、页眉页脚均无重叠或裁切；首两页已将后续完整内容前移，组件末页保留给整章合并器继续承接后续 2.3 内容。
- 验证：组件测试先失败后通过；全量 `full/tests` `138 passed`，`git diff --check` 通过。下一步：继续第二章 2.3 系统函数及其与系统性质的关系。

## 2026-07-29 第二章 2.1 性质组件与连续续排

- 已完成线性、移位、卷积和其他常用 z 变换性质的三页可修改组件；有限长矩形序列和零极点相消例题的题干、已知条件、求解目标均保持原样，后接 ROC 解释与推导。
- 第二页在“判定要点”后续入“z 变换的卷积和性质”的完整标题、导语和总公式；第三页从“卷积和性质（续）”承接例题，避免因强制分页留下大片空白。
- 以 180 dpi 渲染复核第二、三页：所有公式、正文、页眉页脚无重叠或裁切。验证：组件测试通过；全量 `full/tests` `136 passed`，`git diff --check` 通过。
- 下一步：继续依据相同规则完成第二章 2.1 余下原课件内容，随后汇编、整章视觉验收并发布到 `output/`。

## 2026-07-29 第二章 2.2 DTFT 基础组件

- 已完成离散时间信号傅里叶变换基本定义、正反变换、绝对可和收敛条件、2π 周期性及 z 变换单位圆关系的三页可修改组件；第三页继续进入共轭复数、共轭对称与共轭反对称分量的定义。
- 通过前移相邻完整标题、导语和公式压实三页：不拆积分或公式框，且没有以 PPT 页面边界强制留白。以 180 dpi 渲染检查三页，所有公式、正文、页眉页脚无重叠或裁切。
- 验证：新增组件先失败后通过；全量 `full/tests` `137 passed`，`git diff --check` 通过。
- 下一步：继续 2.2 的共轭对称分量、频域对应关系与原例题，并依同一规则汇编第二章。

## 2026-07-29 第一章交付与第二章 2.1 首批重制

- 第一章已完成整章验收并发布至 `output/数字信号处理讲义_第一章_离散时间信号与系统.pdf`；`output/` 仅保留已完成章节成品，未放入组件或临时预览。
- 第二章 2.1 已完成首批五页：z 变换定义、s--z 平面映射、收敛域、反变换、典型序列与同一代数式不同 ROC 的例题。复杂的 ROC 分类图直接嵌入原课件完整有效图，避免不完整重绘；所有公式均为可渲染公式。
- 已按页修复固定分页造成的空白：将系统函数、极点与 ROC、判定顺序和有限长序列 ROC 接回同一批页面；180 dpi 视觉检查确认正文、图形、公式、页眉和页脚无重叠或裁切。
- 验证：组件测试先失败后通过；全量 `full/tests` 为 `134 passed`，`git diff --check` 通过。下一步：继续完成 2.1 后续例题、推导与收敛域图示，并保持原题干不改。

## 2026-07-29 第二章 2.1 反变换方法续排

- 已完成部分分式展开、幂级数展开及其原课件例题的可修改组件。两道例题的已知式、收敛域与求解目标保持不变；在其后补全了极点代入、系数求解、ROC 方向判定和反变换结果。
- 组件由固定四页续排为三页：第 1 页承接部分分式系数计算，第 2 页承接两个例题的结果，第 3 页承接方法比较、结论与线性性质导入。逐页渲染确认无公式裁切、重叠或大面积无意义空白。
- 验证：组件测试通过；全量 `full/tests` 为 `135 passed`，`git diff --check` 通过。下一步：继续 2.1 的线性、移位、卷积性质和对应原例题。
## 2026-07-29 稳定性条件至差分方程连续续排

- 将差分方程的完整标题、导语和总公式续入 LSI 稳定性条件页；差分方程组件首面改从“常系数线性差分方程（续）”和术语说明开始。
- 删除稳定性条件页残留的提示卡片输出，避免恢复已废止的“复习提示”式内容。
- 最终合并 PDF 第 29--30 页已以 180 dpi 视觉复核，无重叠、裁切或不必要的大面积空白；`full/tests` 全量回归 `129 passed`，`git diff --check` 通过。
- 当前下一步：继续审校第一章剩余正文低密度页；第二章仍暂停，直至第一章完成验收。
## 2026-07-29 反馈结构图后采样参数续排

- 在完整反馈结构图与“理想时域采样”导语之后，续入采样参数公式；采样组件续页从幅度解释开始，频域示意图仍保持完整。
- 最终合并 PDF 第 32--33 页已以 180 dpi 视觉复核，无重叠、裁切或不必要留白；`full/tests` 全量回归 `130 passed`，`git diff --check` 通过。
- 当前下一步：继续审校第一章其余正文低密度页；第二章仍暂停，直至第一章完成验收。

## 2026-07-29 第一章图形重审与表示方法矢量续排

- 按全局数学公式与图形规范重新审计第一章的 20 个组件：旧的 ReportLab 手工 stem/轴、手工大括号、公式位图、固定坐标结构图和课件截图均不合规；审计结论维持为“不通过”，旧组件不得再作为第一章最终成品依据。
- “离散时间信号的表示方法”已替换为 MathJax 完整 LaTeX 与真实 SVG stem 图组件；其分段函数、三组数列、单位抽样序列及移位加权和在 A4 渲染中均未见裁切或重叠。组件内重复章节眉线已删除，仅保留总讲义统一页眉。
- 合成器现从 HTML/SVG/MathJax 的真实矢量几何边界计算裁切范围，不再因 PDF 文本抽取遗漏矢量路径而截掉公式或图。回归：第一章合成器 7 项测试通过；已重新渲染临时 A4 PDF 复核。
- 未完成项：表示方法页尾与下一旧“基本运算”组件之间仍不能进行真正块级续排，仍会留下不允许的大空白；原因是下一组件仍为固定整页旧式实现。下一步先重做“基本运算”组件为统一 MathJax/SVG 流式组件，再与表示方法一起合成，禁止把当前 60 页 PDF发布到 output/ 成品目录。

## 2026-07-29 第一章基本运算组件重做

- “离散时间信号的基本运算”已新建为 MathJax/SVG 流式组件并接入第一章临时合成：序列和/积、反褶的六张离散序列图均由真实样点数据生成标准 stem 图；回声、累加和、前/后向差分、时间尺度、能量和平均功率均由完整 LaTeX 整体渲染。
- 组件 A4 渲染为四页；第 7--9 页实际视觉复核：公式、上下标、求和限、坐标箭头、负索引与样点均清楚，未见裁切、叠压或手工大括号。回归：表示方法、基本运算、合成器共 9 项通过。
- 当前临时合成的第 10 页仍有大面积余白，因为下一“典型序列”仍为旧固定页组件而无法接续；下一步重做典型序列组件后才允许把后续内容填入，不能用留白掩盖旧组件边界。

## 2026-07-29 第一章典型序列组件重做

- “几种常用的典型序列”已迁移到 MathJax/SVG：单位抽样、单位阶跃、矩形、两类实指数、正弦、复指数实部/虚部/模均为完整 LaTeX 公式和真实样点 SVG stem 图；已排除 MATLAB 内容。
- 为满足自然续排，典型序列首块仅保留标题、导语、单位抽样定义，作为可接入前页余量的块；余下正文从下一页继续。临时合成 PDF 第 10 页已实际复核，原本大面积空白已被“典型序列”标题、导语和分段定义填入；第 11 页接续图形与单位阶跃内容。
- 回归：前三个迁移组件与第一章合成器共 10 项测试通过。后续仍须迁移周期性、线性、时不变、卷积、差分方程、采样及结构图组件，旧固定坐标实现不得作为最终输出。

## 2026-07-29 第一章周期性组件重做

- “离散时间序列的周期性”已迁移到 MathJax 流式组件：周期定义、正弦周期条件、有理性判据、余弦和复指数两个判定例、组合序列方法、调幅频率分量及最小公倍数结论均按原顺序保留。
- 新组件为两页 A4；实际渲染复核显示分数、集合、最大公约数、复指数、最小公倍数和上下标均完整，无位图公式、裁切或重叠。
- 回归：前四个迁移组件与第一章合成器共 11 项测试通过。下一步迁移线性与时不变组件；现有旧组件仍不能作为最终输出。

## 2026-07-29 第一章线性性质组件重做

- “离散时间系统的线性性质”已迁移到 MathJax 流式组件：可加性、比例性、叠加原理、平方系统反例、时间反褶系统证明和三点中值滤波器反例均完整保留。
- 新组件两页 A4 实际视觉复核：方括号、集合、求和、交叉项、Mid 算子与不等号均为整体数学渲染，无公式位图、裁切或重叠。
- 回归：前五个迁移组件与第一章合成器共 12 项通过。下一步迁移时不变组件，并继续把相邻组件的尾部空白以可续排块填入。

## 2026-07-29 第一章时不变性质组件重做

- 将“离散时间系统的时不变性质”改为完整 MathJax 流式组件：定义、两条判别路径、两种累加器、尺度变换反例与滑动平均系统均以完整 LaTeX 公式整体渲染；未使用公式位图、HTML 拼接或手工定界符。
- 经 A4 实际渲染与合成预览检查，线性性质末页以“时不变性”的标题和导语自然承接；定义、判别步骤和例题再续页，未裁切任何公式或缩小公式字号掩盖分页问题。
- 验证：六个已迁移组件与第一章合成器共 13 项测试通过；已检查合成预览第 20--25 页，页眉、页脚、正文和公式无重叠或裁切。下一步：继续重做卷积、系统性质、差分方程与采样等仍采用旧固定坐标/公式位图的第一章组件，并逐一复核图形。

## 2026-07-29 第一章卷积、因果稳定性与差分方程重做

- 已将“线性卷积求解与运算规则”“因果性与稳定性”“常系数线性差分方程”接入第一章的 MathJax/SVG 管线。卷积例题保留原题干，输入、单位脉冲响应和输出均为数据驱动的标准 stem 图；运算律、相关、因果、稳定和差分方程均为完整 LaTeX 渲染。
- 差分方程的一阶反馈结构图已重绘为结构化 SVG：主信号自左向右，反馈支路经过标准延时块、增益块并接入求和器负端；所有系数标签用同一 MathJax 公式路径渲染。实际 A4 预览已核对箭头、支路、标签和公式没有重叠或裁切。
- 续排验收实际修复了原先卷积输出图和相关页后的大块空白：相邻章节的可独立起始块被拆分并自然续接。第 23--30 页预览已检查，卷积、因果稳定性、差分方程和结构图均无空白整页、公式裁切或图文叠压。
- 验证：组件与第一章合成器共 11 项相关测试通过；本地重建整章 PDF 并以 150 dpi 逐页预览。下一步继续迁移采样、恢复和模拟—数字处理链组件，并按相同的可续排规则检查第一章尾部。

## 2026-07-29 第一章采样定理组件重做

- “理想时域采样”已替换为完整 MathJax 与数据定义 SVG 频谱图：周期冲激采样、采样频率/角频率、频域周期延拓、不混叠与混叠、Nyquist–Shannon 条件、折叠频率和抗混叠滤波均按原顺序转写。
- 三幅频谱图采用真实坐标轴、箭头、\(\Omega\) 轴标记和等间隔谱副本；不混叠与混叠用不同副本宽度体现是否发生交叠，不再使用旧的 ReportLab 固定绘图或公式位图。
- 验证：采样组件及第一章合成器相关测试 8 项通过；A4 预览第 31--33 页检查了中文、公式、频谱轴、标签和重叠关系。第 33 页仍需由下一“采样工程与恢复”组件继续填充，不能作为最终章节尾页。

## 2026-07-29 第一章图形重审：处理链与采样案例

- “模拟信号的数字处理方法”已由旧 ReportLab 手工布局重制为 MathJax + 数据定义 SVG：模拟—数字处理链采用标准左到右模块、箭头和域标记；零阶保持采用真实样值生成的 stem 与阶梯波形。独立 A4 预览确认模块、公式、坐标标签和箭头没有重叠。
- “从采样定理到信息论”已由旧 ReportLab/公式位图实现迁移到 MathJax + SVG；其中两个连续信号等值采样关系用完整 LaTex，车轮混叠图用几何生成的轮辐、轮毂和标记绘制，不再使用截图或手工拼贴。
- 回归：上述两个迁移组件及第一章合成器相关测试 9 passed，git diff --check 通过。
- 发现并修复候选：处理链末尾的“处理链的检查顺序”在合并版曾留下整页空白；已改为与标题保持在同一自然续排块内。该修复尚未重新渲染验收：系统额度在调用本机 Edge 视觉复查时被拦截，需恢复后立即重建整章、重点检查第 40--42 页，然后才允许提交该里程碑。
- 后续实测验收已完成：将“从采样定理到信息论”首段拆为可续排块后，第 41 页由该段自然填充，整页空白消失；同时将车轮图题与整图设为不可拆分，最终第 42--43 页图题、图形、正文、小结均无分离、重叠或裁切。
- 最新验证：两项新组件及第一章合成器相关测试 8 passed，git diff --check 通过；已通过 150 dpi 最终 PDF 预览检查第 41--43 页。下一步：提交并推送本轮处理链与采样案例迁移，然后审计第一章训练、补充题与答案组件中遗留的旧式公式/图形。
## 2026-08-01 第二章真题训练并入候选讲义

- 已将 2015、2021、2025 三道原题题干按原文纳入第二章「分章强化训练」；每道题均从新页开始，保留自然作答空白。详解采用连续续排，避免把普通详解拆成空白整页。
- 2015 年题使用无水印、真实 SVG 零极点图；2021 年题使用无水印的调制、频谱搬移与相干解调 SVG；2025 年题全部公式以 MathJax 整体渲染。150 dpi 逐页检查第 15--20 页，未见公式源代码、公式裁切、图文重叠或页眉页脚冲突。
- 已按实际合成 PDF 的 20 页分页校正交叉页码：2015 年题、2021 年题均为「详解见 P.18」，2025 年题为「详解见 P.19」。相应分页与页码回归测试通过（2 passed；pytest 缓存目录权限警告不影响结果）。
- 当前 `full/outputs/chapter_02_mathjax_handout.pdf` 仅为 20 页候选稿：尚未加入本章全部补充真题与全书末尾详解，不能写入根目录 `output/`，也不能作为第二章完成成品。
## 2026-08-01 第二章补充真题第一批与公式渲染回归

- 已将 2002、2003、2004、2005、2006、2007 年的八项第二章关联真题按原题干补入候选讲义；不复制含水印试卷图片，题内数学全部使用完整 MathJax LaTeX 渲染。对应详解涵盖连续/离散稳定性、ROC、梳状滤波器、由单位取样响应判别滤波器、DTFT 与指定 ROC 的反 z 变换。
- 新增浏览器端公式回归：必须实际生成 MathJax 节点，且 2003 年题干中的行内 H(z)、a、b 不得以 LaTeX 源码残留。修复了真题页右侧的内部流程措辞，统一显示为“详解见 P.____”；最终全书合成后再回填实际页码。
- 候选 PDF 已重新生成并以 150 dpi 抽查第 21、27、28、30 页：题干、公式、上下标、分数、分段函数、页眉页脚均未见源码泄漏、裁切、重叠或错位。训练题页按用户确认的“一题一页”保留作答空间；候选稿尚有后续真题未加入，仍不得复制到根目录 output。
- 验证：第二章补充真题组件、浏览器 MathJax 回归和第二章总稿组装测试共 3 passed；pytest 仅有既有 .pytest_cache 权限警告。下一步：继续按题库逐批补入剩余第二章真题，并在所有题目和书末详解完整后做最终页码回填与章节成品验收。
## 2026-08-09 第二章 2017 年第十题：差分方程、ROC 与稳定解

- 已从原卷核对并纳入 2017 年第十题；题干及三个小问保持原意与顺序，整体归入第二章，未拆分相互依赖的小问。
- 新增统一 MathJax 组件，给出系统函数、零点、两个实极点、因果 ROC/响应，以及包含单位圆的双边稳定响应。z 平面为真实矢量图：零点、极点、单位圆、轴箭头和标签均按实际坐标绘制，无水印或手工公式拼接。
- 实际重建第二章候选 PDF 并在 150 dpi 逐页查看第 73--75 页。为消除详解首段的无意义留白，已缩短图形画布，使零极点图续排到前页；题页保留一题一页的答题空间，详解连续两页均无重叠、裁切或公式源代码。
- 验证：新增测试先失败后通过；test_chapter_02_2017_difference_equation.py 与 test_build_chapter_02_mathjax_handout.py 共 3 passed，仅有既有 .pytest_cache 权限警告。
- 下一步：从已审核题库选择下一道整体归属于第二章的真题，重复“原卷核对 → 失败回归 → MathJax/SVG 实现 → 候选 PDF 视觉检查 → 独立提交”的流程。
## 2026-08-09 第二章 2015 年第五题编号与年份核对修正

- 原卷核对发现：已有“单位脉冲响应 \(h[n]=\delta(n)-0.98\delta(n-6)\)”题被错误标成 2013 年第六题；实际为 2015 年第五题。已恢复原题年份、题号和题干，未改动题目内容。
- 新增失败回归后修复并通过；第二章候选 PDF 已以 150 dpi 检查第 39--40 页，独立题页与详解起始页均清晰，无公式源码、裁切或重叠。
- 下一步：继续从第二章题库中选择尚未纳入的整体题，保持逐题核对与可视化验收。
## 2026-08-09 第二章 2015 年第四题来源保真修正

- 依据原卷修正两级延时反馈系统题：由误标的 2013 年第五题恢复为 2015 年第四题；三小问、系统函数推导和稳定非因果响应均完整保留。
- 结构图采用无水印 SVG，A4 150 dpi 实际检查确认求和器、延时器、\(5/2\)、\(-1\) 两支反馈及箭头均无重叠或裁切。
- 验证：来源保真、组件结构和第二章装配回归共 6 passed；仅有既有 pytest 缓存权限警告。
- 下一步：持续核查第二章已有训练条目的来源信息及重复项，再补入缺失整体题。

## 2026-08-09 第二章 2015 年第七题旧副本清理

- 原卷第 30 页确认零极点题为 2015 年第七题；删除 batch_four 中错误标为“2013 年第八题”的重复题干、图形与详解，batch_four 仅保留 2015 年第四题。
- 整书仍有“分章强化训练”与专用补充题两个既有第七题入口；为避免误删训练或详解，本次仅清理旧批次副本，后续在全书训练结构审计中归并。
- 6 项相关测试通过（pytest 缓存权限警告除外）；重建 73 页第二章 PDF，150 dpi 复核第 42 页的 2015 年第四题图形和公式无重叠、裁切或乱码。

## 2026-08-09 第二章 2013 年第二组第 5 小题题号保真

- 按题库的人工审阅定位，将双边序列 \(z\) 变换题从易误解的“第五题”恢复为“第二组第 5 小题”；原题序列、条件和求解目标未改动。
- batch-five 与第二章整书装配共 5 项测试通过（仅既有 pytest 缓存权限警告）；重建第二章 PDF 并以 150 dpi 检查第 44 页，组别题号、分式、上下标和 \(z\) 变换均清晰，无重叠或裁切。

## 2026-08-09 第二章 2013、2015 同题年份归并

- 原卷核对确认：2013 年第六题与 2015 年第五题的 FIR 零极点题题干完全相同。讲义中合并为一个条目，保留题干和详解，仅将来源标签明确为“2013 年第六题／2015 年第五题”，避免重复印题而遗漏考查年份。
- 按测试先失败、后修复的流程完成；相关回归 `4 passed`，仅有既有 pytest 缓存权限警告。
- 已重建第二章候选 PDF，并以 150 dpi 实际检查第 39--40 页：题目页保持一题一页的允许书写空间，详解标题、理想滤波器图、分段公式和新增年份标识均无裁切、重叠或原始公式源码。

## 2026-08-09 第二章 2013、2015 同题反馈系统年份归并

- 原卷核对确认：2013 年第五题与 2015 年第四题的二级延时反馈系统题完全相同。讲义保留一个完整题目、结构图和三小问，并在题目、图形辅助标签和详解中同步保留“2013 年第五题／2015 年第四题”。
- 相关回归 `5 passed`，仅有既有 pytest 缓存权限警告。
- 已重建第二章候选 PDF，并以 150 dpi 检查第 42--43 页：求和器、两级延时、两条反馈支路、增益块与箭头均不重叠；三个小问、分式、ROC 和稳定非因果响应均完整清晰。

## 2026-08-09 第二章零极点题重复入口审计

- 已确认最终第二章同时装配了同一零极点题的“分章强化训练”与“补充真题 batch-fourteen”两份入口；原卷还存在相同的 2013 年第八题。直接移除后者会撤掉一个既有训练/详解入口，自动化保护已拒绝该删除操作。
- 当前未删除任何讲义内容，也已恢复用于验证的临时测试改动。待用户明确授权“从最终装配中移除 batch-fourteen、保留分章强化训练并合并标注为 2013 年第八题／2015 年第七题”后再继续。

## 2026-08-09 第二章 2016 年变换关系简答题

- 已按原卷 PDF 文件页 33 纳入“二、简答题第 1 小题：说明 FT、LT、ZT 的关系；”。题干保真；无须复用带水印的原卷图形。
- 详解使用完整 MathJax LaTeX，给出 \(X(j\Omega)=X(s)|_{s=j\Omega}\)、\(X(e^{j\omega})=X(z)|_{z=e^{j\omega}}\)、两个收敛域前提，以及采样映射 \(z=e^{sT}\)。
- 新增组件与第二章装配回归 `4 passed`（仅既有 pytest 缓存权限警告）；候选 PDF 为 75 页，150 dpi 检查第 74--75 页无公式源码、裁切、重叠或乱码。

## 2026-08-09 第二章 2017 年拉氏—z 映射简答题

- 已按原卷 PDF 文件页 36 纳入“二、简答题第 1 小题：在信号与系统里面，拉氏变换和 z 变换的对应关系是怎样的？”。该题与 2016 年 FT/LT/ZT 关系题关注点不同，作为独立题目保留。
- 详解用完整 MathJax LaTeX 给出 \(z=e^{sT}\)、左右半平面与单位圆内外的三种对应、极点映射与稳定性解释，并标明离散化方法及收敛域前提。
- 新增组件和第二章装配回归 `4 passed`（仅既有 pytest 缓存权限警告）；候选 PDF 已为 77 页，150 dpi 检查第 76--77 页无公式源码、裁切、重叠或乱码。
## 2026-08-09 第一章普通正文安全续排

- 根因：总装订器将普通正文组件页当作不可拆分块，下一整块放不下时直接换页，造成可避免的大面积空白。
- 修复：普通正文页仅在 PDF 真实矢量对象的安全间隙处分段续排；公式、图形和真题训练页不切开。第 6 页已自然承接“离散时间信号的基本运算”，候选总讲义由 60 页压缩为 58 页。
- 验证：先失败后通过的回归测试；第一章 MathJax 组件与总装订共 9 项测试通过（仅 `.pytest_cache` 权限警告）；58 页候选 PDF 已全部渲染并视觉检查，重点 180 dpi 检查第 6、7 页无重叠、裁切或无意义留白。
- 下一步：继续第一章真题覆盖、书末详解整合和最终页码回填；临时 QA 产物不提交。
## 2026-08-09 第一章下一道真题训练

- 开始审计第一章训练与原题卷：仅选取尚未纳入、完整归属第一章且与既有条目不重复的题目；题干保持原样，详解使用完整 MathJax。

## 2026-08-09 第一章 2004 年矩形脉冲自卷积真题

- 已按原卷第 11 页将 `已知 \(f(t)=u(t)-u(t-2)\)，求 \(f(t)*f(t)\)的表达式及波形。` 纳入第一章补充真题；题干逐字保留，题目页维持一题一页。
- 书末详解采用完整 MathJax `cases` 公式，并加入由真实坐标驱动的 SVG 三角波形。180 dpi 实际预览确认大括号覆盖四行，坐标轴、刻度、标签、峰值和正文均无重叠或裁切。
- 验证：补题前的回归按预期失败；实现后 `test_build_chapter_01_supplemental_mathjax_component.py` 为 `2 passed`，浏览器 MathJax DOM 完整。最终全书合成时再统一回填 `详解见 P.____` 的实际页码。
- 下一步：仅提交本题脚本、测试及记录，推送后继续审计下一道完整归属第一章的真题。

## 2026-08-09 第一章 2005 年时间尺度变换系统性质真题

- 已按原卷第 14 页纳入 `信号 \(y(t)=x(3t)\)是否线性，是否时变，为什么？`；题干未改写，独立置于训练页。
- 详解以完整 MathJax `aligned` 给出线性验证，并比较 \(\mathcal{T}\{x(t-t_0)\}=x(3t-t_0)\) 与 \(y(t-t_0)=x\left(3t-3t_0\right)\)，结论为线性、时变。
- 验证：先失败后通过的局部回归 `3 passed`。初次 180 dpi 预览发现答案续页稀疏，修正固定页后断为桥接续排后，第 6 页由后续重建详解自然填充；第 5--6 页无大块无意义留白、公式裁切或重叠。临时 PDF/PNG 不提交。
- 使用独立临时组件根目录重建第一章候选总 PDF（63 页），不覆盖工作区现有成品。新增 2004/2005 训练页分别位于第 48、49 页，书末详解位于第 61--62 页；180 dpi 总装订抽查确认页眉页脚、题干、公式、分段函数、波形和续排均无重叠或裁切。

## 2026-08-09 第一章 2005 年冲激序列与单位阶跃卷积真题

- 按原卷第 14 页纳入“求两信号的卷积”及 \(x(t)=2\delta(t-1)-3\delta(t-2)-2\delta(t-5)\)、\(h(t)=u(-2-t)\)；题干未改写。
- 详解使用 MathJax `aligned` 推出 \(2u(-1-t)-3u(-t)-2u(3-t)\)，再用 `cases` 给出四段结果。
- 发现 `<` 被 HTML 误解析导致分段公式源码泄露后，已以 HTML 安全实体修复。局部回归 `4 passed`；180 dpi 预览确认公式、大括号、条件与后续续排均无重叠、裁切或源码泄露。
## 2026-08-09：第一章补充真题——2005 年冲激序列卷积

- 已纳入原题干“求两信号的卷积”，题干公式保持完整 MathJax 渲染，训练页位于临时整章候选 PDF 第 50 页。
- 书末详解以冲激移位筛选、线性叠加和四段 `cases` 结果给出；候选 PDF 第 63 页经 180 dpi 视觉检查，公式、大括号和续排无裁切、重叠或 TeX 源码泄漏。
- 临时候选文件仅用于验证，未覆盖现有正式输出；最终答案页码仍在全书定稿后回填。

## 2026-08-09：第一章补充真题——2005 年二阶差分方程

- 已按原题两问纳入由单位阶跃响应 \(y(n)=[2^n+3(5)^n+10]u(n)\) 反求二阶差分方程、再对 \(f(n)=3u(n)+3u(n-7)\) 求响应的真题；题干未改写。
- 详解以完整 MathJax 推导 \(H(z)\)，给出二阶差分方程及移位阶跃响应；组件回归为 `5 passed`。
- 临时整章候选 PDF 共 66 页：训练页为第 51 页，书末详解为第 64--65 页；180 dpi 检查确认无公式裁切、重叠或源码泄漏。候选产物未覆盖正式输出，答案页码待全书定稿回填。

## 2026-08-09：第一章重点训练——2006 年离散系统结构

- 已按已审题库完整纳入第七题，保留原题三问；重绘无水印 SVG 系统框图，使用标准加法器、增益块、`z⁻¹` 延时器、分支点及带箭头的正交连线。
- 由图中真实连接关系写出状态方程，再得到传递函数、差分方程和单位阶跃响应；补充真题组件回归为 `6 passed`。
- 临时整章候选 PDF 共 68 页：训练页为第 52 页，书末详解为第 66--67 页；180 dpi 检查确认图形、公式、标签和续排均无水印、裁切、重叠或源码泄漏。候选产物未覆盖正式输出，答案页码待全书定稿回填。

## 2026-08-09：第一章 2006 年重点训练结构纠偏与去重

- 审计确认 2006 年第七题本来就是本章三道重点训练之一；此前补充组件的加入会造成重复，而旧重点训练结构图的信号连接及详解传递函数均与源题不符。
- 已把按源图核对的教材级 SVG、原题三问和 MathJax 详解迁入唯一的重点训练入口：\(H(z)=(0.35+0.175z^{-1}-0.03z^{-2})/(1-0.4z^{-2})\)，并同步给出相应差分方程和单位阶跃响应；补充组件中已移除该重复题及其详解。
- 验证：重点训练、重点训练详解与补充题组件共 `11 passed`（仅既有 pytest 缓存权限警告）。临时 180 dpi 预览确认第 2 页训练图与第 2 页详解无水印、重叠、裁切或公式源码泄露；未覆盖正式成品 PDF。

## 2026-08-09：第一章补充真题范围纠偏

- 以已完成的 exam_question_review_2004.json、exam_question_review_2005.json 为权威依据复核：此前临时加入的 2004 年矩形脉冲自卷积、2005 年时间尺度性质与冲激卷积均为连续时间内容，不纳入本 DSP 讲义；已从读者题页和书末详解移除。
- 保留审核明确归属第一章的 2005 年第七题，并将 2005-q七-whole 同步写入第一章补充题内容模型，消除“脚本已有、审计模型缺失”的不一致。
- 验证：范围纠偏后的组件、重点训练与详解回归 `9 passed`；临时补充题 PDF 为 9 道题、6 页详解，180 dpi 检查保留的 2005 年第七题与 2002 年采样频谱详解均无大面积异常空页、重叠、裁切或公式源码泄露。临时产物未覆盖正式成品。

## 2026-08-09：第一章补充真题——2004 年第八题

- 已按人工依赖审查的 `2004-q八-whole` 将整题纳入第一章补充真题：单位阶跃输入下的离散 LTI 响应，求单位冲激响应，并判定稳定性和因果性。题干保留原有两问和数学内容。
- 详解将题给输出明确为单位阶跃响应 \(s[n]\)，用 \(h[n]=s[n]-s[n-1]\) 得到冲激响应；分别以 \(n<0\) 时响应为零和绝对可和性证明因果、稳定。所有公式由完整 MathJax LaTeX 渲染。
- 验证：补充真题组件回归 `6 passed`；`git diff --check` 通过。唯一警告为既有 `.pytest_cache` 权限限制，不影响生成或成品。已建立独立任务记录；临时 PDF 视觉检查显示题干、详解与公式均完整清晰，题目页下部留白仅作单题书写区。

## 2026-08-09：第一章补充真题——2007 年填空第 3 小题

- 已按人工依赖审查的 `2007-qintro-p3` 将“已知 \(x(t)=\cos(1000\pi t)\sin(400t)/(\pi t)\)，对其进行时域抽样；”原题纳入第一章补充真题，不加入原卷水印或非题目内容。
- 详解以完整 MathJax 公式给出低通核频谱、调制后的带通频谱、冲激串采样关系及 \(\Omega_s\geq2(1000\pi+400)\) 的无失真恢复条件；分段频谱使用标准 `cases` 排版。
- 验证：新增测试先失败、实现后补充真题组件回归 `7 passed`；临时 PDF 已以 160 dpi 渲染检查，题目页的留白只作单题书写区，详解页中的分段公式、上下标、分数和正文均无裁切或重叠。

## 2026-08-10：第一章补充真题——2007 年第六题

- 已按人工审查的 `2007-q六-whole` 收录完整原题两问，并将原卷结构图重绘为无水印的 `2007-system-structure` 矢量图：保留两级延时、0.5 与 0.4 反馈、0.2 前向支路、三处求和及所有信号方向。
- 详解由图中节点关系推得 \(H(z)=(1+0.3z^{-1}+0.1z^{-2})/(1+0.1z^{-1}-0.2z^{-2})\)，给出相应差分方程与因果单位冲激响应；所有公式使用完整 MathJax LaTeX。
- 验证：新增测试先失败、实现后补充真题组件回归 `8 passed`；`git diff --check` 通过。临时 PDF 以 160 dpi 检查，结构图、求和符号、箭头、公式和正文均无水印、重叠或裁切。

## 2026-08-10：第一章补充真题——2007 年第九题第 1 小题

- 已按审定的 `2007-q九-p1` 收录有限长离散卷积支持区间填空题，保留题干中两个严格区间条件；不使用原卷水印或非题目元素。
- 详解从 \(N_3<m<N_4\) 与 \(N_1<n-m<N_2\) 的乘积非零条件推出 \(N_1+N_3<n<N_2+N_4\)，不以只给结论代替推导。
- 验证：补充真题组件回归 `9 passed`，`git diff --check` 通过；160 dpi 临时 PDF 视觉复核确认题目页的空白仅作单题书写区，详解页无公式源码、裁切或重叠。

## 2026-08-10：第一章补充真题——2013 年第一组第 3 小题

- 已按审定的 `2013-qintro-p3` 收录原题：\(x(t)\) 截止角频率为 \(1500\pi\,\mathrm{rad}/\mathrm{s}\)，对 \(y(t)=x(t)\cdot x(2t)\) 采样并求最大采样间隔。
- 详解严格保留 \(\pi\) 单位：时缩放后带宽为 \(3000\pi\)，频域支撑相加得 \(4500\pi\,\mathrm{rad}/\mathrm{s}\)，最终 \(T_{\max}=1/4500\,\mathrm{s}\)。
- 验证：补充真题组件回归 `10 passed`，`git diff --check` 通过；临时 A4 PDF 以 160 dpi 视觉复核，题干、单位、公式、分数和上下标均无裁切、重叠或源码泄露。

## 2026-08-10：第一章补充真题——2013 年离散 LTI 系统因果性与稳定性

- 已按审定的 `2013-qintro-p2-3` 收录原题“设离散时间 LTI 系统的冲激响应 \(h(n)=a^n u[n]\)，试判断系统的因果稳定性。”；题干未改写。
- 详解使用完整 MathJax 推导 \(\sum_{n=-\infty}^{\infty}|h[n]|=\sum_{n=0}^{\infty}|a|^n\)，明确系统恒因果、且仅当 \(|a|<1\) 时 BIBO 稳定。
- 同步修复书末详解组件：题页保留一题一页书写区，详解页自然续排，避免短题详解被强制留成大面积空白；续标题不再在同页重复出现。
- 验证：回归 `12 passed`，唯一警告为既有 pytest 缓存目录权限；临时 A4 PDF 以 180 dpi 视觉复核，公式、标题、分页和续排均无裁切、重叠、源码泄露或无意义整页留白。

## 2026-08-10：第一章补充真题——2014 年有限长离散序列卷积

- 已按审定的 `2014-qintro-p5` 收录原题：\(f_1[n]=2^n\{u[n]-u[n-3]\}\)、\(f_2[n]=2\delta[n+1]+5\delta[n-1]\) 的卷积；题干未改写。
- 详解基于冲激移位性质推得 \(y[n]=2f_1[n+1]+5f_1[n-1]\)，并合并为五个冲激项的结果，避免只列答案而缺少卷积依据。
- 验证：回归 `13 passed`，`git diff --check` 通过（仅行尾转换提示）；临时 A4 PDF 以 180 dpi 检查，题干、公式和跨页续排均无裁切、重叠或源码泄露。

## 2026-08-10：第一章补充真题——2014 年时扩信号乘积的采样间隔

- 已按审定的 `2014-qintro-p6` 收录原题：对 \(x(t/4)\cdot x(t/2)\) 采样，求频谱不混叠的最大采样时间间隔；题干未改写。
- 详解由两个时扩信号的最高频率 \(\omega_m/4\)、\(\omega_m/2\) 推出乘积信号最高频率为 \(3\omega_m/4\)，再由奈奎斯特条件得到 \(T_{\max}=4\pi/(3\omega_m)\)。
- 验证：回归 `14 passed`，`git diff --check` 通过（仅行尾转换提示）；临时 A4 PDF 以 180 dpi 复核，题干、分数、上下标、不等式及自然续排均无裁切、重叠或源码泄露。

## 2026-08-10：第一章补充真题——2015 年数字信号处理的基本运算

- 已按审定的 `2015-qintro-p7` 收录原题“数字信号处理的三种基本运算是______、______、______。”；题干未改写。
- 详解明确答案为加法、乘法与延时（单位延时），并说明它们是离散系统框图的基本运算单元。
- 验证：回归 `15 passed`，`git diff --check` 通过（仅行尾转换提示）；临时 A4 PDF 以 180 dpi 检查，题干、正文和自然续排均无重叠、裁切或源码泄露。

## 2026-08-09：第一章补充真题——2004 年第八题

- 已按人工依赖审查的 `2004-q八-whole` 将整题纳入第一章补充真题：单位阶跃输入下的离散 LTI 响应，求单位冲激响应，并判定稳定性和因果性。题干保留原有两问和数学内容。
- 详解将题给输出明确为单位阶跃响应 \(s[n]\)，用 \(h[n]=s[n]-s[n-1]\) 得到冲激响应；分别以 \(n<0\) 时响应为零和绝对可和性证明因果、稳定。所有公式由完整 MathJax LaTeX 渲染。
- 验证：补充真题组件回归 `6 passed`；`git diff --check` 通过。唯一警告为既有 `.pytest_cache` 权限限制，不影响生成或成品。已建立独立任务记录；临时 PDF 视觉检查显示题干、详解与公式均完整清晰，题目页下部留白仅作单题书写区。

## 2026-08-10：第一章补充真题——2016 年两离散序列卷积

- 已按原卷 PDF 第 33 页收录计算题第 2 小题“系统如图所示，求 \(f(n)=f_1(n)*f_2(n)\)；”；题干未改写。原卷图有水印，因此根据原样值与索引重绘为两幅干净的标准 stem 图。
- 详解给出全部非零样值、\(-2\leq n\leq5\) 的卷积支撑区间、逐点卷积结果与冲激序列形式；图形由真实 SVG 坐标生成，零刻度、纵轴与负样本杆已主动错开。
- 验证：回归 `16 passed`，仅既有 pytest 缓存权限警告；临时 A4 PDF 在 180 dpi 下检查题目页和两页详解，坐标轴、箭头、标签、样值、公式和自然续排均无水印、重叠、裁切或公式源代码泄露。

## 2026-08-10：第一章补充真题——2016 年由单位阶跃响应求有限序列输出

- 已按原卷 PDF 第 34 页收录 DSP 第 1 小题：
  \(\mathcal{T}\{u(n)\}=g(n)\)，那么 \(x(n)=(n+1)R_3(n)\)，则 \(y(n)=?\)。题干使用完整 MathJax 渲染，未改写其知识条件。
- 详解先由 \(R_3[n]=u[n]-u[n-3]\) 将输入改写为四个移位阶跃的线性组合，再由线性时不变性得到 \(y[n]=g[n]+g[n-1]+g[n-2]-3g[n-3]\)。
- 验证：修正一处测试对 `aligned` 等号标记的错误预期后，补充真题组件回归 `17 passed`；临时 A4 PDF 在 180 dpi 下检查题目页和详解跨页，无公式源代码、裁切、重叠或无意义留白。
## 2026-08-10：第一章补充 2017 年系统性质真题（已视觉验收）

- 已从 2017 年真题第 36 页核对并加入“线性、时不变性、因果性”题目与逐项详解；题库记录为 2017-qintro-p2-discrete。
- 已完成 Python/JSON 静态语法检查，确认无控制字符及裸公式；git diff --check 通过（仅有既有 CRLF 提示）。
- 已完成不落盘的内存生成核验：题页与详解页都包含 MathJax、原题三条系统表达式和最终判定。
- 定向 pytest 通过（19 passed，只有 pytest 缓存权限警告）；A4 题页与详解页已在 180 dpi 下检查，无裁切、重叠、公式源码泄漏。
## 2026-08-10：第一章补充 2019 年 Sa(2t) 抽样真题（已视觉验收）

- 已完整加入 2019 年第七题四个小问：奈奎斯特频率、冲激采样、采样频谱与采样保持恢复。
- 新增波形与频谱均由真实函数、采样点和 \(\omega_s=12\) 的周期副本程序化生成；未复用带水印页面。
- 定向 pytest 通过（19 passed，只有 pytest 缓存权限警告）；已对 180 dpi A4 题页与详解页进行视觉检查，曲线、冲激箭头、频谱副本、坐标轴和标签均无裁切或重叠。
- 采样保持恢复已补足零阶保持器 \(H_0(j\omega)\) 的幅相效应，以及补偿并抑制频谱副本的 \(H_r(j\omega)\)；更新后再次通过 `19 passed`，并在 180 dpi A4 第 10 页确认分段公式、分数、上下标、图形和正文均无裁切、重叠或公式源码泄漏。

## 2026-08-10：第一章补充 2019 年离散系统性质题

- 题库审查发现 `2019-qintro-p3` 先前遗漏；现已按第一章归属加入。题面左端的输出符号为客观笔误，按标准系统记号修正为 \(y[n]\)，内部模型保留修正说明。
- 详解完整给出线性、时变性和因果性判别。定向回归 `20 passed`；独立 A4 PDF 在 180 dpi 下复核新增题页第 18 页与详解第 8–9 页，未见公式源码、裁切或重叠。
- 未覆盖已有未提交的正式第一章 PDF 与组件输出；正式合入需用户明确许可。

## 2026-08-10：第一章补充 2025 年离散系统可逆性题

- 已从原卷第 56 页核对并收录填空第 4 小题；原题末项把求和变量误印为 \(x(n)\)，现按求和下标校正为 \(x[i]\)，内部内容模型保留该客观修正记录，读者页不显示来源性说明。
- 详解以非零零空间输入 \(x_0[n]=\delta[n]-a\delta[n-1]\) 证明输出为零，得到系统不可逆、无逆系统的结论。
- 定向回归 `21 passed`（仅既有 pytest 缓存权限警告）；JSON 和 `git diff --check` 通过。临时 A4 预览在 180 dpi 下确认题页书写区与书末详解均无裁切、重叠或公式源码泄漏。
- 临时 QA 产物未覆盖正式 PDF；后续仍需用户明确授权，才能重建并覆盖正式第一章成品及纳入该补题组件。

## 2026-08-10：第一章补充 2021 年离散序列卷积题

- 已逐项核对原卷第 43 页的两组离散样值，并用真实 SVG 坐标重绘无水印标准 stem 图；详解给出完整卷积样值、结果 stem 图及 \(n=2\) 的逐项计算。
- 视觉复核发现结果图的 \(n=0\) 样本杆初始与纵轴重叠，已将纵轴左移并加入回归断言，避免同类问题回归。
- 定向回归 `23 passed`（仅既有 pytest 缓存权限警告）；在 180 dpi 临时 A4 预览中确认题图、结果图、标签和公式无重叠、裁切或水印。临时预览未覆盖正式 PDF。

## 2026-08-10：第一章补充 2022 年采样频谱题

- 已按原卷第 47 页完整收录第五题第 2 小题的三个采样频谱小问；FFT 的第 3 小题仍按依赖归属留给第四章。
- 详解以完整 MathJax 给出 \(\omega_s\geq8\omega_0\)、原信号冲激频谱、采样频谱复制和两种采样周期下的混叠判断。
- 首轮 PDF 检查发现硬展开所有冲激副本会产生横向滚动，已改为引用已列 \(X(j\Omega)\) 的等价紧凑频谱式；`25 passed`，临时 A4 复查无滚动条、裁切、重叠或公式源码。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充 2016 年有限序列的偶、奇分解

- 已按原卷第 34 页收录 \(x(n)=R_6(n)\) 的偶分量和 \(x_o(n)R_8(n)\) 两问；同题的 DFT 小问未混入第一章。
- 详解使用完整 MathJax `aligned` 与 `cases`，给出定义、\(R_8[n]\) 的截取区间和最终乘积结果。
- 定向回归 `26 passed`，JSON 与 `git diff --check` 通过；题页与详解页经 180 dpi 临时 A4 检查，公式、自动大括号、下标和分页均无裁切、重叠或公式源码。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充 2021 年抽样频谱与低通恢复

- 已按原卷第 43 页完整收录第二题的抽样频谱及无失真恢复两个小问；不复制带水印的原卷页面。
- 详解给出积化和差分解、\(\Omega_s=500\pi\) 下的冲激抽样频谱、\(220\pi\leq\Omega_c\leq280\pi\) 的低通截止范围与 \(T_s\) 通带补偿增益。
- 回归 `27 passed`，JSON 与 `git diff --check` 通过；临时 A4 PDF 的第 16--17 页以 160 dpi 检查无横向溢出、裁切、重叠或公式源码。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充真题——2022 年周期理想采样的拉普拉斯变换

- 已按原卷第 46 页收录“\(f(t)=\mathrm{e}^{-5t}u(t)\) 经周期 \(T\) 的理想采样后，求 \(F_s(s)\)”；题干未改写。
- 详解用完整 MathJax `aligned` 推导采样冲激串、几何级数和收敛域：\(F_s(s)=1/[1-\mathrm{e}^{-(s+5)T}]\)，\(\operatorname{Re}\{s\}>-5\)。
- 回归 `24 passed`，JSON 与 `git diff --check` 均通过（仅既有缓存/CRLF 警告）；临时 A4 PDF 的题页及详解页经 180 dpi 检查，公式、分页和续排均无裁切、重叠或源代码泄漏。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充真题——2022 年 A/D、数字低通与 D/A 串联

- 已按原卷第 48 页完整收录第七题的两问：连续信号经 \(T\) 周期采样、A/D、截止频率 \(\omega_c=\pi/4\) 的数字低通和 D/A 后，分别在 \(T=0.05\,\mathrm{ms}\)、\(0.25\,\mathrm{ms}\) 下绘制输出频谱。
- 结构图与输入频谱均按实际链路和坐标数据重绘为无水印 SVG；输出截止频率统一由 \(f_c=(\omega_c/2\pi)f_s=f_s/8\) 计算。\(0.25\,\mathrm{ms}\) 的输出谱在 \(\pm0.5\,\mathrm{kHz}\) 垂直截断，同时保留三角谱在截止点处的 \(0.5\) 幅值，不误画为零。
- 回归 `28 passed`（仅既有 pytest 缓存权限警告）；临时 A4 PDF 的第 26 张题目页和第 17--18 张详解页经 180 dpi 视觉检查，链路箭头、坐标轴、`cases` 公式、标签与频谱均无裁切、重叠或公式源码泄漏。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充真题——2023 年单位阶跃响应求单位脉冲响应

- 已按原卷第 50 页完整收录 DSP 简答第 1 小题，题干保留 \(x(n)=(1/2)^n u(n)\)、输出 \(g(n)\) 与“求系统单位脉冲 \(h(n)\)”的原有条件。
- 详解以完整 MathJax 推出 \(X(z)=1/(1-\frac12z^{-1})\)，再由 \(G(z)=H(z)X(z)\) 得 \(h[n]=g[n]-\frac12g[n-1]\)。
- 回归 `29 passed`（仅既有 pytest 缓存权限警告）；临时 A4 PDF 的第 27 张题页及第 18--19 张详解页在 180 dpi 下检查无裁切、重叠或公式源码泄漏。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充真题——2023 年正弦采样序列的 DTFT

- 已按原卷第 50--51 页收录 DSP 简答第 2 小题：对 \(x_c(t)=\cos(100\pi t)\) 理想采样，分别在 \(f_s=100\,\mathrm{Hz}\)、\(150\,\mathrm{Hz}\) 下绘制 \([0,2\pi]\) 内的 \(X(\mathrm{e}^{j\omega})\)。
- 详解使用完整 MathJax 给出余弦序列的 DTFT 冲激对；\(f_s=100\,\mathrm{Hz}\) 的两组冲激在 \(\pi\) 合并为幅值 \(2\pi\)，\(f_s=150\,\mathrm{Hz}\) 的两根谱线位于 \(2\pi/3\)、\(4\pi/3\)，幅值均为 \(\pi\)。
- 两幅频谱均重绘为真实坐标 SVG 离散谱，坐标轴、箭头、刻度与标签清晰；临时 A4 PDF 的第 28 张题页和第 19--20 张详解页经 180 dpi 验收，无重叠、裁切或公式源码。定向回归 `30 passed`（仅既有 pytest 缓存权限警告）。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充真题——2023 年正弦信号采样频率与周期

- 已从原卷第 50 页的半波整流题中按依赖审查独立收录第 3 小问：对 \(f(t)=A\sin(\omega_0t)\) 采样，给出满足采样定理的频率和周期；前两小问的连续时间内容未混入第一章。
- 详解用完整 MathJax 同时给出普通频率、角频率和周期条件：\(f_s\geq\omega_0/\pi\)、\(\Omega_s\geq2\omega_0\)、\(T_s\leq\pi/\omega_0\)。
- 定向回归 `31 passed`（仅既有 pytest 缓存权限警告）；临时 A4 PDF 的第 29 张题页及第 20--21 张详解页在 180 dpi 下无重叠、裁切或公式源码。正式 PDF 未覆盖，未提交或推送。

## 2026-08-10：第一章补充真题——2024 年矩形脉冲采样与 DTFT

- 已按原卷第 52 页纳入“矩形脉冲信号 \(f(t)\) 如图所示，以采样角频率 \(\Omega_s=2\pi\,\mathrm{rad}\,\mathrm{s}^{-1}\) 对其采样”的完整题干；“能否恢复原信号”和“计算 DTFT”两个问法均保留。
- 题图以真实 SVG 坐标重绘为无水印矩形脉冲，保留 \(-2\)、\(0\)、\(2\) 刻度、单位高度、完整坐标轴与箭头；详解用标准 MathJax `cases`、`aligned` 给出 \(T_s=1\,\mathrm{s}\)、不可无失真恢复的原因及 \(X(\mathrm{e}^{j\omega})=1+2\cos\omega+2\cos(2\omega)\)。
- 定向回归 `32 passed`（仅既有 pytest 缓存权限警告）；临时 A4 PDF 第 30 张题页与第 21 张详解页在 180 dpi 下检查，无公式源码、重叠、裁切或不合规空白。未覆盖正式 PDF，未提交或推送。

## 2026-08-10：第一章补充真题——2024 年非理想脉冲采样与恢复

- 已按原卷第 54 页收录三问：\(10^4\operatorname{Sa}(10^4t)\) 的频谱与带宽、周期 \(40\,\mu\mathrm{s}\)/脉宽 \(10\,\mu\mathrm{s}\) 矩形脉冲采样的频谱，以及结合 \(H_1(f)\) 恢复 \(f(t)\) 的系统设计；题干未改写。
- 采样脉冲、\(H_1(f)\)、基带频谱和副本频谱均以真实 SVG 坐标重绘。首轮视觉检查发现脉冲图内的周期/脉宽辅助文字彼此压住，已定位为重复标注占用同一绘图区，移除重复图内标注并保留题干数据；复渲染后无重叠，新增回归断言禁止此类标注回归。
- 详解使用 MathJax `cases` 和完整频域推导：\(B=5\,\mathrm{kHz}\)、\(C_k=\frac14\operatorname{Sa}(k/4)\)、副本间隔 \(25\,\mathrm{kHz}\)，以及经 \(2\cos(2\pi f_st)\)、\(5\,\mathrm{kHz}\) 低通和 \(2/\operatorname{Sa}(1/4)\) 增益恢复的级联结构。
- 定向回归 `34 passed`（仅既有 pytest 缓存权限警告）；临时 A4 PDF 的题页第 31 页与详解第 22--23 页经 180 dpi 检查，无公式源码、裁切、重叠或异常留白。未覆盖正式 PDF，未提交或推送。
## 2026-08-13 Difference-equation feedback port correction

- User reported that the feedback arrow in the first-order difference-equation diagram ended in empty space rather than at the delay block.
- Root cause: the vertical wire itself carried an arrowhead but did not reach the module. The structure now explicitly routes output branch -> vertical segment -> horizontal segment into the `z^{-1}` right-side port -> gain block -> negative summer input.
- Regression passed; `tmp/dsp-main-body-arrow-port-v13.pdf` has MathJax `unrendered=[]`, `pageErrors=[]`, and visual QA on page 21 confirms all arrows terminate on real ports.

# 2026-08-13 Main-body running headers and folios

- The assembled main-body PDF had correct A4 content but no final running furniture. Added a reusable PDF post-processing layer that infers each page's chapter from the actual browser-paginated page text, then overlays the fixed left header `数字信号处理讲义`, the current chapter title at upper right, and the true folio centered at the foot.
- This avoids a fixed-page-number map and avoids browser fixed-position header collisions: Chromium lays out the body first, then ReportLab/PyPDF writes only in the reserved margins.
- Visual QA: rendered page 1 and the chapter-two start page 31; header, chapter label, rule, and page number are sharp and do not overlap the body. The same stamped run contains all 90 body pages.

# 2026-08-14 Circular-index notation

- Per feedback, replaced every body occurrence of the inline `mod N` circular-index notation with the requested parenthesized index plus outer period subscript, such as `x((n-n_0))_N` and `x^*((N-n))_N`.
- Visual QA on regenerated page 49 verifies that the period subscript attaches to the complete parenthesized index. Full export reports MathJax `unrendered=[]`, `pageErrors=[]`; the assembled body HTML no longer contains `mod N` notation.

# 2026-08-14 Stem-axis clearance

- First-chapter page-2 audit found that the generic stem SVG placed the drawn y-axis exactly on a real `n=0` sample. This contradicted the established textbook-plot rule that the first stem, origin label, and y-axis must not overlap.
- The real-coordinate SVG renderer now leaves `n=0` at its exact tick coordinate but moves the visual vertical axis left by `0.32` sample interval only when a zero-index sample exists. The behavior is guarded by a regression test.
- Verification: `6 passed`; rebuilt and stamped 90-page body reports MathJax `unrendered=[]`, `pageErrors=[]`. Rendered A4 page 2 at 150 dpi confirms the axis, zero tick and impulse stem are separate and readable.

# 2026-08-14 Chapter-one pagination audit

- Audited the first chapter through page 12 after the stem correction. Page 8 had a non-chapter-end large blank after the rectangle-sequence figure, despite the next section safely fitting in the remaining space.
- Reduced only the continuation chart maximum width from 560px to 500px. This retains legible A4 graphics but lets the real-exponential section heading, formula and lead paragraph flow onto page 8; the complete paired stem figures then begin page 9.
- Verification: targeted assembly/component tests `2 passed`; rebuilt body reports MathJax `unrendered=[]`, `pageErrors=[]`. Rendered stamped pages 8--9 at 150 dpi show natural content flow without clipping, overlap, or raw formula source.

# 2026-08-14 Zero-order-hold assembled SVG repair

- Chapter-one page-28 audit found that the zero-order-hold component generated a correct data-driven step path but its `.hold` style was absent from the shared full-book stylesheet. SVG default fill therefore produced an opaque black area instead of a line waveform.
- Added the shared `.signal-svg .hold` rule: unfilled teal stroke, 3pt-equivalent line width and rounded joins. The waveform now remains a genuine staircase with separate sampled stems, points and time ticks.
- Verification: targeted component/full-body tests `2 passed`; rebuilt/stamped PDF reports MathJax `unrendered=[]`, `pageErrors=[]`. Rendered A4 page 28 at 150 dpi confirms the corrected textbook-style graphic.

# 2026-08-14 Chapter-four visual audit and pagination repair

- Inspected all fourth-chapter main-body pages (56--63). Both DIT/DIF butterfly flows preserve their full technical content and labels; no crop, overlap, raw MathJax source, or formula error was found.
- Converted the direct-DFT computation-count block on page 56 to the shared ruled-table class, restoring every horizontal and vertical grid line.
- Allowed the DIF butterfly figure to continue naturally into the remaining space on page 60 rather than forcing the whole figure to the following page. The continued figure and its caption remain complete and readable on page 61, eliminating the otherwise non-chapter-end blank area.
- Exporter diagnosis: a blocked CDN MathJax defer script prevented the local PDF helper from reaching `networkidle`. The local QA helper now aborts that CDN request before navigation and loads the local MathJax copy, so PDF QA completes normally. Verification: targeted tests `4 passed`; export reports `math=2812`, `unrendered=[]`, `pageErrors=[]`; rendered pages 56, 60 and 61 visually checked.

# 2026-08-14 Circular-index notation verification

- Reader-facing circular indices must never use `mod N`, `\bmod N` or `\operatorname{mod}`. The period is written as the subscript of the complete parenthesized index: `x\left((n+m)\right)_N`; for the conjugate-symmetry form, `x^*\left((N-n)\right)_N`.
- Regenerated the chapter-three/four output and the chapter-three LSI output to eliminate stale cached renderings. Active source and regenerated outputs contain no old notation.
- Visual A4 QA on page 49 confirms the outer period subscript attaches to the full index parentheses and remains readable without overlap.

# 2026-08-14 Chapter-two visual audit

- Rendered and inspected every main-body page of chapter two (pages 31--43) at A4 scale. The s--z mapping, unit circle, pole-zero distance figure, all standard LaTeX formulas, limiter cases and typography are clear with no overlap, crop, black fill or missing technical graphic.
- Page 43 is the chapter-end page, so its remaining writing space is intentional. No source change was needed; the audit itself is recorded and committed as a verified milestone.

# 2026-08-14 Chapter-three visual audit

- Rendered and inspected pages 44--55 at A4 scale. The Fourier-domain mapping table retains visible rows and columns; DFS/DFT, frequency-sampling and all long formulas are whole MathJax expressions; circular indices use the confirmed outer-subscript notation.
- No clipping, collision, raw formula source, missing graphic or non-chapter-end large blank was found. Page 55 is intentionally the chapter-end page.

# 2026-08-13 Ch4/Ch5 source-figure crop repair

- Replaced the unsafe IIR second-order cascade course-slide crop with a full formal SVG. Tightened the DIT butterfly crop to exclude residual slide banner while retaining all technical content.
- Verification: chapter-five/crop tests `6 passed`; rebuilt `tmp/dsp-main-body-crop-qa-v2.pdf` with MathJax `unrendered=[]`, `pageErrors=[]`; rendered affected A4 page and visually verified no cut technical content or course-slide shell.
- Next: audit remaining source-derived figures in the main body using the same per-image rule, then continue the main body before frozen training/appendix work.
# 2026-08-13 DTMF summing-node port correction

- Corrected the DTMF parallel resonator diagram: each branch arrow now terminates on the corresponding upper/lower input port of the summing node, rather than ending in empty space near it.
- Rebuilt `tmp/dsp-main-body-crop-qa-v6.pdf`; visual QA on page 67 confirms the two inputs, summer, output arrow, and labels are unambiguous. MathJax: `unrendered=[]`, `pageErrors=[]`.
- Added structural regression assertions for the exact two branch-to-summer paths and widened the input label allocation to prevent its formula from being truncated.
# 2026-08-13 Ch4 FFT crop frame cleanup

- Tightened DIT/DIF FFT source figure boxes to remove remaining green course-slide borders while preserving every technical input/output, butterfly path, node, and twiddle-factor label.
- Regression: main-body, chapter-five, and crop suites `4 passed`; both output figures inspected directly before commit.

# 2026-08-13 Ch8 vector figure audit

- Replaced all four retained chapter-eight course-slide crops with clean, self-contained SVG teaching figures: twofold-decimation spectrum transformation, anti-alias decimation, interpolation, and rational-rate conversion.
- Removed slide frames, background texture, institution marks, and teaching callouts; arrows terminate on actual module boundaries. Corrected a first-pass lower spectrum overflow before acceptance.
- Verification: main-body/chapter-eight/crop suites `3 passed`; `tmp/ch08-diagram-qa-v2.pdf` rendered with MathJax `unrendered=[]`, `pageErrors=[]`, and its affected pages were visually checked at A4 resolution.

# 2026-08-13 Main-body multirate SVG assembly repair

- Root cause: chapter-eight vector figures were styled correctly in their standalone component, but the full-book assembler did not carry the component-local `.multirate-svg` rules into the shared document stylesheet. The spectrum paths therefore inherited SVG defaults and rendered as black filled areas in the assembled PDF.
- Repair: added the complete multirate SVG style family to the full-book stylesheet and a regression assertion that verifies both spectrum stroke rules exist in assembled HTML.
- Verification: targeted regression suite `2 passed`; rebuilt `tmp/dsp-main-body-visual-audit-v8.pdf` with MathJax `unrendered=[]` and `pageErrors=[]`; visually inspected assembled pages 86--90 at A4 resolution. The decimation spectrum, decimator, interpolator, rational-rate converter, formulas, and table now render cleanly with no black fill, cropping, overlap, or raw formula source.
- Next: continue the page-density audit. Non-chapter-end low-density pages must be reflowed by carrying suitable following content upward; only chapter-ending and one-question training pages may retain a large writing area.

# 2026-08-13 典型序列分页续排

- 审核发现正文第 8 页在单位阶跃图后仍有可用空间，却将后续“矩形序列”整体误推到下一页；这不符合正文应自然续排、不得机械按块换页的规则。
- 调整典型序列段的图形续排规则：保留矩形序列标题、公式和说明在上页，完整坐标图在下页续排；并缩小续排图至仍清晰可打印的尺寸。第 9 页同时承接矩形图、实指数双图与正弦定义，内容密度恢复自然。
- 复核了原先的第 70、75 页：它们分别是第五、六章的章末页，符合章末可留较大空白的例外，未作无必要挤压。
- 验证：正文构建回归通过；`tmp/dsp-main-body-visual-audit-v12.pdf` MathJax `unrendered=[]`、`pageErrors=[]`，第 8--9 页 A4 视觉检查无公式裁切、图形切割或重叠。

# 2026-08-21 Chapter-five direct-I structure repair

- Root cause: the direct-I difference equation reused `n` as both the free sample index and the feedback summation index; the original SVG omitted the undelayed `b_0` path and its crowded routing did not permit a reliable reverse reading of the equation.
- Rebuilt the figure as a formal, layered Direct-I realization: an upper feedforward delay chain with `b_0,b_1,b_2,\ldots,b_M` taps, a lower right-to-left feedback delay chain with `-a_1,-a_2,\ldots,-a_N` taps, and a single standard summing node. Added `figure{break-inside:avoid}` so a technical figure and its caption cannot be split between A4 pages.
- Verification: targeted component, chapter-five-to-eight, and full-body assembly tests `3 passed`. Exported `tmp/dsp-main-body-ch5-structure-v3-stamped.pdf` with MathJax `unrendered=[]`, `pageErrors=[]`; rendered pages 64--67 show the full figure on page 65 with no overlap, crop, or broken formula.

# 2026-08-21 Chapter-six page audit

- Rendered and inspected all main-body pages of chapter six (pages 71--75) at A4 scale. The Butterworth and frequency-transform equations, impulse-invariance and bilinear-transform equations, examples, comparison table, headers and folios are clear and fully rendered.
- No raw LaTeX, clipping, overlaps, missing grids, or non-chapter-end large blank space was found. Page 75 is the chapter-end exception and its writing area is intentional.

# 2026-08-21 Chapter-seven page audit

- Rendered and inspected all main-body pages of chapter seven (pages 76--84) at A4 scale. Linear-phase derivations, standard `cases` formulas, FIR zero-pole, time/frequency correspondence, symmetry, and frequency-sampling figures all render clearly with no missing graphic, black fill, collision or clipping.
- The two classification/comparison tables retain all horizontal and vertical rules. Formula descriptions use `\text{...}` within their complete MathJax expressions, rather than raw mathematical-mode Chinese. Page 84 is the chapter-end exception; its writing space is intentional.

# 2026-08-21 Chapter-eight page audit and spectrum-axis repair

- Rendered and inspected all main-body pages of chapter eight (pages 85--90). Its multirate formulas, tables, anti-alias/interpolation/rational-rate system diagrams and page furniture have no black fill, crop, collision or raw formula source.
- The four panels in figure 8-1 were the remaining coordinate-axis exception: each had a frequency axis but lacked a vertical amplitude axis. Added a separate upward amplitude axis and label to every panel, with paths that remain clear of the triangular spectra and tick labels. A regression locks all four axes and labels.
- Verification: targeted component, chapter-five-to-eight and full-main-body assembly tests `3 passed`. Regenerated and stamped PDF reports `unrendered=[]`, `pageErrors=[]`; its A4 page 86 was visually checked. This completes the current main-body chapter audit; training, consolidated detailed answers and appendices remain intentionally frozen pending the requested next phase.

# 2026-08-21 Chapter-eight full coverage recheck

- Rechecked the source-model count against the body component: `full/source/chapter_08.json` contains 47 source pages and 47 transcribed pages. In the current A4 export, sections 8.1--8.5 begin on pages 86, 88, 89, 90 and 91 respectively.
- Rendered and visually reviewed every chapter-eight body page (86--91) at 160 dpi. Formulas, ruled table, spectrum transformation graphic and all three multirate system diagrams are clean: no raw TeX, black fill, crop, collision, non-chapter-end large blank, slide shell or watermark was found.
- The following page starts the frozen training section, so its intentional one-question writing space is not treated as a body-layout defect. Targeted component regression passed (`1 passed`).

# 2026-08-21 Chapter-one full A4 audit and zero-order-hold reflow

- Rendered and inspected all 30 first-chapter body pages. The MathJax formulas, `cases` structures, stem/continuous signal diagrams, sampling spectra, block diagrams and headers are clean; no raw TeX, overlap, crop, black fill or non-chapter-end technical-figure loss was found.
- Fixed the remaining non-chapter-end whitespace defect around the zero-order-hold waveform. The previous `figure` rule pushed the whole chart after its explanatory lead, leaving page 27 underfilled. Allowing arbitrary splits instead separated the caption from the chart, so that attempt was rejected.
- The dedicated `zero-order-hold-flow` class now retains its caption and chart as one unit at a print-legible 145 mm maximum width. The full unit fits at the end of page 27; the formula and following explanation flow naturally on page 28. A 160 dpi visual check confirms both pages are dense, clear, and uncut.
- Verification: targeted suite `2 passed`; rebuilt `tmp/dsp-main-body-ch1-reflow-v2.pdf` reports `math=2858`, `unrendered=[]`, `pageErrors=[]`.

# 2026-08-21 Chapter-seven supplemental training completion

- Added the remaining eight verified chapter-seven training questions and their consolidated detailed answers: 2007 (window-function effect and frequency sampling), 2015, 2016, 2017, 2019, 2021 and 2024.
- The 2007 frequency-sampling question retains the original response condition through a clean, watermark-free textbook coordinate plot; the associated comb-filter/resonator-bank realization is a formal SVG. The assembled book now contains 110 training question pages.
- Verification: six focused suites report `41 passed`; assembled HTML checks confirm two frequency-sampling realization figures, one target-response plot, no reader-facing `mod N`, and no watermark text. New PDF visual QA remains blocked by the known local headless GPU backend failure and is not claimed as complete.

# 2026-08-21 Chapter-three supplemental training batch one

- Added six source-checked DFT questions and detailed answers: 2003 cyclic-shift and IIR IDFT judgement items; 2006 ten-point DFT/circular convolution and overlap-save count; 2007 finite-sequence unit-circle sampling and spectral-leakage explanation.
- Circular indices use the required outer period subscript notation, and every formula in the new questions and answers is an intact MathJax expression.
- Verification: eight focused component/full-assembly suites report `51 passed`. The assembled handout has 116 training question pages; static checks found no reader-facing `mod N`, malformed raw-math fragments, or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch two

- Added eight source-checked DFT/DFS questions and answers: 2013/2014 real-even periodic DFS coefficients, 2015 z-domain sampling and circular-convolution length, and 2016 spectral decimation, cosine DFT, picket-fence effect, and real-sequence packing.
- The packed-real-sequence solution uses the required complete outer-period index notation and every mathematical expression is a complete MathJax unit.
- Verification: nine focused component/full-assembly suites report `52 passed`. The assembled handout now has 124 training question pages; static checks found no reader-facing `mod N`, malformed raw-math fragments, or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch three

- Added five source-checked DFT questions and detailed answers: 2017 DFT time shift/circular convolution, five-point circular convolution and frequency-resolution design, plus 2019 and 2020 picket-fence-effect questions.
- Every circular index now uses the required outer-period subscript notation; all formulas are complete MathJax units and the original question wording is retained.
- Verification: ten focused component/full-assembly suites report `53 passed`. The assembled handout has 129 training question pages; static checks found no reader-facing `mod N`, malformed raw-math fragments, or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch four

- Added three source-checked DFT questions and detailed answers: 2020 frequency decimation/2-point IDFT, circular time shift and six-point circular convolution, and 2023 frequency-domain multiplication.
- All circular indices retain the required outer-period subscript notation; every formula is a complete MathJax unit and the original question wording is retained.
- Verification: eleven focused component/full-assembly suites report `54 passed`. The assembled handout has 132 training question pages; static checks found no reader-facing `mod N`, malformed raw-math fragments, or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch five

- Added the source-checked 2023 overlap-save question and detailed answer, including the segment count and exact first/last retained-input blocks.
- The original question wording is retained; every derivation is a complete MathJax expression with no reader-facing modular-operator notation.
- Verification: twelve focused component/full-assembly suites report `55 passed`. The assembled handout has 133 training question pages; static checks found no reader-facing `mod N`, malformed raw-math fragments, or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch six

- Added the source-checked 2024 real-even finite-sequence DFT question and its paired-term cancellation proof for the Nyquist-index value.
- The question wording is retained and all derivations are complete MathJax formula units.
- Verification: thirteen focused component/full-assembly suites report `56 passed`. The assembled handout has 134 training question pages; static checks found no reader-facing `mod N`, malformed raw-math fragments, or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch seven

- Added the source-checked 2003 DFT spectrum-analysis question, preserving its wording and providing the complete Nyquist sampling, DFT-length, digital-frequency and signed analog-frequency mapping.
- The signed-frequency mapping uses a standard LaTeX `cases` formula; reader-facing circular-index notation remains free of `mod N`.
- Verification: fifteen focused component/full-assembly suites report `59 passed`. The assembled handout has 135 training question pages; static checks found no reader-facing `mod N`, malformed raw-math fragments, or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch eight

- Added the source-checked 2025 four-point DFT question and its conjugate-symmetric/conjugate-antisymmetric decomposition.
- The answer retains exact four-point values and uses the required outer-period subscript in \(X^*\left((-k)\right)_4\), never a `mod N` operator.
- Verification: sixteen focused component/full-assembly suites report `60 passed`. The assembled handout has 136 training question pages; static checks found no reader-facing `mod N` or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch nine

- Added the source-checked 2025 DTFT-sampling/4-point-IDFT question and its periodic time-domain summation proof.
- The detailed answer obtains \(y(n)=\frac{16}{15}\left(\frac{1}{2}\right)^n\) for the four-point principal sequence, with complete MathJax alignment.
- Verification: seventeen focused component/full-assembly suites report `61 passed`. The assembled handout has 137 training question pages; static checks found no reader-facing `mod N` or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch ten

- Added the source-checked 2025 real-symmetry DFT special-frequency question, retaining both original subparts and their separate pairwise-cancellation proofs.
- The answer uses standard display MathJax, including the \(k=N/2\) phase factor and complete finite sums.
- Verification: eighteen focused component/full-assembly suites report `62 passed`. The assembled handout has 138 training question pages; static checks found no reader-facing `mod N` or watermark text. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-21 Chapter-three supplemental training batch eleven

- Added the source-checked 2007 targeted-frequency DFT question and its 512-point zero-padding solution.
- The detailed answer gives the exact 16 Hz bin spacing and selects (X(50)) for 800 Hz.

# 2026-08-22 Chapter-three supplemental training batch twelve

- Added the source-checked 2024 four-point DFT question, preserving the original request for every \(X(k)\) value and for \(X_{\mathrm{ep}}(k)\).
- The detailed answer writes \(x(n)=\{2,0,2,0\}\), derives \(X(k)=2+2(-1)^k\), lists all four DFT values, and computes the even part with \(X^*\left((-k)\right)_4\); reader-facing circular indices remain in the required outer-period-subscript form.
- Verification: the targeted chapter 1/3/7/8 and full-assembly regression suite reports `70 passed`. The assembled handout has 140 training question pages; static checks found no reader-facing `mod N`, `\bmod N`, `\operatorname{mod}`, or `\mathrm{mod}`. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-22 Chapter-three supplemental training batch thirteen

- Added the source-checked 2021 whole DFT question with all four original subparts: six-point DFT, ten-point frequency-grid convolution, unit-circle six-point sampling, and spectrum leakage.
- The detailed answer derives each six-point DFT value, computes the non-aliased convolution \(\{1,3,6,10,14,18,15,11,6\}\), gives the six-point IDFT periodic summation, and explains leakage suppression without altering the question text.
- Verification: the targeted chapter 1/3/7/8 and full-assembly regression suite reports `71 passed`. The assembled handout has 141 training question pages; static checks found no reader-facing `mod N`, `\bmod N`, `\operatorname{mod}`, or `\mathrm{mod}`. PDF visual QA remains deferred because of the known local headless GPU backend fault.

# 2026-08-22 Chapter-one supplemental training batch one

- Added the source-checked 2016 impulse-sampling question and its full answer: sampling-period derivation, sampled \(\operatorname{Sa}(2t)\) waveform, and non-overlapping periodic spectrum replicas.
- The question preserves the original “麦奎斯特频率” wording. The answer uses intact MathJax formulas and separate, data-driven SVG figures for the time waveform and frequency replicas.
- Verification: focused component and full-handout tests report `5 passed`; full assembly now has 142 rendered training-page heads. Headless Edge launched an isolated profile but failed to write its requested screenshot, so visual rendering of this new answer remains explicitly pending rather than claimed complete.

# 2026-08-22 Chapter-one supplemental training batch two

- Added the source-checked 2007 sampling-spectrum question. Its question system is reconstructed as a clean multiplier-plus-sequence-generator diagram, preserving the original \(x(t)\), \(T\), impulse train and both requested calculations without retaining source watermarks.
- The detailed answer gives the triangular \(X(\omega)\), periodic \(X_p(\omega)\), the \(2\pi\)-periodic DTFT, and the requested values \(10^{-3}\) and \(2\). All conditional formulas use standard MathJax `cases` syntax.
- Verification: chapter-one batch-one/batch-two and full-handout suites report `7 passed`; `git diff --check` is clean; both generated SVG documents parse successfully and contain their expected axes, arrows and MathJax label regions. Edge screenshot generation remains unavailable, so this is not counted as completed visual QA.

# 2026-08-22 Chapter-one supplemental training batch three

- Added the source-checked 2025 periodic-pulse sampling question, retaining its \(T_s=10^{-2}\,\mathrm{s}\), \(\tau\ll T_s\), amplitude, period and pulse-width conditions. The original watermarked waveform was replaced by a clean coordinate plot.
- The answer distinguishes the finite-width pulse-train \(\operatorname{Sa}\)-weighted replicas from ideal-impulse equal-amplitude replicas, and explicitly concludes that the full square wave is not bandlimited. All reader-visible fractions, including \(\omega_s=\frac{2\pi}{T_s}\), use standard LaTeX fractions.
- Verification: all chapter-one supplemental and full-handout suites report `9 passed`; `git diff --check` is clean; both generated SVG documents parse successfully with complete axes, arrows and label regions. Visual browser screenshot QA remains pending because the local Edge renderer does not write screenshots.

# 2026-08-22 Chapter-one question-ID coverage audit

- Added an explicit manifest-coverage test instead of inferring completeness from the number of rendered training pages. It compares all 41 chapter-one IDs in `exam_training_manifest.json` with the priority component, existing supplemental component and the three newly added source-checked batches.
- Result: expected 41, covered 41, missing none, extra none. The 2016, 2007 and 2025 whole questions close the prior gap.
- Verification: the chapter-one coverage test plus all three supplemental batch suites and full-handout suite report `10 passed`; `git diff --check` is clean. The next training audit must use question IDs for chapters 4 and 6 as well.

# 2026-08-22 Chapter-four and chapter-six question-ID audit

- Audited the manifest IDs against the rendered chapter components rather than counting `exam-head` elements. Chapter six is complete: its 13 manifest IDs are represented by 11 training heads because the two 2005 subparts and the two 2006 subparts are correctly grouped by common original question.
- Chapter four has 12 manifest IDs but currently covers only `2017-q六-p5`, `2022-q五-p3`, and `2004-q十一-whole`. The confirmed missing IDs are `2024-dsp-p5`, `2007-q十一-whole`, `2007-q十三-p2`, `2015-qintro-p5`, `2015-qintro-p6`, `2017-q六-p4`, `2020-qintro-p1`, `2020-qintro-p5`, and `2023-dsp-p5`.
- Next action: add these nine chapter-four questions and detailed answers, using source-page verification for every original question text and required FFT flow figures.

# 2026-08-22 Global stem-origin correction and chapter formula-summary requirement

- Corrected the shared render_stem_svg primitive: the vertical axis now always passes exactly through the mathematical \(n=0\) coordinate. It is no longer shifted left to make a separate zero sample appear clear.
- The zero label is emitted once as an origin label in the fourth quadrant; zero is omitted from the ordinary horizontal-tick label list, so no false zero axis or duplicate zero label can appear.
- Regenerated the eight-chapter main-body HTML from the shared component. Static audit confirms all generated shared stem figures use the data-origin-at-zero marker and none retain the old first-sample-clearance marker.
- Added the hard delivery requirement that every chapter end with a complete chapter formula summary, rendered as standard MathJax / LaTeX formulas before that chapter’s training pages.
- Verification: shared-stem and full-main-body assembly suites report 6 passed; the separate chapter-seven priority-training suite reports 2 passed after converting its inline formulas to MathJax delimiters. py_compile passes for the modified SVG and chapter-seven component modules. Browser PDF visual inspection remains pending because of the known local headless GPU backend failure.
- Implemented the chapter formula-summary generator in the eight-chapter body builder. It strips SVG-only labels, collects display and inline MathJax formulas from each chapter, de-duplicates exact repeats, and inserts a forced-page-break formula summary before the next chapter or the training section.
- Verification after integration: all relevant assembly, shared-plot and chapter-seven MathJax suites report 12 passed. Regenerated main-body and full-handout HTML each contain exactly 8 formula-summary sections; the full-handout static audit reports zero old first-sample-clearance markers.
- Follow-up global audit: regenerated outputs contain 23 shared stem-origin markers and 23 matching fourth-quadrant origin labels in both the main body and full handout; both outputs contain zero old clearance markers and zero shifted-axis logic markers.
- Also corrected the independent chapter-one 2019 convolution training plot to omit the ordinary zero tick and label the actual (n=0) axis once in the fourth quadrant. The chapter-one supplemental 2016/2021 convolution plot implementations use the same origin treatment in the current worktree; their source file already contains unrelated uncommitted task work and is deliberately left unstaged for a later isolated commit.
- After rebuilding the complete handout, the static output contains 33 explicit `data-origin-at-zero` markers and 33 matching single origin labels; it contains no previous clearance marker or explicit left-offset expression.

# 2026-08-22 Chapter-four supplemental training completion

- Rendered and visually read the original 2007 pp. 22–23, 2015 p. 29, 2017 p. 37, 2020 p. 40, 2023 p. 50 and 2024 p. 54 exam pages before transcription. Added all nine audited chapter-four units: `2007-q十一-whole`, `2007-q十三-p2`, `2015-qintro-p5`, `2015-qintro-p6`, `2017-q六-p4`, `2020-qintro-p1`, `2020-qintro-p5`, `2023-dsp-p5`, and `2024-dsp-p5`.
- The component emits nine clean single-question training pages and centralized detailed answers. The two 8-point DIT questions retain an algorithm flow diagram; the 2023 flow also labels the specified input values after bit reversal. All answer mathematics is emitted as complete MathJax LaTex strings.
- Added a manifest-ID coverage test and full-book assembly checks. Verification: the chapter-four supplemental component, existing chapter-four component and full-handout suites report `8 passed`; `py_compile` and `git diff --check` pass. Pytest cache write warnings stem from a pre-existing denied `.pytest_cache`; a unique task-local `--basetemp` was used successfully.
- The standalone Chromium visual render still cannot start because its GPU process terminates even with `--disable-gpu`, matching the known Edge failure. No visual-QA completion claim is made for this new component; the defect remains queued for a separate rendering-environment repair rather than repeated browser retries.

# 2026-08-22 Late-chapter question-ID coverage audit

- Added a manifest-backed coverage test for the remaining training chapters. It records the rendered IDs in the fifth-, seventh-priority-, seventh-supplemental- and eighth-chapter components instead of inferring completeness from question-page counts.
- Result: chapter five covers 1/1 IDs; chapter seven covers 11/11 IDs across priority and supplemental pages; chapter eight covers 4/4 IDs. No question is missing or duplicated across those chapter assignments.
- Verification: the manifest audit, all four affected component suites and the full-handout suite report `9 passed`; all affected builders also pass `py_compile`.

# 2026-08-22 Complete generated stem-origin audit and chapter-seven table normalization

- Re-audited every generated discrete stem implementation and rebuilt the full handout. All 33 visible discrete plots now mark the actual \(n=0\) axis with `data-origin-at-zero`, carry exactly one fourth-quadrant origin label, and contain neither the former first-sample-clearance marker nor a separately rendered ordinary zero tick.
- The remaining custom chapter-one convolution builders now use the same exact \(n=0\) vertical-axis convention. Legacy ReportLab helpers only offset their label text; they are not used by the assembled handout and do not move a coordinate axis.
- Normalized the chapter-seven standalone table stylesheet with the complete horizontal-and-vertical textbook grid used by the full book; each table is kept together when possible. This prevents the standalone chapter view from silently losing the borders supplied by the final assembler.
- Verification: first created a failing component assertion for the standalone full-grid table rule, then applied the single style fix. Chapter-seven, body-builder and full-handout suites report `6 passed`; `py_compile` and both body/full HTML rebuilds pass. Static full-handout audit reports 33 origin-axis markers, 33 origin labels, and zero legacy clearance/left-offset/duplicate-zero-tick markers. Browser/PDF visual QA remains pending because the local headless Chromium/Edge renderer is still unavailable.

# 2026-08-22 Full-book assembly integrity and PDF-export root-cause check

- Rebuilt the eight-chapter body and complete handout after the origin-axis correction. The current assembled HTML contains 8 chapter formula summaries, 182 training-question pages and 81 centralized detailed-answer headings; static checks find no reader-visible MATLAB or source-identity wording, and no black SVG rectangle marker.
- The 153 remaining `详解见 P.____` instances are intentional final-pagination placeholders. They cannot be replaced safely until the exact final PDF page map exists.
- Isolated the current browser-export failure from the document content: Chromium fails even on `about:blank` with a fresh workspace-local profile, both with GPU disabled and with the SwiftShader software backend. In every case the GPU child exits with `-1073741790` while opening `GPUPersistentCache/DawnGraphiteCache`; the renderer never reaches HTML or MathJax. This is now treated as an external rendering-environment failure, not a page-layout defect; no further flag-by-flag retries will be used.
- Verification: body, full-handout, shared-stem and late-training-manifest suites report `11 passed`; all three core builders pass `py_compile`. Final PDF page-reference backfill and visual QA remain pending a separate, stable PDF export architecture.

# 2026-08-22 Offline mathematics-rendering audit

- The assembled handout currently loads MathJax only from `https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js`. No local `tex-mml-chtml.js` or equivalent MathJax distribution is present in the bundled Node runtime or either installed browser profile.
- Therefore an offline, filtered, or failed CDN load would leave the standard MathJax delimiters and LaTeX source visible instead of rendered mathematics, matching the previously reported raw-formula symptom. A final export solution must vendor a verified local MathJax distribution before page-map generation; this is coupled to the pending non-Chromium PDF-export architecture and is not safe to simulate with ordinary HTML text.

# 2026-08-22 Local MathJax installation and offline source switch

- With user approval, vendored the official npm package `mathjax@3.2.2` under `full/vendor/mathjax/`, including its license and all required ES5 component assets. The primary `tex-mml-chtml.js` file is present with SHA-256 `300480069078B5892D2363A2B65E2DFBBF30FE5C80F83EDBFECF4610FD093862`.
- The sole handout formula-rendering entry point now computes a local `file:` URI from the repository path, so all body, training and answer builders inherit an offline MathJax script without individually reintroducing CDN URLs.
- Verification: wrote the local-asset expectation first and observed it fail against the former HTTPS CDN source; after switching the shared entry point, formula, body-builder and full-handout suites report `7 passed`. The regenerated full handout contains one local MathJax script reference and zero CDN references. PDF visual validation is still pending the separately unresolved browser GPU export failure.

# 2026-08-22 Formula names and purposes in the offline PDF

- Replaced generic lead text such as “第 X 章核心关系”“某年真题的计算关系”“求和计算关系” with formula-specific Chinese names and purposes. Examples include the Nyquist sampling condition, the discrete-time frequency-shift relation, the ideal low-pass reconstruction response, and the frequency-domain energy relation.
- Hardened the MathJax source pipeline to decode HTML comparison entities inside LaTeX and removed a control-character source error. The static source audit is zero MathJax errors and zero form-feed characters, preventing the former black substitution blocks.
- Added TDD coverage for formula name-plus-purpose labels and for no duplicate label in chapter formula tables. Current relevant regression suites: `16 passed`. The regenerated full PDF is under final representative-page visual review; page-reference backfill and all-book pagination remain open.

# 2026-08-22 Formula labels name the formula, not its chapter

- Removed the remaining fallback that turned a heading such as “第三章 傅里叶变换” into “……中的推导公式”. Formula labels now classify common transformation structures directly (continuous-/discrete-time Fourier transform, z transform and Laplace transform), and the final fallback is structural rather than chapter-based.
- Added a red-green regression asserting that the Fourier integral is named “连续时间傅里叶变换定义” and states its time-to-frequency purpose. Relevant formula/export suites: `18 passed`.
- Rebuilt `full/outputs/dsp_full_handout.html`; scan result: 1,208 formula labels, zero occurrences of “核心关系”“中的推导公式”“第 X 章” or “某年真题的计算关系”. Final static MathJax/PDF rebuild remains coupled to page-reference backfill.

# 2026-08-22 Formula labels use semantic topics when needed

- Further reduced generic “数学等式关系” labels by using the current semantic knowledge-point title after removing chapter/section numbering; for example, an otherwise unclassified DFT formula is now “DFT 的基本性质的关系式（用于说明该性质中各变量的对应关系）”.
- Added red-green coverage for both a listed discrete sequence and a numbered section heading. After full-HTML rebuild, 1,208 labels contain zero chapter-number labels and zero “中的推导公式”; only 35 context-free mathematical equalities retain the structural fallback. Relevant regression suites: `20 passed`.

# 2026-08-22 Detailed-answer anchors for final pagination

- Added stable `answer-001` through `answer-150` identifiers to every detailed-answer heading in the book-end answer section. These anchors are invisible in the printed handout and form the authoritative targets for the next page-map pass.
- Created the anchor assertion first, observed the expected failure, then applied the smallest assembler change. All formula, assembly and MathJax regression suites report `21 passed`.
- The audited question bank has 156 assigned training units and the answer section has 150 answer headings, confirming that the final map must permit a small number of intentionally shared detailed answers rather than assume one-to-one title counts.

# 2026-08-24 Verified formula-purpose and answer-page milestone

- Refined formula lead text to name the actual formula and explain its concrete use. The new regression covers periodic impulse-sampling spectrum copies, an ideal low-pass response, and frequency-sampling target values; all targeted formula, full-handout and page-backfill tests pass (`19 passed`).
- Exported a 568-page offline-MathJax PDF. WeasyPrint reports no image or layout errors. Visual checks at answer pages 415 and 560 confirm correctly rendered LaTeX and formula labels that state what the formula is used for, not a chapter heading.
- Verified 153 printed training-page references against 150 PDF answer anchors: no missing target and no page mismatch; answer pages span P.415–P.567.
- Remains: map Appendix E navigation rows to audited detailed-answer anchors, then rerun final PDF and its visual/link audit. GitHub push is pending the external account-usage limit; no push retry was attempted.

# 2026-08-24 Chapter-one navigation identity audit

- Built the first auditable input to Appendix E backfill: all 41 chapter-one manifest IDs map exactly to `answer-001` through `answer-041`. The test verifies the manifest ID set, the continuous answer-anchor set and actual generated full-handout anchors together (`11 passed` with the full-handout suite).
- The mapping is intentionally marked partial. No Appendix E reader-visible page number is filled until every chapter has the same identity evidence.

# 2026-08-24 Chapter-four navigation identity audit

- Extended the audit map to all 12 chapter-four manifest IDs. They resolve to nine verified detailed-answer anchors (`answer-115` through `answer-123`); three pairs of independent subparts correctly share their parent-question answer.
- Identity-map regression and full-handout assembly tests remain green (`11 passed`). Audited coverage is now 53 of 156 question IDs; Appendix E remains intentionally unfilled until all chapters are equivalent.

# 2026-08-24 Late-chapter and IIR answer-identity audit

- Added all fifth-, sixth-, seventh- and eighth-chapter manifest IDs to the answer-anchor audit. The sixth chapter's 13 IDs correctly map to 11 answers because each 2005/2006 two-part original question shares one detailed solution.
- Audited mapping now covers 82 of 156 IDs across chapters 1 and 4–8. The next evidence-gated work is chapter two and chapter three; no reader-visible partial Appendix E backfill is permitted.

# 2026-08-24 Chapter-two and chapter-three answer-identity audit

- Mapped 72 additional manifest IDs by matching each training question's identifier, prompt, answer title, and decisive formula/data to its book-end anchor. Legacy repeated answer fragments were not treated as extra question identities.
- Mapping coverage is now 154/156. The two unresolved IDs are `2006-q十-whole` and `2007-q九-p4`: the current answer section lacks a matching detailed solution for each, so their navigation links remain deliberately absent.
- Verification: `pytest full/tests/test_exam_answer_anchor_audit.py full/tests/test_build_full_handout.py -q --basetemp tmp/.codex-test-anchor-map-green` → `11 passed`.
- Next action: source-check and add the two missing detailed answers before final Appendix E and page-number backfill.

# 2026-08-24 Missing detailed-answer completion

- Source-checked and restored the two genuine omissions: `2006-q十-whole` (piecewise DTFT magnitude-spectrum inversion) and `2007-q九-p4` (the unaffected range of an 80-point circular convolution). The original question wording is preserved; neither item is replaced with a similar question.
- Repurposed only the two legacy duplicate answer slots, `answer-066` and `answer-108`, so the existing 150-answer anchor sequence and all other answer navigation remain stable. The identity audit is now 156/156 with no unresolved question ID.
- Each new display formula has an explicit name and concrete purpose rather than a chapter label, including the inverse-DTFT integral, periodic-folding relation, low-index folding sum, and unaffected-interval criterion.
- Verification: targeted component, mapping, catalog, and full-handout suites pass (`14 passed`); the generated full HTML contains 150 answer anchors and exactly one occurrence of each new semantic formula label.
- Next action: backfill appendix E and all training-page answer references from this complete audited mapping, then generate and visually QA the final PDF. Push remains blocked by the external account usage limit.

# 2026-08-24 Appendix E identity-based backfill

- The audit metadata now truthfully marks the mapping as complete: all 156 training IDs resolve to verified answer anchors and no unresolved item remains.
- Added `apply_navigation_page_map`: it resolves each Appendix E `data-exam-id` to the audited answer anchor, then to the page read from the first-pass PDF. It validates all 156 navigation rows are replaced and rejects missing identities/pages; it never relies on table row order.
- Verification: backfill, full identity mapping, and full-handout suites pass (`13 passed`). Final PDF authoring is waiting on the required artifact-operation marker: the marker script is absent from this workspace and the default `node` command is unavailable. Source and tests are committed independently; no PDF success is claimed.
- Next action: when the marker tooling is available, build a first-pass offline-MathJax PDF, extract answer target pages, apply both training and Appendix E page maps, rebuild, and render-check the final PDF. Push remains blocked by the external account usage limit.

# 2026-08-24 Final offline-MathJax PDF export and visual QA

- Used the bundled Node runtime and completed the required PDF artifact marker. Rebuilt the full handout, prerendered every LaTeX block with MathJax SVG, and generated a 533-page A4 PDF at `full/outputs/数字信号处理讲义_最终页码回填版.pdf`.
- Diagnosed and corrected a WeasyPrint-specific formula bug: SVG dimensions expressed as `ex` collapsed to tiny unreadable fragments. The prerenderer now converts MathJax SVG dimensions and vertical alignment to physical points while retaining complete MathJax SVG rendering; regression tests cover the fix.
- Corrected long-table pagination so Appendix E starts with its first navigation table rather than leaving a title-only page. Its final reader-facing note says the pages have already been backfilled.
- Final page-map audit: all 150 detailed-answer anchors are present; the stable answer range is P.391-P.532. All 156 Appendix E rows and every linked training reference are mapped through the audited question identity map rather than row order.
- Visual QA rendered and inspected representative formula, table, DIT-FFT, filter-structure, training, appendix-navigation and detailed-answer pages. No formula source, missing-image warning, black replacement block or formula-size collapse was observed in those samples. PDF exporter emitted only duplicate SVG marker-name warnings.
- Verification: `pytest full/tests/test_backfill_answer_page_references.py full/tests/test_build_all_main_body.py full/tests/test_prerender_mathjax_svg.py -q` -> `15 passed`; final PDF -> 533 pages, 150 answer anchors.
- Next action: commit this PDF/export and regression fix as a task-scoped milestone, then attempt the configured push once; keep progressing only after preserving unrelated dirty work.

- Milestone committed as `4550f28` (`Export full handout with readable MathJax formulas`) and successfully pushed to `origin/main`.

# 2026-08-24 Chapter-eight math and multirate diagram repair

- Reproduced the chapter-eight standalone-export defect: the training component used legacy parenthesized inline formulas, while conversion to MathJax units occurred only during full-book assembly. This left raw LaTeX visible in a direct chapter PDF.
- Applied the shared inline-math normalization at both chapter-eight component output boundaries, so a standalone chapter export follows the same complete-LaTeX-to-MathJax path as the full book. Regression coverage asserts the multirate question's frequency-domain expressions use MathJax delimiters.
- Repositioned the multirate input-spectrum axis end and the `X(e^{j\omega})`, `1`, `\pi`, and `\omega` labels to prevent the prior label collisions. Exported and visually inspected the repaired A4 page: the question formulas render, the system chain is legible, and the spectrum labels are separated.
- Verification: `pytest full/tests/test_build_chapter_08_training_mathjax_component.py full/tests/test_chapter_training_placement.py full/tests/test_exam_figure_queue_integration.py -q --basetemp tmp/.codex-test-ch8-inline-math-green` -> `5 passed`.
- Next action: commit and push this narrowly scoped chapter-eight repair, then continue the visual audit/redraw queue for remaining programmatic DSP figures before the next full-book candidate export.

# 2026-08-24 Chapter-two feedback-diagram port repair

- Visual export of the `2015-q四-01` redraw confirmed the two feedback paths ended as bare lines at the summer boundary, making their intended input ports ambiguous on paper.
- Added separate arrowheads to the lower-left and bottom summer-port paths; the branch gains and two delay stages remain separate. A regression first failed against the former bare paths, then passed after the smallest SVG change.
- Exported the repaired A4 training page and visually verified that both feedback arrows now enter the summing node rather than pointing at empty space.
- Verification: `pytest full/tests/test_build_chapter_02_supplemental_training_batch_four_mathjax_component.py -q -k 'not browser_dom' --basetemp tmp/.codex-test-feedback-port-arrows-green` -> `2 passed, 1 deselected`. The browser-DOM test remains environment-blocked by a local Edge exit code, but the offline MathJax-prerendered PDF was the artifact visually inspected.
- Next action: continue the source-figure audit with the chapter-two zero-pole and AM system figures, then collect these visual repairs into the next full-book candidate export.

# 2026-08-24 Global SVG MathJax label sizing repair

- A second-chapter visual export uncovered a cross-cutting defect: when a MathJax formula inside an SVG `foreignObject` was externalized for offline PDF output, the full layout slot width and height became the formula SVG viewport. Inline labels such as `H(z)` then expanded to diagram width and destroyed page layout.
- Added a red-green exporter regression. The externalizer now retains the formula's intrinsic MathJax aspect and scales it from the original foreign-object font size, centering the resulting label inside its reserved SVG slot instead of stretching it to fill the slot.
- Re-exported the chapter-two zero-pole and AM pages. The formerly page-sized inline formulae now render at label size; the AM spectrum's axis labels, peak label and π/ω annotations are readable and separated. The chapter-two source CSS now scopes diagram sizing to figure SVGs rather than all SVGs, so ordinary inline MathJax SVGs cannot inherit diagram width.
- Verification: `pytest full/tests/test_prerender_mathjax_svg.py full/tests/test_build_chapter_02_training_mathjax_component.py -q --basetemp tmp/.codex-test-static-svg-math-size-green` -> `6 passed`; visual inspection performed on the regenerated offline-MathJax A4 PDF pages.
- Next action: commit the shared exporter fix, then rerender the previously reviewed chapter-eight graph and continue the remaining figure queue before the full-book export.

# 2026-08-24 Figure-queue visual audit: chapters two and eight

- Re-rendered the chapter-eight multirate training page after the exporter repair. Its graph-label source font was raised from 16px to 22px for the 960-unit canvas, so printed formula labels remain readable without changing the established body or display-formula scale.
- Performed static-PDF visual checks and promoted four reviewed redraws from pending to verified: `2015-q四-01` (two-delay feedback), `2015-q七-01` (zero-pole plane), `2021-q六-01` (AM modulation/demodulation) and `2015-q八-01` (multirate chain and triangular spectrum). Each was checked for watermark removal, required labels and diagram-specific structural correctness.
- The chapter-eight output retains separated \(\pi\), \(\omega\), spectrum-height and vertical-axis labels after the global exporter change; no source-formula text remains in the training question.
- Verification: `pytest full/tests/test_build_chapter_08_training_mathjax_component.py full/tests/test_prerender_mathjax_svg.py -q --basetemp tmp/.codex-test-ch8-readable-labels-green` -> `6 passed`, plus visual inspection of regenerated offline-MathJax A4 pages.
- Next action: audit the remaining seven queue items, then rebuild a new whole-book candidate PDF so all reviewed figure repairs are assessed in their actual assembled layout.

# 2026-08-24 Test-environment note after figure-audit commit

- Commit `948c136` contains the four verified queue-status updates and the readable-label adjustment for the chapter-eight source figure.
- The broader selected suite produced `10 passed, 1 deselected, 1 failed`; the one failure was `test_2021_am_question_keeps_all_formulae_in_mathjax_after_typesetting`, before assertions, because local Edge headless exited with `2147483651`. This is the same host-level Edge launch condition seen in the feedback-diagram browser-DOM test, not a formula or SVG assertion failure.
- The independent offline-MathJax pre-render plus WeasyPrint PDF export and visual inspection remain the valid artifact-level checks for these four figures while Edge headless is unavailable.

# 2026-08-24 Coordinate-origin and static-SVG visibility repair

- Recorded the global coordinate rule: in every coordinate figure, the vertical axis/horizontal-axis intersection is the sole zero-index location; the origin label appears once at the lower right and no separate zero axis or offset zero sample is allowed.
- Repaired the 2019 sampling answer figure where the lower time-domain axis was ten SVG units left of its zero sample. The y-axis and (t=0) sample now share one coordinate; the duplicate tick label was removed, leaving one origin label.
- Static A4 PDF audit also exposed an export-specific defect: outer-page CSS was not reliably inherited by inline SVGs, so the same plot could lose its axis and stem lines, while an unstyled polyline was filled black. The repaired source inlines the curve, axis, tick and stem paint attributes. Visual PDF inspection confirmed the black fill is gone and the coordinate lines render instead of arrowheads alone.
- Verification: `pytest full/tests/test_chapter_one_zero_axis_contract.py full/tests/test_chapter_one_convolution_origin_axes.py full/tests/test_signal_plot_svg.py -q --basetemp tmp/.codex-test-origin-axis-final` -> `8 passed`; offline MathJax plus WeasyPrint export was rendered and inspected.
- Next action: commit this scoped first-chapter coordinate repair, then continue the remaining original-figure audit before the next whole-book candidate export.

- Completed milestone: commit `692e694` (`Repair zero-origin sampling diagram`) was pushed successfully to `origin/main`.

# 2026-08-24 Chapter-one convolution stem visual audit

- The 2016 convolution redraw had correctly matched the (n=0) vertical axis in source coordinates, but its axes, stems and ticks still depended on outer CSS. The static PDF export could therefore degrade into arrowheads or lose linework.
- Added a red-green regression requiring direct SVG paint attributes for both 2016 and 2021 convolution figures, then inlined the axis, tick, stem and sample-dot paint in both renderers.
- Exported the 2016 question page to offline-MathJax A4 PDF and visually checked both input panels: axes are visible, (n=0) shares each vertical axis, each origin is labelled once, positive/negative rods retain their data values and no watermark/black fill remains. Queue item `2016-qintro-01` is now verified.
- Verification: `pytest full/tests/test_chapter_one_zero_axis_contract.py full/tests/test_chapter_one_convolution_origin_axes.py full/tests/test_signal_plot_svg.py -q --basetemp tmp/.codex-test-convolution-inline-green` -> `9 passed`.
- Next action: continue the first-chapter source-figure queue with `2019-q二-01`, `2021-qintro-01`, `2022-q七-01` and `2025-q六-01` before rebuilding the all-book PDF candidate.

# 2026-08-24 Chapter-one 2019 convolution figure audit

- Repaired the 2019 convolution stem renderer to inline axis, tick, stem and sample-dot paint attributes. This is the same static-PDF compatibility correction already proven on the 2016 figure.
- Exported and visually inspected the actual A4 2019 training page: both (f_1(n)) and (f_2(n)) show full axes, visible integer stems, a single origin label per plot, (n=0) on the vertical axis, no offset zero axis, no duplicate zero labels, no raw LaTeX and no watermark/black fill.
- Queue item `2019-q二-01` is now verified. Regression: `pytest full/tests/test_chapter_one_zero_axis_contract.py full/tests/test_build_chapter_01_training_mathjax_component.py full/tests/test_chapter_one_convolution_origin_axes.py -q --basetemp tmp/.codex-test-2019-inline-green` had 5 relevant assertions pass; the only separate old-suite failure was the obsolete CDN-string assertion (`mathjax@3`) against the intentional local MathJax deployment.
- Next action: audit `2021-qintro-01` and remaining first-chapter source figures, then regenerate the whole-book candidate PDF.

# 2026-08-24 Chapter-one 2021 convolution figure audit

- Rendered the actual 2021 source-question A4 page rather than inferring correctness from component reuse. The two input stem panels retain their original integer locations and values, full axes, one origin label per panel, and no watermarks, black fill, missing rods or overlapping axis labels.
- The `n=0` coordinate is the vertical axis in each panel; the second input's zero value is correctly represented by the baseline rather than an invented nonzero stem. Queue item `2021-qintro-01` is verified.
- Next action: audit the first-chapter A/D chain (`2022-q七-01`) and finite-width pulse-sampling figures (`2025-q六-01`) using the same static PDF visual gate.

# 2026-08-24 Chapter-one A/D--D/A chain visual audit

- Rendered the actual 2022 A/D--(h[n])--D/A source-question page. The main signal flow is left-to-right with explicit arrowheads; input/output labels, the sampling-period mark, filter cutoff and the triangular input spectrum are all visible and separated.
- The spectrum has a complete horizontal axis, a zero-aligned vertical axis, a single origin label, and clear ±1 kHz marks. No watermark, surrounding courseware strip, black fill, formula source, crop or label collision was observed. Queue item `2022-q七-01` is verified.
- Next action: audit `2025-q六-01` before moving the first-chapter source-figure queue to the all-book rebuild stage.

# 2026-08-24 2025 sampling figure formula-source repair

- The 2025 finite-width pulse-sampling frequency-plot title embedded `\\operatorname{Sa}` directly in an SVG text node. That violates the global rule that reader-visible mathematics must be rendered as complete MathJax/LaTeX rather than literal source text.
- Added a failing regression, then replaced the title with an equivalent pure-Chinese explanation; the mathematical `\\operatorname{Sa}` relation remains in the adjacent complete MathJax display formula. The targeted component suite now passes (`2 passed`).
- The figure remains pending static-A4 visual validation before its queue status may be promoted.

# 2026-08-24 Chapter-one 2025 pulse-sampling visual audit

- Exported the 2025 question and detailed-answer figures through local MathJax pre-rendering and WeasyPrint, then visually checked both A4 pages. The square-wave origin has one lower-right `0`; its axes, amplitude labels and period marks are clear.
- The three frequency-domain panels have visible coordinate axes, distinct finite-width envelope and ideal-impulse stems, unambiguous labels, no raw LaTeX, no black fill, no crop and no watermark. Queue item `2025-q六-01` is verified.
- With `2016-qintro-01`, `2019-q二-01`, `2021-qintro-01`, `2022-q七-01` and `2025-q六-01` verified, all first-chapter source-figure queue items are now complete. Next action: audit remaining second-chapter queue figures, then rebuild the whole-book candidate PDF.
