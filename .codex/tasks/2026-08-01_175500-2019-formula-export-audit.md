# 2019 真题公式导出核查

## Objective

排查并修复第一章 2019 年图形卷积真题中可能出现的公式漏渲染；建立覆盖整份答案文档的导出前拦截。

## Evidence

- 当前 `chapter_01_handout.pdf` 第 57 页经实际渲染核对，2019 年真题的行内公式和展示公式均已显示为 MathJax 数学排版。
- 既有测试只抽查 2019 年题中的若干固定公式，无法保证其他公式片段不会以原始 TeX 分隔符输出。

## Required rule

PDF 导出前必须检查浏览器完成 MathJax 排版后的完整 DOM；若仍含 `\(`、`\)`、`\[` 或 `\]`，或不存在 MathJax 容器，则阻止导出。

## Result

- 已添加 `assert_mathjax_ready`：要求存在 MathJax 容器，且完整 DOM 不含任何未处理的 TeX 分隔符。
- 第一章答案组件在实际 PDF 导出前调用该拦截；未通过将直接中止导出。
- 已重新生成第一章答案组件与合成讲义；第 57 页实际渲染复核中，2019 年真题的全部行内与独立公式均正常显示。
- 回归：11 项第一章合成与公式测试通过；仅保留既有 `.pytest_cache` 权限警告。

## Status

Verified — 等待任务范围内提交并推送。
