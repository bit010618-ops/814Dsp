# 第 41 页加法器反馈箭头清晰度

## Objective

将第 41 页 2013 年真题结构图中加法器下方两条反馈箭头改为教材式、清晰的单向反馈表示，避免箭头尖端紧贴求和节点造成的视觉拥挤。

## Root cause

两条反馈回路把箭头尖端都放在求和器下方端口；即便端口不同，两个折线路径和箭头在节点附近仍显得局促。

## Plan

1. 先用回归测试规定：箭头应位于两个增益块的输出侧，回到求和器的末段不再带箭头。
2. 只重排该 SVG 的两条反馈线，不改变题干、数学关系或其他页面。
3. 重建 PDF 并在 220 dpi 检查第 41 页。

## Status

Completed

## Result

- 两个反馈箭头已从求和器下方端口移到各自增益块的输出侧；之后的回路线只保留清晰的折线接入。
- 两条反馈仍分别接入求和器的左下端口和下端口，未合并到主输入。

## Verification

- RED：新回归测试在旧布局上因缺少“增益块输出箭头”而失败。
- GREEN：批次四结构图与第二章合成测试共 5 项通过（仅保留既有 pytest 缓存权限警告）。
- 实际：重建 49 页候选 PDF，并以 220 dpi 检查第 41 页；无重叠、裁切或求和器端口拥挤。
