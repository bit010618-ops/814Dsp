# 第一章安全续排修复

## 目标

消除第一章普通正文页因组件整页不可拆分造成的大面积空白；仅在 PDF 的真实矢量内容间隙处分段续排，保持公式、图形与真题页完整。

## 根因

总装订器把每个普通正文组件页视作原子块。当前页仍有可用空间时，只要下一组件整页放不下，就直接新建页，导致第 6 页下半部留白。

## 已完成

- 确认最终第一章使用 `chapter_01_representation_mathjax_component.pdf`，其中分段函数为完整 `\\begin{cases}` MathJax 公式；旧 ReportLab 手工大括号组件不进入总装订流程。
- 装订器新增对普通正文 PDF 页的安全切分：只在真实矢量对象之间不少于 16 pt 的间隙处选择切线，永不切开公式、图形或真题训练页。
- 新增回归：第 6 页必须承接“离散时间信号的基本运算”。

## 验证

- 红灯：新增回归在旧装订器上失败，证明第 6 页未承接下一普通正文组件。
- 绿灯：相关 9 项测试通过；唯一警告是 `.pytest_cache` 无写入权限。
- 生成 58 页 A4 候选 PDF，逐页缩略检查，并在 180 dpi 下检查第 6、7 页：标题、正文、MathJax 公式、stem 图、页眉页码均无裁切或重叠。

## 下一步

继续第一章剩余真题覆盖和书末详解整合；最终整书完成后再回填实际详解页码。
