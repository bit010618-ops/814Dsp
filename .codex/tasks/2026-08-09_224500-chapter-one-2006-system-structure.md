# 第一章 2006 年离散系统结构真题

## 目标

将 2006 年第七题完整纳入第一章重点训练：保留原题三问，按源图真实信号连接重绘无水印、教材级 SVG 系统框图，给出传递函数、差分方程和单位阶跃响应的完整 MathJax 详解。

## 已核对范围

- 已审题库标识为 `2006-q七-whole`，完整归属第一章，且为本章三道重点训练之一；不拆题。
- 真题源卷第 18 页（PDF 第 19 页）题干：一离散系统如图所示，求（1）系统的传递函数；（2）描述系统的差分方程；（3）系统的单位阶跃函数响应。
- 源图连接关系已逐项核对：输入支路系数为 \(0.25\)、\(0.5\)、\(0.2\)；中间状态经一拍延时，延时状态向首加法器反馈系数 \(0.4\)，并以 \(0.2\) 进入输出加法器；中间状态以 \(0.3\) 进入输出加法器。

## 推导预核验

- 令 \(a[n]=0.25x[n]+0.4b[n]\)、\(c[n]=a[n-1]+0.5x[n]\)、\(b[n]=c[n-1]\)、\(y[n]=0.2x[n]+0.3c[n]+0.2b[n]\)。
- 由此应得到 \(H(z)=(0.35+0.175z^{-1}-0.03z^{-2})/(1-0.4z^{-2})\)，并可反向恢复同一差分方程；正式写入前须以生成的结构图和代数推导双重核验。

## 状态

- 已完成源图视觉核对、题库归属核对和结构方程草核。
- 先添加失败回归，确认该题、结构图和详解尚未存在；实现后补充真题组件回归为 `6 passed`。
- 训练页中的 SVG 使用标准加法器、延时器、增益块、分支点和带方向箭头的正交信号路径；输入直通、反馈、状态和输出汇总关系均可由正文方程反向验证。
- 临时训练组件第 7 页、详解组件第 7--8 页均以 180 dpi 检查：图中线条、标签、箭头和增益块无重叠；传递函数、差分方程、根号、分数和阶跃响应无裁切或源码泄漏。
- 使用正确的 `<临时根目录>/full/outputs/` 目录安全合成整章候选 PDF（68 页），未覆盖正式输出；训练题位于第 52 页，书末详解位于第 66--67 页，已完成 180 dpi 视觉检查。
- 后续审计发现该题原本已在“分章强化训练”中，但旧图的连接关系与源图不一致，且旧详解的传递函数不正确。因此已将核对后的 SVG、原题题干与完整 MathJax 详解迁入唯一的重点训练入口，并从补充真题组件移除该题，避免重复。
- 修正后重点训练与补充真题的 11 项回归测试通过（仅有既有 pytest 缓存权限警告）；临时重点训练 PDF 第 2 页和详解 PDF 第 2 页按 180 dpi 复核，结构图、MathJax 公式、分数、根号和页内排版均无裁切、重叠或源码泄露。

## 后续

- 全书最终分页完成后，将训练页的 `详解见 P.____` 回填为最终页码。
- 本批仅包含任务相关脚本、测试与记录；待明确的提交/推送授权后，再单独审查并提交。
