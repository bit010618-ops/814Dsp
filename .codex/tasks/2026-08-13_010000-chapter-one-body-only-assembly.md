# 第一章正文专用装配

## 目标

建立第一章的纯 MathJax 正文入口，剥离训练与旧 PDF 拼接链，并保留离散信号、系统、差分方程和连续时间采样四个原知识板块。

## 下一步

已确认新入口不存在时测试失败；随后建立 `full/tools/build_chapter_01_body_only.py`。

## 已完成

- 用 MathJax 重建第一章导入内容：离散采样、采样周期与采样频率、奈奎斯特条件。
- 按原章节顺序接入序列表达与运算、系统性质、卷积、差分方程、采样与恢复、模数处理链和章节收束正文。
- 未调用旧 PDF 组件、训练、补充训练或答案组件；生成的正文没有 MATLAB、真题、公式图片或 `drawImage`。
- 正文专用装配测试通过。

## 2026-08-13 源页补漏

- 对照第一章源页 5–6，补回钢琴音频采样率实例及 \(44100\,\mathrm{Hz}\)、\(f_s/4\)、\(f_s/8\)、\(f_s/16\) 四档采样率的完整关系。
- 该补充保留基础采样知识，不包含原课件中的音频播放、MATLAB 或实验操作内容。
- 正文入口和全书正文基线已重新生成，相关测试通过。

## 2026-08-13 公式静态审计

- 新增精确断言，防止“时间间隔 T”这类正文行内数学变量退化为普通字符。
- 该断言先确认失败，再将正文中的 `T` 修复为完整 MathJax 行内公式 `\(T\)`；章节一与全书正文装配测试均已通过。

## 下一步

将第一至第八章正文接入全书正文总装配，并在不含训练的版本上开展首次整体分页检查。
