# 全书正文总装配

## 目标

将第一至第八章的正文专用入口合成为一份可编辑的全书主体；真题、答案、附录和目录继续排除。

## 下一步

已确认总装配器不存在时测试失败；随后建立 `full/tools/build_all_main_body.py`。

## 已完成

- 第一至第八章正文按原知识顺序合并为同一个可编辑 MathJax HTML 主体。
- 每章以 `chapter-start` 作为章节边界，只有章节之间换页，章节内部自然续排。
- 不调用训练、补充训练、答案、目录或附录；测试确认不含真题、MATLAB 或强制练习分页。
- 已生成 `full/outputs/dsp_main_body.html` 作为后续全书分页与图形终审的正文基线。

## 下一步

对全书正文基线进行完整公式渲染与分页检查，优先修复正文遗漏、公式异常与非题目页大面积留白；完成后再回接冻结的真题模块。
