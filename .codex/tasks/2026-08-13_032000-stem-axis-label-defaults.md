# 正文 stem 图坐标轴标签修复

## 目标

修复全书正文中由统一 stem 绘图函数生成的空坐标轴标签，使离散序列图满足教材图的基本坐标标注要求。

## 已完成

- 静态审计发现正文共有 46 个 `axis-label`，其中 43 个为空。
- 先新增失败测试，规定调用方传入空标签时必须补齐横轴 `n` 与纵轴“幅值”，且不允许输出空标签节点。
- 在统一 `render_stem_svg` 入口加入安全默认值，不逐图手工修补。
- 重新生成全书正文：46 个坐标轴标签均为非空；绘图与正文装配测试共 6 项通过。

## 下一步

继续审计正文图形的轴线、标题和边界，并逐章核对唯一知识点、公式和图形覆盖；真题模块继续冻结。
