# 第五章逐页图形与版面审计

## Objective

对第五章数字滤波器结构的 A4 主讲义正文逐页审计，核验结构图的箭头端口、标准元件、裁切边界、公式整式渲染、表格和正文自然续排；发现不合格内容即修复并提交。

## Result

- 修正直接 I 型的差分方程反馈下标：使用独立下标 `r`，避免将自由变量 `n` 误用为求和下标。
- 重画完整的直接 I 型结构：保留 `b_0` 直达支路、`b_1,b_2,\ldots,b_M` 前向抽头、`-a_1,-a_2,\ldots,-a_N` 反馈抽头、标准求和器和从右向左的反馈延时链。
- 所有第五章 `figure` 设为不可分页拆分；A4 PDF 第 65 页实测确认图题、图形、公式均完整、无重叠或裁切。
- 验证：组件、第五至八章组装、整书正文组装测试共 `3 passed`；PDF 导出报告 `unrendered=[]`、`pageErrors=[]`。
