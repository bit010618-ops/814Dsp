# 项目交接（兼容入口）

完整交接历史已原样归档到 `docs/archive/HANDOFF-full-before-compact.md`；请勿将其全部重新写回本文件。

新会话、上下文压缩或恢复后，请按以下最小顺序操作：

1. 阅读 `AGENTS.md`。
2. 阅读 `.codex/CONTEXT.md`。
3. 阅读 `.codex/TASK.md`。
4. 运行 `git status --short`、`git log -3 --oneline` 与 `git diff --name-only`。

随后只按 `.codex/TASK.md` 的明确指引按需读取文件。

详细需求与后续产品决定：

- `docs/requirements/2026-07-18-需求访谈与执行方案.md`
- `docs/requirements/2026-07-18-变更记录.md`

历史归档：`docs/archive/HANDOFF-full-before-compact.md`。

本文件只保留兼容入口；不得重新堆积完整里程碑、逐年审计和长日志。
