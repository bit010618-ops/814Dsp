# 历史全文生成基线审计

审计日期：2026-07-22

## 来源与导入方式

上一轮可修改工程来自本机 `C:\Users\HP\Documents\Codex\2026-07-01\zh` 的提交
`4231653`。已原样、隔离地导入本仓库的 `legacy_full/`，不覆盖本项目的
`full/`、`sample/`、需求记录或任务状态。

导入范围包括：13 个 ReportLab 批次生成器、数学与图形重绘辅助代码、86 个
旧工程回归测试、内容审计记录、稳定 PDF `legacy_full/outputs/华理814DSP讲义_完整彩色A4版.pdf`
以及对应交接文档。

## 可复现性证据

1. 从 13 个批次脚本重新生成中间 PDF，并用 `merge_full_handout.py` 合并成功。
2. 重建产物为 119 页 A4，约 14.93 MiB。
3. 在 `legacy_full/` 根目录运行旧项目全套测试，结果为 `246 passed`。

## 采用边界

这是一套可复用的**排版、数学渲染和图形重绘基线**，不是当前项目的最终讲义：

- 旧工程采用 7 章合并结构，当前任务锁定为源课件的 8 章顺序；不能把前者当作后者已经完成的证据。
- 旧工程的原课件追踪范围止于其历史 436 页版本，当前主课件审计范围为 1056 页，必须继续按 `full/source/chapter_01.json` 到 `chapter_08.json` 逐页覆盖。
- 旧交接记录仍列出第七批孤字换行和 IIR 直接 I／II 页回退问题；在当前输出中必须逐页复验并按用户最新标准修正。
- 页眉课程名、黄铜金细线、公式字号、真题组织和真题图水印要求均以当前仓库需求记录为准，不能被旧工程设置覆盖。

因此，后续实现以 `full/` 的 8 章内容清单为范围真相，以 `legacy_full/work/` 的成熟绘制函数为实现资产；每个新生成章节仍须单独渲染、内容核对、提交与推送。
